# AgtXIv V3：从论文到可核查、可复用的科学知识

**版本：** V3 / 3.0.0-design.1

**日期：** 2026-09-05

**状态：** 设计提案；本文和待办清单不代表实现、验证或发布已经完成。

**目标依据：** [CHARTER.md](CHARTER.md)，`AgtXIv-Charter/1.0`。当前仓库将 Charter 标为待批准；本提案按其目标设计，不改变其批准状态。正式生效仍按 [ADR 0007](docs/adr/0007-adopt-project-charter.md)。

**Authority note:** The proposed Charter remains pending. Only after ratification does the hierarchy become Charter > version specifications and governance > ADRs, contracts, schemas, and plans > implementation and generated outputs. This design proposal does not ratify the Charter or alter the contracts governing historical records.

**文档角色：** 当前跨版本设计入口及 V3 实施蓝图。V1、V2 的历史记录继续按各自绑定的契约解释；本文不修改旧版权威或自动迁移旧数据。

**阅读路径：** 本文看目标、框架和设计取舍；[V3 实施清单](docs/roadmaps/v3-implementation-plan.md)看任务、依赖和验收；[vibefeld 接入依据](docs/architecture/v3-vibefeld-integration.md)看固定版本的上游事实与接口限制。

**2026-09-07 实施进展：** 已开始实验契约 `0.0.0` 的本地实施。完整英文 JSON Schema 位于 [`schema v0.0`](schema%20v0.0/README.md)，附[中文逐字段说明](schema%20v0.0/SCHEMA.zh-CN.md)。[执行状态](docs/roadmaps/v3-execution-status.md)逐项区分契约覆盖、已检查的机制和仍待真实材料验收的工作；不表示 V3 整体完成。

**历史对照：** [重写前的 v0.6 阅读归档](docs/archive/specifications/AgtXIv-v0.6-pre-v3-2026-09-05.md)、[V1 bridge](docs/specifications/v1-bridge.md)、[V2 规范](docs/specifications/v2-paper-agentization.md)。

## 1. 先说明项目究竟要交付什么

AgtXIv 的目标是：**让下一篇论文能够从已有的、经过明确核查的知识前沿出发，而不必从头重走所有历史推导。**

这里的“核查”不是给论文盖一个“正确”的章。系统需要分别回答：作者原本说了什么、数学推理是否成立、形式化是否忠实、适用什么物理情景、有什么观测支持、计算能否复现。回答可以是有条件成立、存在反例、尚未检查或被某个依赖卡住。

Paper Agent 是一篇论文的可追问、可审计知识档案。它保存原文主张、推理结构、证据、争议和边界，并把能复用的部分接入公共知识库。对话只是访问这些记录的界面。

读者拿到一篇处理过的论文，应能直接看到五件事：

1. **论文在说什么：** 主要主张及其原文位置、前提和适用范围。
2. **为什么可能成立：** 用了哪些定义、旧结果、推理步骤、计算或观测。
3. **核查到了哪里：** 六个独立维度上的证据与未解问题。
4. **相对已有知识改变了什么：** 新结论、新证明、推广、修正、反驳或独立复核，以及明确的比较基准。
5. **下一篇论文能直接拿走什么：** 带版本、假设、适用条件和证据的知识条目，以及仍需补上的步骤。

### 1.1 V3 的范围和完成边界

V3 首先面向理论物理和数学结构明确的科学论文；以精确 arXiv 版本为主要输入，保留论文中的数学、模型、计算、经验和解释性主张。它不要求把每一个段落都变成节点，也不要求全篇 Lean 化。

V3 的主要交付物是一条**从整篇论文清点，到论证审阅，再到有条件知识积累的完整流程**：

- 一份查询无关、范围明确的 Paper Agent Release；
- 一组可独立复用的知识条目、关系和证据；
- 一份相对既有知识快照的贡献变化记录；
- 一份可追踪的未解决问题清单；
- 一次后续论文的增量复用记录，显示哪些旧工作被沿用、哪些必须重新检查。

其中“完整”指约定范围中的事项均有交代。发现错误、找不到依赖或没有完成形式化，都可以是有价值的结果。是否满足某一发布标准，要另按该标准判断。

V3 不以自动判定所有科学真理、全领域文献闭合、论文质量排名、完全自动发现新研究方向或大规模生产服务为完成条件。经验性论文的自动统计审查、多领域扩展和大规模运行留作后续；六个核查维度及缺失状态从第一版数据契约起就必须保留。

## 2. 从已有版本保留什么、调整什么

以下是对当前文件和历史报告的阅读判断，不是本次重新运行得到的结果。工作区还存在未提交的 demo、数据库和其他文件，不能仅凭目录存在宣称某阶段完成。

| 现有材料 | 值得保留的内容 | V3 的调整 |
|---|---|---|
| v0.3—v0.6 总体设计、[数学流水线](docs/specifications/mathematics-pipeline.md) | 来源定位、显式假设、多前提推理、形式化对齐、局部修复 | 把查询驱动的单目标流程放到下游；不以最小依赖图或 Lean 成功替代科学目标 |
| [V1 bridge](docs/specifications/v1-bridge.md) | 原始科学主张与数学表示逐组件对应，保留未数学化的科学含义 | 成为 V3 的源主张对齐机制；V1 的有界回答不升级为整篇论文审计 |
| [V2](docs/specifications/v2-paper-agentization.md) 与[执行计划](docs/roadmaps/v2-end-to-end-implementation-plan.md) | 先处理论文、冻结范围、独立审计、发布与逐条入库分离 | 加入论证中间层、六轴原生评估、知识基准及贡献变化；不再把包发布当作项目终点 |
| [V2 Charter 对照](docs/governance/v2-charter-conformance.md) | 已明确记录三个汇总轴与 Charter 六轴的冲突 | V3 使用新契约逐轴表达；旧记录没有证据的维度保持未知 |
| [Checkpoint D 审计](docs/audits/v2-m1-contract-kernel-checkpoint-d-2026-09-01.md)、[E 计划](docs/roadmaps/v2-m1-contract-kernel-checkpoint-e.md) | 已有契约、引用、清点和状态校验工作 | 按精确版本逐项复用；历史审计明确 M1/V2 未完成，E 计划/候选及当前代码不能视为端到端实现 |
| [科学主张研究](docs/specifications/scientific-claim-interface/main.tex) | 可独立修订的断言、来源跨度、归属和范围完整性 | 保留完整主张语义；不能只留下几个标识字段而丢失限定条件 |
| [贡献研究](docs/specifications/mathclaim-semantic-contribution/main.tex) | 数学等价与科学含义分离、贡献按基准比较 | 把贡献变化纳入主流程；研究中 `ASSUMED_VERIFIED_FOR_STUDY` 等记录仍是研究输入 |
| [database 原型](database/README.md) | 不可变文件、运行清单、知识记录和可重建索引分离 | 从查询运行原型扩展为论文发布与知识快照；不直接把现有索引当权威数据库 |
| Stabilizerness、agents、formal 及界面原型 | 有条件结论、反例、对齐失败和历史成果展示 | 作为迁移与验收素材；逐项检查证据，界面中的状态不构成新证据 |

核心改变：V3 把“处理一篇论文”放进“不断增长的知识库”之中；数学论证是科学知识生产的一部分，并有一个可审阅的中间层。

## 3. 把 Charter 转化为设计约束

本表是**设计覆盖**，不是已实现的合规证明。正式 V3 契约与后续合规审查还须绑定适用的 Charter 身份；若 Charter 获批，还须绑定批准提交。

| Charter 原则 | V3 必须保留的记录或机制 | 怎样判断没有偏离目标 |
|---|---|---|
| 1. 精确且可归属的来源 | SourceSnapshot、来源跨度、变换履历、贡献归属 | 任一主张和关键限定条件能返回准确来源或明确的系统推导 |
| 2. 显式推理与适用条件 | 论证步骤、假设范围、依赖、科学语义、缺口 | 每个结论能说明用了什么、遗漏什么、适用于哪里 |
| 3. 独立核查维度 | 六轴 Assessment；证据、评估和流程状态分离 | 没有一个全局 `verified` 字段把其他问题遮住 |
| 4. 保留不确定性与冲突 | FrontierItem、并存评估、范围清点、修订历史 | 失败和反例不会因归档、排序或新版发布而消失 |
| 5. 权威可审计、不能自我认证 | 生产者、审阅者、论文审计者、机械认证者、入库者职责分离 | 每次决定绑定对象、证据、规则和实际执行身份 |
| 6. 保留来源的复用与积累 | KnowledgeSnapshot、ReuseDecision、环境兼容记录 | 后续论文真正减少重复工作，且仍能追到原始前提和证据 |
| 7. 贡献是可追踪变化 | BaselineSnapshot、ContributionDelta、关系见证 | 能指出相对哪份既有知识增加或改变了什么 |
| 8. 查询无关、人与 agent 均可审计 | 固定论文范围、只读查询、结构化记录与解释对应 | 换一个问题不会偷偷改变论文主张或核查范围 |

Charter 的七项版本责任分别落实在：使命与交付物（第 1 节）、范围和验收（第 1、15 节）、原则覆盖（本节）、未实现能力（第 1、15 节及待办）、后续可解释性（第 12、14 节）、宪章要求与实现选择的分离（本节及第 17 节）。

## 4. 总体框架：两条生产线在同一主张上汇合

一条线重建**数学推理**，另一条线保留和检查**科学含义与证据**。贡献判断在二者已有结果上进行，并与固定的既有知识比较。

```text
精确论文版本 + 处理标准 + 既有知识快照
                    │
           来源清点 → 整篇主张清点 → 冻结处理范围
                    │
           原始科学主张及逐组件对照
                    │
          ┌─────────┴────────────────────┐
          │                              │
  数学论证工作区（vibefeld）        模型、物理对象、近似范围
  步骤 / 假设 / 异议 / 依赖          观测 / 计算 / 解释性证据
  ↔ 候选数学交接单 MathClaimIR             │
          │                              │
          ├→ 论证审阅证据 ────────┐       │
          │                      │       │
   按处理标准选择形式目标         │       │
   固定交接 → Lean 检查          │       │
   + 独立含义对照                │       │
          └─────────┬────────────┴───────┘
                    │
          六轴评估 + 依赖边界 + 冲突
                    │
        相对既有知识的贡献变化与复用判断
                    │
       独立论文审计 → 机械认证 → 论文发布包
                    │
         归档 → 逐条知识入库 → 新知识快照
                    │
         人和 agent 查询 / 下一篇论文复用
```

图中的箭头表示输入输出关系，不表示自动认可。论证审阅证据可以直接进入六轴评估；处理标准要求的形式支持义务仍必须满足。Lean 失败、异议或新增证据会触发局部修订；旧版本始终保留。科学适用性工作可与数学工作并行，不必等 Lean 完成才开始。

## 5. 原始主张必须始终留在场上

### 5.1 四类对象不要混在一起

| 对象 | 保存什么 | 不允许代替什么 |
|---|---|---|
| SourceSnapshot：来源快照 | 论文精确版本、原始文件、内容指纹、取得方式和缺失项 | 不能用标题或最新版本地址代替 |
| ScientificClaim：原始科学主张卡 | 可独立判断的原文断言，包含量词、条件、系统、语气、归属和来源 | 不能因证明方便而改成更弱的结论 |
| ArgumentWorkspace：数学论证工作区 | 如何从前提出发，谁提出异议、补了什么步骤、仍缺什么 | 不能作为形式证明或全部科学证据 |
| MathClaimIR：数学交接单 | 本次要证明的对象、定义、量词、前提、结论及对应关系 | 不储存全部讨论日志，也不自带“正确”状态 |

`IR` 在这里仅指便于机器处理的中间表示。vibefeld 记录“为什么这一步有理由”；MathClaimIR 固定“这一次究竟要证明什么”。**整理、固定版本、认可证据是三件不同的事。**

### 5.2 来源到主张的生产规则

先建立整篇论文的结构和粗粒度主张清单，再按已声明的处理标准细分承重主张。这里“承重”指删掉它会影响目标结论、贡献判断或适用条件。不能由临时用户问题决定哪些论文主张永久消失。

每个原文组件必须列入对应清单：数学表示、科学语义、证据说明、非主张叙述或尚未分类；一段话承担多种作用时分别标注。多处原文可共同支持一个主张，但必须逐条记录支持关系。

原始 TeX、展开后的文字和渲染内容分别有身份及变换记录。注释、未被引用的文件、宏展开和附录遗漏可能改变“作者实际发表了什么”，因此不能只扫描目录中的所有字符串。PDF 输入保留精确文件和页内定位，并记录提取映射；不能伪造不存在的 TeX 字节跨度。来源规范继续参考[既有采集设计](docs/specifications/paper-source-acquisition.md)。

### 5.3 补充条件时怎样避免偷偷换题

若原文是“对所有状态成立”，论证或 Lean 只能证明“对纯态成立”，必须保留两个对象：

- 原文主张及当前未解决或反驳状态；
- 系统生成的条件化命题，明确新增“纯态”前提，另设主张身份并记录 `specializes` 或其他适当关系。

修正提取错误可以生成同一原文断言的新表示版本，并记录被修复的语义差异；对原文断言本身改变真假条件、归属或适用范围，则必须新建命题。系统补出的引理注明系统推导，不自动归给作者。来源忠实只说明“作者确实这样说”，即使该说法被反例推翻也可以成立。

## 6. 数学 claim 中间层：用 vibefeld 展开可质疑的论证

### 6.1 为什么需要这一层

原文通常省略数学家认为显然的步骤，而 Lean 要求每一步满足明确的形式条件。直接翻译时，缺失前提、定义漂移、局部假设泄漏或证明目标被削弱，容易隐藏在生成代码中。

V3 在二者之间保留一份人能阅读、机器能定位的论证记录。提出论证的 agent 拆解步骤；另一个审阅 agent 尝试找反例、缺口和范围变化；有争议的部分继续展开，无法完成的部分留下明确缺口。

上游 vibefeld 已提供结构化自然语言证明、层级步骤、质疑记录、局部假设、事件重放和依赖导出。但在本次固定的提交中，Lean 导出仍列为未来能力；其信任模型也列明引用依赖、身份和历史完整性方面的限制。[固定版本 README](https://github.com/tobiasosborne/vibefeld/blob/392b2da3bee5766cca1a201f28e0255056baaba4/README.md)、[trust model](https://github.com/tobiasosborne/vibefeld/blob/392b2da3bee5766cca1a201f28e0255056baaba4/docs/trust-model.md)。

因此采用方式是：**vibefeld 作为论证工作引擎；AgtXIv 定义科学记录、跨论文依赖、核查与发布规则。** 上游的具体状态保留为证据原文，再由 V3 解释，不能直接成为知识库结论。

### 6.2 论证层的最小对象

以下是便于理解的概念接口。2026-09-07 起，对应的具体字段见[实验 schema `0.0.0`](schema%20v0.0/SCHEMA.zh-CN.md)；跨记录规则的实际实现及限制见[校验边界](schema%20v0.0/INVARIANTS.md)。概念接口不替代精确契约。

| 对象 | 必需内容 |
|---|---|
| ArgumentNode | 精确节点身份、陈述、节点种类、来源或推导归属、适用的假设上下文、修订来源 |
| InferenceStep | 共同前提集合、结论、推理理由、使用的定义、对应异议和证据 |
| AssumptionContext | 假设内容、来自原文还是新增、引入位置、有效范围、解除假设的步骤 |
| Challenge | 被质疑的精确节点/推理/依赖版本，问题类型、严重性、公开理由、提出者、处置及证据 |
| DependencyBinding | 被使用的精确知识条目/来源/形式声明、对象与假设对应、引用的用途及复用决定 |
| ArgumentReview | 审阅的精确快照、检查范围、结论、开放问题、审阅者和独立性记录 |
| ArgumentSnapshot | 完整论证输入、事件、节点、依赖、定义、假设、异议、版本和内容指纹 |

`ArgumentSnapshot` 可以完整记录一次未完成调查。保存完整记录，不意味着其中论证已经成立。

### 6.3 展示用的树与数学依赖图要分开

vibefeld 的层级树适合阅读；AgtXIv 的推理依赖必须允许一个引理被多处、跨论文使用。不能把父子关系直接解释为数学推导，也不能只检查树上的不确定性传播。

统一推理方向为：

```text
前提 A ─┐
        ├→ 推理步骤 S → 结论 C
前提 B ─┘
```

A 和 B 是共同前提，不能拆成“A 单独推出 C”和“B 单独推出 C”。另设 `contains` 表示文稿组织关系。旧版 `depends_on` 常从依赖者指向前提，适配时必须显式转换方向。

候选图可包含循环和错误边；一个被用于支持结论的证明方案必须说明如何避免循环论证。归纳或递归使用专门推理规则并检查其条件，不靠强行删除边来得到无环图。论文层面的相互引用允许有环，不等于同一数学证明允许循环自证。

对同一结论可以并存多个证明方案。风险沿各方案真正使用的依赖传播；一个失败方案不会自动否定另一个独立证明，也不能从展示树上随意删掉承重步骤来“清理”状态。

### 6.4 工作循环与停止条件

1. 输入精确科学主张、组件对应、候选交接单和已审阅的可用依赖。
2. Prover（论证提出者）提出有限的推理分解，标明省略步骤、外部结果与新增引理。
3. Verifier（论证审阅者）检查量词、定义、前提、局部范围、反例、引用适配和结论强度；异议必须定位到精确对象。
4. Prover 回应、细分或另立条件化命题；回复本身不等于异议已解决。
5. 独立审阅者决定异议是否已被解决、是否需要保留不同意见。审阅者的更换和异议撤回都记录理由。
6. 任一有用阶段可固定交接快照给 Lean。高风险局部步骤可以提前尝试形式化，不要求全树先通过审阅。
7. 达到资源预算、发现反例、缺少外部来源或多轮无进展时停止本次尝试，产出下一步所缺的最小信息。

工作项设定步数、成本、时间和无进展阈值。中止只产生 `BLOCKED`、`DEFERRED` 或其他适当处置；没有证明成功不能判为数学错误。

### 6.5 上游状态如何解释

| vibefeld 状态/字段 | V3 保留的含义 | 不允许推断 |
|---|---|---|
| `validated` | 上游报告特定节点获审阅接受；身份、范围和完整上下文经导入校验后，才可作为审阅证据 | Lean 通过、原文已验证、科学主张成立 |
| `clean` | 上游规则计算的树范围不确定性标记 | 所有引用都可靠、无未声明公理、全依赖已闭合 |
| `admitted` | 未完成证明而暂作前提的步骤，保留证明义务 | 知识库条目已获准入库 |
| `refuted` | 上游记录了反驳判定；仍需独立检查反例与精确目标 | 整篇论文或相邻主张均被推翻 |
| `archived` | 某分支停止活跃或被替代，历史仍保留 | 异议消失、依赖自动断开 |
| `closed` / readiness | 上游自身定义的流程状态 | 数学支持完整、V3 发布或复用条件满足 |

已有定理作为可靠输入、论文明确提出的数学假设、尚未证明的辅助引理，是三种不同来源。一个“假设 H 推出 C”的严格证明可以有效，但若原文要无条件 C，未证明的 H 必须继续显示为源主张的缺口。

### 6.6 接入边界

V3 不把 vibefeld 安装为全局可信核心，也不把其文件目录直接用作正式知识库。适配器须固定上游提交、命令协议、配置、事件区间和内容指纹，保留完整源数据，并创建 AgtXIv 自有的稳定引用。

图导出可用于导航，但不能单独充当完整交接记录：当前导出缺少部分上下文、假设范围、审阅依赖和完整异议正文；节点内容哈希也不代表完整审阅上下文。[具体核实与所需补充](docs/architecture/v3-vibefeld-integration.md)。

身份隔离、所有引用依赖上的风险传播、正式快照完整性、源主张对齐、Lean 交接、贡献判断和入库，均由 AgtXIv 明确负责。替换工作引擎后，只要保持这些接口，已积累的知识仍可解释。

## 7. 从论证交接给 Lean，再把结果返回科学主张

### 7.1 一次交接的内容

`FormalizationPacket`（形式化交接包）固定以下输入：

- 原始主张及组件对应版本，相关科学语义引用；
- ArgumentSnapshot 与本次选中的证明方案；
- MathClaimIR：对象类型、量词顺序、定义、单位、约定、归一化、数学前提、结论、精确/近似性质及误差范围；
- 每个前提的来源、有效范围及未解除的证明义务；
- 导入知识条目与形式声明的版本、适配关系；
- 计划形式化的节点集合、未选中部分及其原因；
- Lean 工具链、依赖包提交、预期声明和允许的公理政策。

命题意义变化产生新的交接版本。形式代码的语法修正不应迫使原始主张修改。可先固定一个含未解决义务的包进行尝试，但结果只能支持它实际证明的条件命题。

### 7.2 五类工作分开记账

| 工作 | 产物 | 能回答什么 |
|---|---|---|
| 生成 Lean 文件 | FormalizationAttempt | 是否生成了目标文件，以及诊断信息 |
| 检查形式声明 | FormalCheckRecord | 在固定环境中，该声明是否通过内核及声明/公理审计 |
| 独立反向解释 | BacktranslationRecord | 仅根据形式声明及必要定义，它到底说了什么 |
| 含义对照 | AlignmentAssessment | 原文、论证、交接单和 Lean 是否保留同一对象、范围与强度 |
| 数学与科学评估 | 六轴 Assessment | 每份证据针对哪个问题提供了多大范围的支持 |

生成成功、构建成功和声明被可靠证明分别记录。检查必须确认目标声明确实参与构建，并记录传递依赖、公理、占位符和任何超出所选可信政策的机制。把目标定理重新声明成公理，或在未参与构建的文件中保存证明，都不能满足形式核查要求。内核通过不自动证明所有前提彼此相容，也不证明物理假设成立；这些问题不能确定时保留未知。

### 7.3 对齐不是只有一次“翻译回来差不多”

至少保留三种独立对照：

1. **源主张 → 论证和交接单：** 有没有遗漏条件、改变对象、补入无来源前提？
2. **交接单 → Lean 声明：** 编码是否对应目标量词、定义和结论？
3. **源主张 → Lean 的直接复核：** 前两层是否共同继承了同一个提取错误？

反向解释者看形式声明、必要定义和环境，不先看源论文与预期自然语言答案；对齐审阅者再同时比较各方。不同 agent 使用同一模型不意味着错误独立，须记录模型、可见输入、实际身份和相关性限制。

Lean 找到另一条证明时，可能证明结论而未验证原文的证明方法。形式结果绑定其实际证明方案；原文方法的缺口保留。若论文贡献是“新证明”，仅证明相同结论不能确认该方法贡献。

### 7.4 物理直觉：转动物体与转动观察窗口不同

现有[固定测量窗口反例记录](agents/predicting-magic-from-very-few-measurements/reviews/reduced-rom-monotonicity-counterexample.md)适合说明 V3 为什么需要这几层。该历史记录区分：固定测量窗口下，状态旋转可使原本看不到的分量进入窗口；状态与窗口一起旋转则是另一命题。直观上像观察物体的投影，投影变大并不说明物体增加了某种资源。

V3 要保留“窗口是否随状态变化”这个条件：它既影响数学命题，也影响科学解释。即使 Lean 证明了“共同旋转”的版本，也不能用来认可“固定窗口”的版本。本次仅据此设计验收案例，没有重新运行或重新认证该反例。

## 8. 六个核查维度与其他状态如何共存

### 8.1 六轴是六个问题，不是六档分数

| 轴 | 读者问题 | 典型证据 | 不会自动推出 |
|---|---|---|---|
| `source_fidelity` 来源忠实性 | 作者确实如此断言吗？ | 精确来源及组件对照 | 断言成立 |
| `mathematical_correctness` 数学正确性 | 结论从所述数学前提能推出吗？ | 被审阅的推导、有效反例、对齐到目标的形式证明 | 适用于实际物理系统 |
| `formal_alignment` 形式对齐 | Lean 证明的就是所需命题吗？ | 原文/交接单/声明/反向解释对照 | 形式声明已获证明 |
| `semantic_applicability` 科学适用性 | 数学对象代表哪个模型、系统、近似和可观测量？ | 对象映射、近似条件、单位/极限/范围检查 | 有实验支持 |
| `empirical_support` 经验支持 | 观测是否支持相关假设或预测？ | 数据、方法、误差、不确定性及独立证据 | 推理必然正确或普遍有效 |
| `computational_reproducibility` 计算复现性 | 承重计算能在指定条件下重做吗？ | 固定代码、数据、环境、步骤与比较结果 | 模型和科学解释正确 |

数学正确性必须写明目标：是源主张的数学部分、某个论证步骤，还是一个形式声明。只有后者通过内核，不能把前两者直接标为同样获得支持。自然语言审阅和内核证明也须分别标明证据方法，而不共用模糊的“已验证”。

### 8.2 每份评估的最小结构

每条 `AxisAssessment` 至少包含：精确目标、单一轴、适用性、检查范围、方法、结果、条件、支持及反对证据、未解问题、评估者、独立性、规则版本和输入快照。

建议结果词表：`SUPPORTED`、`PARTIALLY_SUPPORTED`、`COUNTEREVIDENCE`、`INCONCLUSIVE`、`NOT_ASSESSED`。另外单独记录适用性 `APPLICABLE | NOT_APPLICABLE | UNDETERMINED`，以及执行处置 `COMPLETED | BLOCKED | DEFERRED | FAILED`。这样“程序失败”“没有调查”和“找到反例”不会混淆。

`NOT_APPLICABLE` 必须有原因和审阅范围。没有运行计算不等于计算不适用；没有做 Lean 不等于数学不适用。形式对齐对一个尚无形式表示的工作项可以暂不适用，但若处理标准要求生成该表示，未履行的工作义务仍保留，不能借此逃过验收。

多个评估可以不一致。原始评估不覆盖；派生状态视图按固定政策展示分歧，不能简单取最新或多数票。数学反例需匹配被反驳的量词、前提和范围；不匹配只能是待调查的反对证据。

### 8.3 完成、发布、入库和科学支持分别判断

- **清点完成：** 固定范围的每项义务恰有一个当前处置，同时保留所有历史尝试。
- **论证审阅：** 针对精确证明方案和范围的判断，可以包含未解异议。
- **形式核查：** 针对精确声明与环境的证据。
- **论文可发布：** 独立论文审计认为该包满足所选处理标准，机械认证核对其绑定与规则。
- **条目可入库：** 满足该类条目的独立准入规则，例如可作为来源断言、论证证据、条件定理或反例记录。
- **科学支持：** 只能从六轴、承重条件和范围中读出，不存在包级真理标签。

知识库允许记录反驳、失败和未解决问题，入库表示该记录的来源与评估足以被公开引用；不表示原命题为真。作为推理前提复用时，还需通过更严格的用途匹配。

### 8.4 V3 首批处理标准

所有标准都输出六轴及其缺失情况，区别在必须实际完成哪些工作，而不是删除哪些维度。

| 标准 | 必须提供的结果 | 可以保留的缺口 |
|---|---|---|
| `V3_SOURCE_MAP` | 精确来源、整篇范围清点、来源主张及范围审阅 | 数学和科学核查尚未进行 |
| `V3_ARGUMENT_AUDIT` | 上述内容，加承重论证、独立异议处置、依赖和科学语义检查；数学轴允许有条件或不确定结果 | 明列未形式化节点、外部依赖及经验/计算缺口 |
| `V3_FORMAL_SUPPORT` | 对冻结的目标集合提供形式证据和独立对齐；每个要求成功的目标都须满足声明与公理政策 | 其他主张的缺口，以及经验或适用性未解决事项 |

另用正交的证据要求标明必须进行的经验审查和计算复现，避免每加一项能力就创造一串组合标准。若所选标准要求某目标得到形式支持而未成功，可以保存调查包；不能宣称满足该标准，也不能在结束时偷偷缩小目标集合。

## 9. 查询无关的整篇流程

| 阶段 | 主要输入 | 主要产物与出口条件 |
|---|---|---|
| 0. 固定任务 | 精确论文版本、处理标准、资源预算、领域 | AgentizationPlan；记录基准知识快照，查询仅可作为触发原因 |
| 1. 取得并清点来源 | 原始文件、附属材料和采集记录 | 来源快照、缺失与许可信息；缺正文则保留阻塞，不能假装整篇已处理 |
| 2. 发现与冻结范围 | 论文结构、粗粒度主张、标准要求 | 全文清点、未分类项及独立范围冻结决定；先有发现，后有固定分母 |
| 3. 建立原始主张 | 冻结范围与来源组件 | 主张卡、组件对应、科学语义、证据角色；遗漏项可追踪 |
| 4. 解析依赖和基准 | 当前知识快照、引用及术语 | 候选关系、复用检查、外部缺口、比较基准；不能靠相似度认可依赖 |
| 5. 重建数学论证 | 数学组件、依赖、假设 | vibefeld 工作区、独立异议、证明方案与候选交接单 |
| 6. 选择形式工作 | 固定论证快照和处理标准 | 逐目标交接、形式尝试、内核证据、反向解释、独立对齐或明确处置 |
| 7. 完成科学核查 | 科学语义、模型和经验/计算证据 | 与数学并行的适用性、经验支持、复现评估；六轴分别汇总 |
| 8. 确定知识变化 | 冻结基准、主张及核查结果 | 贡献变化记录、争议、知识候选和未解决前沿；不得把作者自称新颖当结论 |
| 9. 独立论文审计 | 精确候选包和全部处置 | 论文审计记录，先依赖后结论；不生成自己正在审计的证据 |
| 10. 认证、发布与归档 | 精确审计及发布候选 | 机械认证、不可变发布包、归档回执；不自动认可包内所有条目 |
| 11. 逐条入库 | 精确发布包、当前知识快照 | 接受/拒绝/阻塞清单和原子入库回执；新增快照保留旧记录和冲突 |
| 12. 增量复用 | 新论文或新证据 | 复用决定、局部重查、新包/快照；查询始终指明所用版本 |

冻结后若发现遗漏主张或新的承重步骤，产生新范围版本，记录扩展原因并重审受影响部分；旧范围和处置保留。不能静默加减清点项，也不能删除失败项来满足标准。

任务预算耗尽时，把尚未查明的外部依赖保留为前沿。不要求先把所有引文递归转成完整 Paper Agent：一个来源明确、已核查且满足用途的单条依赖契约就可以成为边界；仅有引用不行。

基准比较快照与入库前快照可以不同。前者固定“贡献相对于谁”；后者固定“这次事务向哪里添加记录”。若工作期间知识库更新，重做相关比较或发布一份新比较，不能篡改旧基准来配合结果。

## 10. 把贡献和复用做成第一类产物

### 10.1 贡献是有基准的变化

每条 `ContributionDelta` 记录：

- 比较的精确既有知识快照、范围、选择理由和已知覆盖缺口；
- 本论文主张、证明方法、计算或证据的精确目标；
- 变化类型：引入、推导、另证、推广、特化、减弱前提、修正、限定、反驳、复现、独立验证、统一或新情景应用；
- 比较关系的见证：对象和条件如何对应，谁支持或反对该变化判断；
- 作者自述与系统评估分别归属；
- 未解决问题及不同合理基准下的敏感性。

“已有命题的新证明”仍可能有贡献；“数学表达等价但物理适用情景不同”不能抹成重复。反驳与独立复核同样属于知识增量。

找不到旧结果只能报告“在本次基准及检索范围中未发现”；不能推出全球首次或优先权。V3 不计算论文质量总分，也不把新增节点数当贡献大小。首版只实现可审阅的逐条变化和分类汇总，暂不追求全局等价类闭包或新颖性评分。

### 10.2 复用前必须过哪些检查

`ReuseDecision` 针对一次具体用途，至少比较：精确身份、对象、定义、量词、前提、结论强度、科学范围、证据方法、评估状态、冲突、环境和政策版本。

强定理可以在明确的特化映射下支持较弱目标；这不是把二者合并为同一个主张。一个只经过自然语言审阅的论证可以帮助构造下一份证明，但不能满足要求内核证据的依赖门。

复用既有 Lean 声明时还须有导入/环境兼容记录。缓存命中与内容相同只意味着可能省去某次工作，不自动证明目标和适用条件相同。

### 10.3 新知识与前沿一起积累

`FrontierItem` 记录缺什么、阻塞了哪些具体目标、何种新增证据可解除、已尝试过什么，以及适用范围。它可能是一个未证引理、缺失数据、对象映射歧义或相互冲突的实验证据。

因此知识库的增长不只统计正面条目，也包括发现的错误、被限定的结论、可复用的失败信息和缩小后的未解边界。后续论文可以直接从这些位置继续。

## 11. 软件框架和责任边界

### 11.1 模块按责任划分，首版采用模块化单体

V3 先用一个应用中的独立模块和受控工作进程，避免在记录语义尚未稳定时部署大量服务。边界由接口和权限定义，将来可按实际并发或隔离需求拆分。

| 模块 | 职责 | 权限边界 |
|---|---|---|
| Source & Inventory | 获取、冻结、解析来源和清点范围 | 外部内容进入隔离区；无知识入库或发布凭据 |
| Claim & Context | 主张、科学语义、组件对照 | 只能生成候选和修订，不能授予评估通过 |
| Argument Adapter | 控制 vibefeld、导出论证并补齐上下文 | 工作区不写正式知识库；保留上游原始状态 |
| Dependency & Reuse | 候选检索、依赖适配、局部影响计算 | 检索只提出候选；认可关系要有独立决定 |
| Formal Verification | 形式交接、生成、隔离检查和证据收集 | 生成者不能把自报构建结果当独立核查 |
| Assessment | 六轴、关系、贡献和异议评估 | 决定绑定精确输入；不覆盖被评估对象 |
| Release & Knowledge | 论文审计、机械认证、归档、逐条入库 | 角色、凭据、操作分开，原子添加并保留历史 |
| Query & Reader | 人类页面、agent 接口和缺口请求 | 标准查询只读；新工作由独立任务计划处理 |

记录持久化先采用不可变文件/对象和可重建索引，吸收现有 database 原型。SQLite 可用于本地索引；生产数据库、分布式任务队列和图数据库不是 V3 前几个里程碑的前置条件。

### 11.2 独立角色不等于很多不同名字

生产者、论证审阅者、形式对齐审阅者、论文审计者、机械认证者和知识准入审阅者分别承担不同职责。调度器只安排工作，不拥有科学判断权。

V3 对外使用“论文审计者”这一名称；兼容 V2 时注明其对应 V2 Root Agent。数学依赖边界称“基础契约”，不再复用 Root Agent 一词。

同一个 actor 不能审阅自己的生产物，也不能自己认证自己的审计。后续发现问题须退回新版本再审，不在原审计对象里边改边批准。验证身份应来自实际进程/账户与受控调度记录，而不是调用者随便填写的字符串。角色矩阵还须声明冲突利益与同模型相关风险；不需要把每个角色硬拆为独立部署服务。

保存公开理由、被检查的数学论证、异议和证据，不保存私有思维链。这里的证明步骤是交付给审阅者的数学对象，不是模型内部思考日志。

### 11.3 外部输入与执行隔离

论文、TeX、代码、上游工作区和模型输出均视为输入材料，不能作为改写项目规则的指令。来源采集和受控文献/声明检索是明确的外网边界；数学与形式工作进程默认无外网、无发布凭据。

后续实现须防止压缩包路径越界、源文件执行、资源耗尽、伪造校验结果和工作区覆盖历史。来源缺失、网络失败、许可不明分别记账；它们不能被转换为主张“无支持”的科学结论。

## 12. 版本、事件、依赖失效和知识存储

### 12.1 不可变记录及可重建视图

所有正式引用至少携带对象身份、修订号、类型和内容指纹；原始字节还需媒体类型、字节长度和字节哈希。显示名称、文件目录和最新版本别名只供导航。格式版本与科学命题版本分开。

正式记录封套包含来源/生产者、精确 schema、规则包、输入引用和时间；禁止在 MathClaimIR 内写一个可变的验证状态。评估与状态视图单独存在。

工作区可以变化；归档快照和评估不可覆盖。事件序列用于重建历史状态，同时记录前一事件和内容指纹。哈希链本身仍不能防拥有写权限的人整体重写，因此正式发布还需独立保存的检查点、身份凭证或签名/归档回执来固定历史。

发布包按无环依赖生成：冻结产物清单 → 对该清单的审计 → 绑定二者的认证 → 聚合包/归档回执。清单不包含尚未产生的认证哈希，避免循环计算身份。贡献比较引用已有基准快照；入库回执在新快照产生后绑定前后快照。

### 12.2 失效传播针对受影响的证据

| 变化 | 必须重新检查的部分 | 应保留的历史 |
|---|---|---|
| 修正来源提取，发现漏掉假设 | 受影响的主张对应、论证、形式对齐、科学评估和贡献 | 旧源版本与旧提取记录 |
| 某个依赖被反驳或限制范围 | 使用该依赖的证明方案、复用决定及其下游支持 | 原先在旧快照下作出的评估 |
| 采用新引理/定义版本，或发现旧版本错误 | 使用该对象的适配与相关证明；新版发布本身不改变旧绑定 | 旧版本记录及其他独立证明方案 |
| Lean 环境或公理政策改变 | 新环境下的导入、形式核查及相关准入 | 旧环境中检查成功的事实 |
| 新经验数据与原结论冲突 | 相关经验支持、科学适用性与贡献评估 | 独立成立的数学条件定理 |
| 只是页面展示变化 | 对应派生视图 | 来源、证据和科学状态 |

对失效使用“当前用途需复核”这一含义，不把历史证据改写为当时从未成立。相反，新发现的错误要作为新评估与关系附加，查询可以同时解释“当时知道什么”和“现在为什么不再这样使用”。

每次影响计算沿所有承重依赖、定义、局部范围、引用及评估输入追踪。开放异议引起复核义务，已证实的前提失效阻止该证明方案提供支持；二者不应被压成同一布尔标记。自动传播产生审查范围，不自动完成新的科学判决。

### 12.3 建议的未来目录

以下目录区分已开始的实验契约与后续运行系统；具体完成情况以执行状态为准：

```text
src/agtxiv_v3/
  source/  claims/  arguments/  dependencies/
  formal/  assessments/  contributions/  knowledge/  query/
schema v0.0/                      # 已创建的 V3 实验契约；不覆写 schemas/v2
contracts/v3/                     # 版本化处理与审阅规则
adapters/vibefeld/                 # 固定版本接口、完整快照、状态映射
fixtures/v3/                      # 后续正反向验收数据
```

每个论文发布包按来源、主张、论证、形式证据、科学证据、评估、贡献、未解问题与清单组织，并以清单中的精确引用决定权威。全局知识快照只引用已准入记录和关系。具体存储位置不参与科学身份，避免移动文件就“变成另一个定理”。

## 13. 人与 agent 如何使用

默认论文页面依次回答：“主要主张—依据—核查边界—知识变化—未解问题”。点击任一结论可展开原文、推理步骤、异议、证据和版本。六轴用各自文字状态展示，不提供一个容易误读的绿色“整篇通过”。

知识查询至少支持：读取某版本主张、查看其承重依赖、列出相关冲突、检查能否在指定条件下复用、比较两份知识快照、查看形式证据和未解问题。

每次回答绑定查询、源发布包、知识快照、评估政策和引用集合。摘要可以缩短文字，但相关反例、限定和冲突的两端记录必须保留。系统可以提供“为什么此处能复用”和“还缺哪一步”的解释，其每项结论都连接结构化记录。

没有已发布结果时，快速回答明确为临时调查；用户问题可以触发新任务，不能自动变成正式知识记录。更强核查须提出新处理计划；旧范围不会被改小。

## 14. 兼容与迁移

1. **保留旧版解释。** V1 有界回答、V2 发布包、旧 ScientificClaim/MathClaimIR 与现有数据库记录均保留原版本和哈希。
2. **新增绑定，不改写证据。** V3 用适配记录引用旧对象。来源已对齐、形式生成成功或研究中假定正确，只保留原含义。
3. **三个旧轴不能推导六个新轴。** 原始评估作为 legacy 证据附上；具备目标、方法、范围和证据兼容性的维度经独立评估后才可形成 V3 Assessment，其余未知。
4. **没有事件不能补造。** 旧自然语言推导可导入为历史论证材料；缺少审阅身份、异议或假设范围时列缺失项，不伪装为曾经发生过的 vibefeld 对话。
5. **旧结果不因补版本号获准复用。** 逐条检查对象、假设、环境、冲突和准入政策。旧版处置与 V3 决定并排展示。
6. **文档边界清楚。** 本次以 V3 重写此入口，并保留 v0.6 阅读归档；V1/V2 规范、schema、实现和历史审计不重写。旧文档中对旧 AgtXIv.md 章节的引用按对应归档/Git 版本解释。

正式实施应先迁移一个很小的旧记录集合，证明缺失信息和弱证据不会被升级，再考虑批量处理。

## 15. 实施顺序和 V3 验收

详细任务见 [V3 实施清单](docs/roadmaps/v3-implementation-plan.md)。所有里程碑均为未来工作，不能用本次文档交付替代实现完成。

| 里程碑 | 交付目标 | 前置 |
|---|---|---|
| P0 设计基线 | 目标、角色、六轴、版本边界和验收矩阵达成一致 | 本提案 |
| P1 最小契约 | 精确引用、六轴评估、范围、论证、复用/贡献核心接口 | P0 |
| P2 来源与主张 | 整篇来源清点、主张与组件对应、冻结范围 | P1 |
| P3 论证中间层 | 固定版本 vibefeld 适配、独立审阅、完整依赖与风险记录 | P1、P2 |
| P4 形式与科学核查 | 交接 Lean、独立对齐、科学语义及六轴证据接口 | P2、P3 |
| P5 知识变化与复用 | 固定基准、逐条贡献、跨论文复用与冲突 | P3、P4 |
| P6 审计、发布与入库 | 精确候选审计、机械认证、归档、原子入库 | P4、P5 |
| P7 增量生命周期 | 下一篇论文复用、影响追踪、只读回答、历史解释 | P5、P6 |
| P8 扩展准备 | 小批旧数据迁移、成本测量、生产化取舍 | P7 |

V3 的有界完成条件是：在一个冻结的小领域集合中，至少包含前置论文和后续论文，能走通整篇范围清点、论证审阅、六轴记账、贡献比较、发布与逐条入库，并在后续论文中实际沿用一项已获准的知识，同时拒绝一次条件不匹配的复用。若所选标准要求形式支持，必须有真实的声明检查和独立对齐证据。至少保留一个未解决前沿，证明“完整交代”没有被实现为“全部成功”。

评价采用来源漏项、错配前提、错误复用、错误升级、遗漏冲突、可追溯性及重复工作减少等指标。核查覆盖率分子与分母分别固定；成本减少比较同一任务范围和相同核查标准。不能以节点越多、通过率越高或形式化比例越大作为项目成功的单独证据。

### 15.1 必须进入后续验收的反向案例

| 案例 | 预期行为 |
|---|---|
| 原文已有“纯态/有限维”等条件，但提取遗漏 | 修订原文断言的表示，记录差异并重查对齐 |
| 为了证明新增原文没有的“纯态/有限维”等条件 | 另立条件化命题，不能覆盖或自动认可原文 |
| vibefeld 节点 `validated/clean`，但引用前提未证 | 独立依赖检查仍显示条件和缺口 |
| 归档或删除必要步骤，包括删除其引用 | 保留历史异议；独立解释原冻结义务如何由替代方案承担，不能靠删边绕过审阅 |
| 同一实际 actor 使用两个角色名 | 拒绝自审；不能只比较显示名字 |
| 定理声明为公理或目标文件未参与构建 | 不满足形式核查标准 |
| Lean 成功但证明了另一个命题 | 形式检查证据保留，源主张无相应支持 |
| 证明搜索失败 | 记录失败/阻塞，不判假 |
| 同一结论有两个证明，其中一个依赖被反驳 | 只复查受影响方案；独立证明仍单独评估 |
| 固定窗口与共同旋转窗口混用 | 拒绝科学/数学条件不匹配的复用 |
| 原文证明有缺口，但 Lean 找到另一条证明 | 区分结论支持与作者方法贡献 |
| 新数据反对物理模型 | 重查经验/适用性；不抹除数学条件定理 |
| 仅在基准中未找到相似结果 | 报告有界未知，不宣称全球首创 |
| 只归档包或写入索引 | 不自动取得逐条准入或正面科学状态 |
| 基准更新、并发入库或被审阅字节变化 | 保留旧比较，重做受影响决定；陈旧审批不能生效 |
| V1/V2 或研究用“假定验证”记录导入 | 原样保留旧含义，新轴缺失不补成通过 |

## 16. 当前交付与尚未解决的设计问题

本次完成的是文档层面的重构：V3 框架、上游接入分析、里程碑与验收设计。未运行 demo、vibefeld、Lean 构建、论文流水线或既有项目测试；未修改现有运行代码、数据记录或 Charter。

实施前需要在 P0/P1 固定的问题包括：哪些处理标准允许纯 agent 审阅、哪些科学问题需领域人员介入；最小可接受的身份/独立性证据；论证节点的合理粒度；经验与计算证据的领域政策；未来公开服务的许可与归档边界。默认采取保守的逐条声明范围和明确缺口，不因未决定这些扩展问题停止普通候选生产。

尚待证明的工程假设是：论证中间层是否减少目标漂移与 Lean 返工，以及跨论文复用是否真正降低同等核查标准下的成本。应在后续固定评估集上比较“直接交接”和“带论证审阅的交接”，报告正确性、对齐错误、人工审阅负担和成本；不能把采用 vibefeld 本身当作效果证据。

## 17. 哪些是不变要求，哪些可以替换

**来自 Charter 的不变要求：** 来源可归属、推理和范围显式、六轴独立、冲突与不确定性保留、不能自我认证、复用不丢来源、贡献相对基准可追踪、查询不能改写权威知识。

**V3 的实现选择：** vibefeld 工作引擎、MathClaimIR 交接格式、Lean 首选后端、模块化单体、不可变文件加索引、本文的记录名和里程碑顺序。它们可以通过版本化设计改进，但不得削弱前面的目标和边界。

V3 应积累的是可复用的判断依据：为什么这条结论在这些条件下得到支持，哪里仍需工作，以及后来的人怎样继续。
