# 本项目的协作约定

## 回答方式

用户容易迷失在上下文中。每次交付先说明结论，再说明本次改变和下一步；减少概念跳跃，解释必要术语，不把大量内部细节直接堆给用户。

## 测试策略（用户要求，2026-09-15 起）

- 默认只开发、阅读代码和规范、查看差异，不执行测试。
- 所有新增或受影响的待测试项统一维护在 `schema v0.1/PENDING_TESTS.md`，不要分散新建待测清单。
- 每项写清对象、准备条件、操作情景、预期结果和当前状态；涉及尚未实现或尚未决定的接口，单独说明前置阻塞。
- 未得到用户后续明确要求，不运行单元测试、集成测试、示例重放、校验器、smoke test、Lean 构建或其他以验证为目的的执行；不能换个“检查”名称绕过此要求。
- 可以阅读文件、查看差异并进行静态推理。不得把静态阅读称为测试通过，也不得把以前通过的测试结论套到本次修改。
- 用户以后要求执行时，仅执行其指定范围，保留运行证据，再更新清单；现有历史测试及其历史报告不删除、不改写。
