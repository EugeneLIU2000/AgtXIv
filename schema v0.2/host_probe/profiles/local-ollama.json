{
  "provider": "openai_compatible",
  "model": "qwen2.5:32b",
  "endpoint": "http://127.0.0.1:11434/v1/chat/completions",
  "auth": {"style": "bearer", "env": ["OLLAMA_API_KEY", "LOCAL_LLM_KEY"]},
  "generation": {"max_output_tokens": 16000, "json_mode": true, "temperature": 0},
  "note": "A model the reader runs themselves. Ollama and LM Studio both serve the OpenAI-compatible shape and ignore the bearer token, so any non-empty value in OLLAMA_API_KEY satisfies the host's credential check. This profile exists to make one claim testable: the protocol does not require anyone's hosted model."
}
