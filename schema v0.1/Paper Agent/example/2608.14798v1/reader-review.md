# 独立 reader / 接口审阅记录

实际分段与审阅任务身份见输出 manifest 的 participants。这里是本次工具返回意见的人工整理，不是数字签名或科学审阅回执。

## 审阅发现与处理

- 精简 Plan 与旧 required 字段冲突、义务不等于主张、提前检索与配对任务规则冲突：完整记录于 spec-review.md，未擅自修改用户规范。
- 不应为了消除 SELF_REVIEW 换一个假的 principal：保留所有 33 个失败 map，完整包 validation=false。
- 子集检查应列出排除对象，不能删引用获得通过：输出 validation.without_unreviewed_maps.excluded_record_refs 明列 33 个精确引用；剩余记录没有改写，使用相同原件重新进行引用与源字节检查。
- 补 visible_input_refs 不能倒填早期阅读历史：这些字段明确只描述保存种子后的记录装配，不是最初阅读论文时的输入历史；不生成假的调用回执。
- 稳定子附加态目标中的 n_A/n_B 应先绑定：最终量词列表已增加两个子系统量子比特数，再绑定各自状态。
- 区分最初 66 条错误和最终 33 条错误：前一轮含 33 个可见引用装配错误，修复后仅保留自审限制，不把所有旧错误都归给规范。

reader 对处理边界提出了上述意见，但未对最终重跑全部产物进行逐项独立复核。最终错误数、字节与引用检查由保存的确定性校验程序实际验证。原文逐条科学忠实性、数学正确性和图像数据仍未独立核验。
