# Author claims extracted from arXiv:2608.14798v1 (AGENT.md 1.1 run)

Reading view of `records.json`. The JSON records are authoritative; this file is a run attachment.
One claim per source assertion unit, per R2: a numbered Proposition/Theorem/Lemma/Corollary/Algorithm
is never merged with another, and unnumbered text contributes the minimal contiguous range that
carries a complete assertion. Every condition and assumption cites its own source span. Nothing here
was proved, checked or reproduced, and nothing here is an independent review.

Claims: 82. Definitions: 16. MathClaims: 84. SemanticContexts: 2. Open frontier items: 64.

| # | Section | Kind | Claim | Modality | Components | Targets | Coverage | Open |
|---|---|---|---|---|---|---|---|---|
| A01 | Sec. I | Prose | Trade-off framing of the existing measure landscape | INTERPRETIVE | 3 | 0 | PARTIAL | 1 |
| A02 | Sec. II B | Prose | Signed Pauli-spectrum convention and the magic-witness reading | ASSERTED | 3 | 1 | PARTIAL | 2 |
| A03 | Sec. II B | Proposition | Stabilizer Pauli Spectrum | ASSERTED | 1 | 1 | COMPLETE | 0 |
| A04 | Sec. II B | Prose | Pauli spectrum is not Clifford invariant | ASSERTED | 2 | 2 | COMPLETE | 1 |
| A05 | Sec. II B | Proposition | Spectrum Splitting into support and Pauli kernel | ASSERTED | 2 | 2 | COMPLETE | 0 |
| A06 | Sec. II C 1 | Proposition | Bounds on the Pauli kernel | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A07 | Sec. II C 2 | Prose | Resource-theoretic properties of the stabilizer Renyi entropies | ASSERTED | 4 | 3 | PARTIAL | 1 |
| A08 | Sec. II C 2 | Prose | Renyi hierarchy and bounds from RoM and T-count | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A09 | Sec. III A | Prose | Pauli gas: stabilizer Hamiltonian and the spectral dictionary | INTERPRETIVE | 5 | 1 | PARTIAL | 2 |
| A10 | Sec. III B | Prose | SPF limits and the support/kernel rewrite | ASSERTED | 3 | 2 | PARTIAL | 2 |
| A11 | Sec. III C | Prose | What the SPF retains and discards | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A12 | Sec. III C 1 | Proposition | Invariance of the SPF under Clifford unitaries | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A13 | Sec. III C 1 | Proposition | Uniqueness of the SPF on pure stabilizer states | ASSERTED | 3 | 2 | PARTIAL | 0 |
| A14 | Sec. III C 2 | Proposition | Clifford-orbit representation of the SPF | ASSERTED | 3 | 2 | PARTIAL | 1 |
| A15 | Sec. III C 2 | Proposition | Stabilizer Renyi representation of the SPF | ASSERTED | 2 | 1 | PARTIAL | 0 |
| A16 | Sec. III C 3 | Theorem | Lipschitz property of the SPF | ASSERTED | 3 | 1 | PARTIAL | 1 |
| A17 | Sec. III C 4 | Theorem | SPF complexity (informal) | ASSERTED | 4 | 2 | PARTIAL | 2 |
| A18 | Sec. III C 5 | Theorem | SPF state-independent bounds | ASSERTED | 3 | 2 | PARTIAL | 1 |
| A19 | Sec. III C 5 | Theorem | SPF state-dependent bounds in terms of M2 | ASSERTED | 3 | 1 | PARTIAL | 1 |
| A20 | Sec. IV A | Proposition | SPF of highly magical states (n copies of |T>) | ASSERTED | 2 | 2 | COMPLETE | 0 |
| A21 | Sec. IV A | Theorem | n-copy SPF for an arbitrary single-qubit state | ASSERTED | 1 | 1 | COMPLETE | 1 |
| A22 | Sec. IV B | Prose | Pauli spectrum factorizes but the SPF is not multiplicative | ASSERTED | 2 | 1 | PARTIAL | 0 |
| A23 | Sec. IV B | Theorem | SPF for separable bipartite states | ASSERTED | 3 | 1 | PARTIAL | 2 |
| A24 | Sec. IV B | Lemma | SPF for Stabilizer + Pure | ASSERTED | 2 | 2 | COMPLETE | 0 |
| A25 | Sec. IV B | Lemma | cSPF supermultiplicativity | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A26 | Sec. IV C | Theorem | Exact ensemble-averaged SPF for Haar-random states | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A27 | Sec. IV C | Lemma | Asymptotic SPF for Haar states | ASSERTED | 2 | 1 | PARTIAL | 0 |
| A28 | Sec. IV C | Lemma | Levy concentration of the SPF for Haar states | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A29 | Sec. IV C | Algorithm | Algorithm 1, Haar state certification via the SPF | ASSERTED | 2 | 0 | PARTIAL | 1 |
| A30 | Sec. IV C | Prose | Why the SPF beats a fixed-index SRE as a Haar witness | ASSERTED | 2 | 0 | PARTIAL | 1 |
| A31 | Sec. IV D | Theorem | Exact ensemble SPF for nu-compressible states | ASSERTED | 1 | 1 | COMPLETE | 0 |
| A32 | Sec. IV D | Prose | Limiting consistency checks and the core-SPF form for nu-compressible states | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A33 | Sec. IV D | Lemma | Concentration for nu-compressible states | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A34 | Sec. IV D | Lemma | Asymptotic SPF for nu-compressible states | ASSERTED | 1 | 1 | COMPLETE | 0 |
| A35 | Sec. IV D | Prose | Asymptotically the SPF sees only the stabilizer-sector size | ASSERTED | 1 | 1 | COMPLETE | 0 |
| A36 | Sec. IV D | Algorithm | Algorithm 2, nu-compressible state verification | ASSERTED | 1 | 0 | PARTIAL | 1 |
| A37 | Sec. IV D | Corollary | SPF of t-doped states | ASSERTED | 1 | 1 | COMPLETE | 0 |
| A38 | Sec. IV D | Prose | Oracle access is a conceptual simplification | ASSERTED | 1 | 0 | PARTIAL | 1 |
| A39 | Sec. IV E | Theorem | Exact ensemble SPF for pseudomagic subset phase states | ASSERTED | 1 | 1 | COMPLETE | 1 |
| A40 | Sec. IV E | Prose | Nullity fails to quantify pseudomagic; SPF curves become exponentially close | ASSERTED | 4 | 2 | PARTIAL | 1 |
| A41 | Sec. V | Prose | Free states and free operations of the resource theory | ASSERTED | 3 | 1 | PARTIAL | 0 |
| A42 | Sec. V | Prose | Why the monotone is built from the core SPF | ASSERTED | 1 | 0 | PARTIAL | 0 |
| A43 | Sec. V | Prose | Stated properties of the stabilizer work | ASSERTED | 4 | 3 | PARTIAL | 2 |
| A44 | Sec. V | Theorem | Sub-additivity of the stabilizer work | ASSERTED | 2 | 2 | COMPLETE | 0 |
| A45 | Sec. V | Corollary | Bounds on the stabilizer work | ASSERTED | 1 | 1 | COMPLETE | 0 |
| A46 | Sec. V | Prose | Limiting regimes and the interpolation claim | ASSERTED | 3 | 0 | PARTIAL | 2 |
| A47 | Sec. V A | Lemma | Continuity of the stabilizer work | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A48 | Sec. V A | Prose | The continuity constant is dimension-free | ASSERTED | 1 | 1 | COMPLETE | 0 |
| A49 | Sec. V A | Prose | Jensen gap between ensemble-averaged SPF and averaged work | ASSERTED | 1 | 1 | COMPLETE | 1 |
| A50 | Sec. V A | Lemma | Small gap between individual and ensemble stabilizer work | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A51 | Sec. V A | Prose | Separating SPS from nu-compressible ensembles by stabilizer work | ASSERTED | 1 | 0 | PARTIAL | 1 |
| A52 | Sec. VI A | Proposition | Stabilizer fidelity upper bounds from the stabilizer work | ASSERTED | 2 | 1 | PARTIAL | 0 |
| A53 | Sec. VI A | Prose | Quoted stabilizer-fidelity lower bound from the SRE | ASSERTED | 2 | 0 | PARTIAL | 1 |
| A54 | Sec. VI A | Prose | Worked example: no stabilizer state is epsilon-close to n copies of |T> | OBSERVED | 2 | 1 | PARTIAL | 1 |
| A55 | Sec. VI B | Theorem | One-shot interconversion bound | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A56 | Sec. VI B | Prose | Incomparability from crossing curves, and the H-to-T counterexample | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A57 | Sec. VI B | Corollary | Magic cost lower bound | ASSERTED | 2 | 1 | PARTIAL | 0 |
| A58 | Sec. VI B | Corollary | Distillation converse | ASSERTED | 1 | 1 | COMPLETE | 0 |
| A59 | Sec. VI C | Prose | H2 method and the explicit basis-dependence caveat | ASSERTED | 2 | 0 | PARTIAL | 2 |
| A60 | Sec. VI C | Prose | H2 STO-3G observations and the role of beta | OBSERVED | 3 | 0 | PARTIAL | 0 |
| A61 | Supplemental Material | Prose | H2 6-31G differences and the reversal of geometry ordering | OBSERVED | 2 | 0 | PARTIAL | 0 |
| A62 | Sec. VI D | Prose | Ising panel (a): small-beta expansion and extensivity in the paramagnetic phase | OBSERVED | 3 | 1 | COMPLETE | 2 |
| A63 | Sec. VI D | Prose | Ising panel (b): stabilizer endpoints and the interior ridge | OBSERVED | 2 | 1 | PARTIAL | 0 |
| A64 | Sec. VII | Prose | Mixed-state SPF by convex roof and its faithfulness | ASSERTED | 2 | 1 | PARTIAL | 0 |
| A65 | Sec. VII | Prose | SPF of a quantum channel via the Choi matrix; monotonicity left open | ASSERTED | 3 | 1 | PARTIAL | 1 |
| A66 | Sec. VII | Prose | Pauli-transfer-matrix characterization of the free operations | ASSERTED | 3 | 1 | PARTIAL | 1 |
| A67 | Sec. VII | Prose | The three stated open problems | ASSERTED | 4 | 0 | PARTIAL | 0 |
| A68 | Abstract | Prose | Framing claims of the abstract | ASSERTED | 5 | 0 | PARTIAL | 1 |
| A69 | AI Acknowledgement Statement | Prose | AI acknowledgement statement | ASSERTED | 2 | 0 | PARTIAL | 0 |
| A70 | Supplemental Material | Lemma | Estimator bounds for the truncated SPF | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A71 | Supplemental Material | Theorem | Estimator concentration for the full normalized SPF | ASSERTED | 1 | 1 | COMPLETE | 1 |
| A72 | Supplemental Material | Proposition | Integer SREs recovered from the SPF by differentiation | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A73 | Supplemental Material | Prose | Pauli spectrum density and its relation to the SPF | ASSERTED | 3 | 1 | PARTIAL | 0 |
| A74 | Supplemental Material | Lemma | Monotone reweighting (Chebyshev-type) lemma | ASSERTED | 1 | 1 | COMPLETE | 1 |
| A75 | Supplemental Material | Proposition | Canonical Splitting of the Pauli spectrum | ASSERTED | 1 | 1 | COMPLETE | 1 |
| A76 | Supplemental Material | Lemma | Variational SPF lower bound | ASSERTED | 1 | 1 | COMPLETE | 1 |
| A77 | Supplemental Material | Theorem | Supporting theorem for sub-additivity | ASSERTED | 1 | 1 | COMPLETE | 1 |
| A78 | Supplemental Material | Theorem | Unfiltered stabilizer work and its missing properties | ASSERTED | 2 | 1 | PARTIAL | 1 |
| A79 | Supplemental Material | Lemma | Connected correlations of a uniformly random function | ASSERTED | 1 | 1 | COMPLETE | 1 |
| A80 | Supplemental Material | Corollary | Average SPS Pauli-spectrum density and its non-smoothness | ASSERTED | 3 | 2 | PARTIAL | 2 |
| A81 | Supplemental Material | Prose | Ensemble-averaged stabilizer Hamiltonian is not interchangeable with the averaged SPF | ASSERTED | 2 | 2 | COMPLETE | 0 |
| A82 | Supplemental Material | Prose | Large-beta limit of the SPF derived in the supplement | ASSERTED | 1 | 1 | COMPLETE | 1 |

---

## A01 — Trade-off framing of the existing measure landscape

*Sec. I · Prose · modality INTERPRETIVE · coverage PARTIAL*

**What the authors said.** The existing landscape of magic measures is claimed to exhibit a persistent trade-off: operationally meaningful measures (robustness of magic, stabilizer extent/rank, relative entropies of magic) bound simulation cost and distillation rates but require optimizations whose size grows superexponentially, while the computable measures (the $\alpha$-stabilizer Renyi entropies) carry structural caveats, and the stabilizer nullity is too coarse to separate states with trivial stabilizer group.

**System.** the literature on magic measures, as surveyed by the authors

**Comparison baseline.** robustness of magic, stabilizer extent and rank, relative entropies of magic, $T$-count, stabilizer Renyi entropies, stabilizer nullity

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) Monotonicity of $M_\alpha$ under stabilizer protocols is stated as established only for $\alpha\geq2$ and unresolved for $\alpha<2$
- `condition:source-2` (SOURCE_EXPLICIT) The estimation-cost caveat is stated for highly magical states, where stabilizer purities become exponentially small

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / RESIDUAL] Quantities that can be computed and measured either carry structural caveats as monotones or are too coarse to resolve the states of interest, while quantities with strong resource-theoretic credentials cannot be evaluated at scale.
  - residual: A survey-level judgement about the literature. It is not a mathematical statement and is not checkable from this paper alone; no measure-by-measure criterion for 'structural caveat' or 'too coarse' is given.
- `component:evidence-1` [EVIDENCE / RESIDUAL] Nullity cannot distinguish any two states with trivial stabilizer group, which is to say almost all states.
- `component:evidence-2` [EVIDENCE / RESIDUAL] For highly magical states the underlying stabilizer purities become exponentially small, so estimating an SRE to fixed additive accuracy becomes resource-intensive exactly where the SRE is largest.
  - residual: No quantitative statement of the cost scaling is given here; the later complexity theorem is about the SPF, not about SRE estimation cost.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · source_fidelity] The framing claim is a literature judgement with no stated criterion, so it cannot be confirmed or contradicted from this source.
  - what could settle it: Comparison against the cited measures' own complexity statements; no cited work was retrieved in this run.

Records: `claim:opus5-v11-2608.14798v1/A01`, `map:opus5-v11-2608.14798v1/A01`.

## A02 — Signed Pauli-spectrum convention and the magic-witness reading

*Sec. II B · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The paper defines the Pauli spectrum with the sign retained, ${\rm Spec}(\psi)=\{\tr(\psi P)\}_{P\in\mathcal{P}_n}$, rather than the absolute-value convention of Beverland et al., states that the signed convention is the natural one for the later typical-set analysis and that the absolute-value version can be recovered whenever needed, and reads the single-qubit examples as showing that the Pauli spectrum separates stabilizer structure from genuinely nonstabilizer behaviour.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the $|\tr(\psi P)|$ convention of Beverland et al. (2020)

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\ket{\psi}$ is an $n$-qubit pure state
- `condition:source-2` (SOURCE_EXPLICIT) Pauli operators are taken modulo phase and global phases are irrelevant, so $\mathcal{P}_n$ is the phase-free set of $4^n$ strings

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] The Pauli spectrum separates stabilizer structure from genuinely nonstabilizer behaviour and is therefore a magic witness: pure single-qubit stabilizer states have spectrum $\{1,\pm1,0,0\}$ while ${\rm Spec}(\ket{T})=\{1,\tfrac{1}{\sqrt2},\tfrac{1}{\sqrt2},0\}$ already has non-integer entries.
  - target (EXACT): For a single qubit, $\psi=(I+r_xX+r_yY+r_zZ)/2$ with $r_x^2+r_y^2+r_z^2=1$ gives ${\rm Spec}(\psi)=\{1,r_x,r_y,r_z\}$; the six pure stabilizer states are exactly those with $\{r_x,r_y,r_z\}$ a permutation of $\{\pm1,0,0\}$, and ${\rm Spec}(\ket{T})=\{1,\tfrac{1}{\sqrt2},\tfrac{1}{\sqrt2},0\}$.
    - quantifier: FORALL \psi in pure states of one qubit
    - assumption (SOURCE_EXPLICIT): $\psi$ is a one-qubit pure state written in the Bloch parameterization with $r_x^2+r_y^2+r_z^2=1$
    - normalization: only the $n=1$ case is displayed; the general-$n$ separation is asserted, not derived here
- `component:definition-1` [DEFINITION / RESIDUAL] ${\rm Spec}(\psi):=\{\tr(\psi P)\ \text{for}\ P\in\mathcal{P}_n\}$, a collection of $4^n$ real numbers.
  - residual: Registered as a Definition record rather than mathematized here; the displayed definition uses set braces while a footnote states the object should be read as a multiset because multiplicities matter, and the source does not say which reading the later theorems use.
- `component:assumption-1` [ASSUMPTION / UNRESOLVED] The absolute-value convention 'can be recovered whenever needed'.
  - residual: No recovery map is given and no statement identifies which later results are convention-dependent.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · source_fidelity] Set-versus-multiset reading of ${\rm Spec}(\psi)$ is fixed only in a footnote while the displayed definition uses set notation.
  - what could settle it: Author clarification, or a check of which downstream sums (SPF, SRE) require multiplicity.
- [MISSING_SOURCE · mathematical_correctness] The claim that the absolute-value convention is recoverable is stated without a construction.
  - what could settle it: An explicit map between the two conventions, or a list of the results that depend on the choice.

Records: `claim:opus5-v11-2608.14798v1/A02`, `map:opus5-v11-2608.14798v1/A02`.

## A03 — Stabilizer Pauli Spectrum

*Sec. II B · Proposition · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Proposition (Stabilizer Pauli Spectrum): the Pauli spectrum of a pure $n$-qubit stabilizer state of a system $A$ consists of $d_A^2-d_A$ zeros and $d_A$ plus/minus ones.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** general pure states, whose spectra contain non-integer entries

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\ket{\psi}$ is a pure $n$-qubit stabilizer state, i.e. the unique common $+1$ eigenstate of an abelian subgroup of $\widetilde{\mathcal{P}}_n$ generated by $n$ independent commuting Paulis
- `condition:source-2` (SOURCE_RECONSTRUCTED) $d_A=|A|=2^n$ is the Hilbert-space dimension of system $A$, per the Sec. II convention

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Exactly $d_A$ spectrum entries equal $\pm1$ and the remaining $d_A^2-d_A$ entries vanish.
  - target (EXACT): $|\{x\in{\rm Spec}(\psi):|x|=1\}|=d_A$ and $|\{x\in{\rm Spec}(\psi):x=0\}|=d_A^2-d_A$, counted with multiplicity.
    - quantifier: FORALL n in positive integers
    - quantifier: FORALL \psi in pure stabilizer states of an $n$-qubit system $A$ with $d_A=2^n$
    - assumption (SOURCE_EXPLICIT): $\ket{\psi}\in{\rm Stab}_n$
    - assumption (SOURCE_RECONSTRUCTED): $|{\rm Spec}(\psi)|=d_A^2$ counted with multiplicity
    - normalization: counts are over phase-free Pauli labels, so the identity contributes the entry $1$

Records: `claim:opus5-v11-2608.14798v1/A03`, `map:opus5-v11-2608.14798v1/A03`.

## A04 — Pauli spectrum is not Clifford invariant

*Sec. II B · Prose · modality ASSERTED · coverage COMPLETE*

**What the authors said.** The Pauli spectrum is not invariant under general Clifford unitaries: Clifford conjugation may permute spectrum entries and flip their signs. For stabilizer states the action permutes and sign-changes the $\pm1$ entries while leaving the stabilizer-group size unchanged, so $|{\rm Stab}(\psi)|$ can be read off by counting entries with $|x|=1$.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** Clifford-invariant functionals such as $M_\alpha$ and, later, the SPF

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\mathcal{C}\in\mathcal{C}l_n=\{U\in U(2^n):U\mathcal{P}_nU^\dagger=\mathcal{P}_n\}$ acts by conjugation

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] ${\rm Spec}(\psi)\neq{\rm Spec}(\mathcal{C}(\psi))$ in general, the action being permutation and/or sign flip of spectrum entries.
  - target (EXACT): For every $\mathcal{C}\in\mathcal{C}l_n$ there exist a permutation $\pi$ of $\mathcal{P}_n$ and signs $s_P\in\{\pm1\}$ with $\tr(\mathcal{C}(\psi)P)=s_P\,\tr(\psi\,\pi(P))$ for all $P$.
    - quantifier: FORALL \mathcal{C} in $\mathcal{C}l_n$
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - quantifier: FORALL P in $\mathcal{P}_n$
    - assumption (SOURCE_EXPLICIT): Clifford unitaries map Pauli operators to Pauli operators under conjugation, up to phase
    - normalization: phase-free labels; the sign is carried by the spectrum entry, not by the label
- `component:conclusion-2` [CONCLUSION / MAPPED] The stabilizer-group size is Clifford invariant and readable from the spectrum by counting entries with $|x|=1$.
  - target (EXACT): $|\{x\in{\rm Spec}(\mathcal{C}(\psi)):|x|=1\}|=|\{x\in{\rm Spec}(\psi):|x|=1\}|=|{\rm Stab}(\psi)|$ for every $\mathcal{C}\in\mathcal{C}l_n$.
    - quantifier: FORALL \mathcal{C} in $\mathcal{C}l_n$
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_EXPLICIT): Clifford conjugation acts on the spectrum by permutation and sign flip
    - assumption (SOURCE_EXPLICIT): the number of $\pm1$ entries equals the size of the stabilizer group
    - normalization: counts with multiplicity over phase-free labels

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · source_fidelity] The text reads 'not invariant ... for any arbitrary Clifford unitary', which is ambiguous between 'for some' and 'for every'; the surrounding argument only needs 'not invariant in general'.
  - what could settle it: Author clarification; no downstream result appears to depend on the stronger reading.

Records: `claim:opus5-v11-2608.14798v1/A04`, `map:opus5-v11-2608.14798v1/A04`.

## A05 — Spectrum Splitting into support and Pauli kernel

*Sec. II B · Proposition · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Proposition (Spectrum Splitting): every pure state $\psi$ induces the disjoint decomposition $\mathcal{P}_n={\rm Supp}(\psi)\cup{\rm Ker}_\mathcal{P}(\psi)$, where ${\rm Supp}(\psi)=\{P:\tr[\psi P]\neq0\}$ and ${\rm Ker}_\mathcal{P}(\psi)=\{P:\tr[\psi P]=0\}$; moreover ${\rm Stab}(\psi)\subseteq{\rm Supp}(\psi)$ with equality for stabilizer states, and $|{\rm Supp}(\psi)|+|{\rm Ker}_\mathcal{P}(\psi)|=d_n^2$.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the undecomposed set of Pauli labels

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\psi$ is pure
- `condition:source-2` (SOURCE_EXPLICIT) the two sets are defined at the level of Pauli labels, not spectrum values

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{P}_n$ decomposes disjointly into support and Pauli kernel, with cardinalities summing to $d_n^2$.
  - target (EXACT): ${\rm Supp}(\psi)\cap{\rm Ker}_{\mathcal{P}}(\psi)=\emptyset$, ${\rm Supp}(\psi)\cup{\rm Ker}_{\mathcal{P}}(\psi)=\mathcal{P}_n$, and $|{\rm Supp}(\psi)|+|{\rm Ker}_{\mathcal{P}}(\psi)|=d_n^2$.
    - quantifier: FORALL n in positive integers
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_EXPLICIT): ${\rm Supp}(\psi)=\{P\in\mathcal{P}_n:\tr[\psi P]\neq0\}$ and ${\rm Ker}_{\mathcal{P}}(\psi)=\{P\in\mathcal{P}_n:\tr[\psi P]=0\}$
    - assumption (SOURCE_RECONSTRUCTED): $|\mathcal{P}_n|=d_n^2=4^n$
    - normalization: the source calls the cardinality identity the 'nullity-rank theorem'; it is set complement in $\mathcal{P}_n$
- `component:conclusion-2` [CONCLUSION / MAPPED] ${\rm Stab}(\psi)\subseteq{\rm Supp}(\psi)$, with equality exactly for stabilizer states.
  - target (EXACT): ${\rm Stab}(\psi)\subseteq{\rm Supp}(\psi)$ for every pure $\psi$, and ${\rm Stab}(\psi)={\rm Supp}(\psi)$ when $\psi\in{\rm Stab}_n$.
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_RECONSTRUCTED): stabilizing Paulis have $\tr[\psi P]=1$, hence lie in the support
    - assumption (SOURCE_EXPLICIT): equality for stabilizer states follows from Proposition (Stabilizer Pauli Spectrum)
    - normalization: ${\rm Stab}(\psi)$ is a group of signed Paulis while ${\rm Supp}(\psi)$ is a set of phase-free labels; the inclusion is read modulo phase
  - residual: The inclusion compares a signed subgroup with a phase-free label set; the source does not state how the phase quotient is taken.

Records: `claim:opus5-v11-2608.14798v1/A05`, `map:opus5-v11-2608.14798v1/A05`.

## A06 — Bounds on the Pauli kernel

*Sec. II C 1 · Proposition · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Proposition: the Pauli kernel of a state $\psi$ obeys $2^n(2^n-2^{\nu(\psi)})\leq|{\rm Ker}_\mathcal{P}(\psi)|\leq4^n-2^n$.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the nullity-dependent upper bound $4^n-2^{n-\nu(\psi)}$ derived first inside the same proof

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\psi$ has stabilizer nullity $\nu(\psi)$
- `condition:source-2` (SOURCE_EXPLICIT) the lower bound uses $|{\rm Supp}(\psi)|=d\,2^{M_0(\psi)}\leq d\,2^{\nu(\psi)}$, attributed to Leone et al. (2024), with $M_0$ the zero-order stabilizer Renyi entropy

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $2^n(2^n-2^{\nu(\psi)})\leq|{\rm Ker}_\mathcal{P}(\psi)|\leq4^n-2^n$.
  - target (EXACT): $2^{n}(2^{n}-2^{\nu(\psi)})\ \leq\ |{\rm Ker}_{\mathcal{P}}(\psi)|\ \leq\ 4^{n}-2^{n}$.
    - quantifier: FORALL n in positive integers
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$ with stabilizer nullity $\nu(\psi)$
    - assumption (SOURCE_EXPLICIT): $|{\rm Supp}(\psi)|=d_n2^{M_0(\psi)}$ and $M_0(\psi)\leq\nu(\psi)$, imported from Leone et al. (2024)
    - assumption (SOURCE_EXPLICIT): $|{\rm Supp}(\psi)|+|{\rm Ker}_{\mathcal{P}}(\psi)|=d_n^2$
    - normalization: $d_n=2^n$; the source writes $d$ without subscript inside the proof
- `component:evidence-1` [EVIDENCE / UNRESOLVED] The proof first derives the nullity-dependent upper bound $4^n-2^{n-\nu(\psi)}$ and then replaces it by the state-independent $4^n-2^n$, described as 'a tighter more fundamental bound' achieved in the stabilizer case.
  - residual: For $\nu>0$ the displayed replacement $4^n-2^n$ is weaker, not tighter, than $4^n-2^{n-\nu}$; the two coincide only at $\nu=0$. The intended comparison is not stated, so no representation of 'tighter' is proposed.

**Open items recorded against this claim.**

- [CONFLICT · source_fidelity, mathematical_correctness] The proof calls $|{\rm Ker}|\leq4^n-2^n$ tighter than $4^n-2^{n-\nu(\psi)}$, but for $\nu>0$ the latter is the stronger statement.
  - what could settle it: Author clarification or errata; no later result in the paper appears to depend on the distinction.

Records: `claim:opus5-v11-2608.14798v1/A06`, `map:opus5-v11-2608.14798v1/A06`.

## A07 — Resource-theoretic properties of the stabilizer Renyi entropies

*Sec. II C 2 · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The stabilizer Renyi entropy is stated to be (i) faithful, $M_\alpha(\psi)=0$ iff $\psi$ is a stabilizer state; (ii) stable under Cliffords, $M_\alpha(C\ket{\psi})=M_\alpha(\psi)$ for all $C\in\mathcal{C}l_n$; and (iii) additive, $M_\alpha(\psi\otimes\phi)=M_\alpha(\psi)+M_\alpha(\phi)$; and for $\alpha\geq2$ it is a magic monotone, attributed to Leone et al. (2024).

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the stabilizer nullity, and the measures listed in the introduction

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) Monotonicity is claimed only for $\alpha\geq2$
- `condition:source-2` (SOURCE_EXPLICIT) Faithfulness, Clifford stability and additivity are stated without an explicit range of $\alpha$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $M_\alpha(\psi)=0$ if and only if $\psi$ is a stabilizer state.
  - target (EXACT): $M_{\alpha}(\psi)=0\iff\psi\in{\rm Stab}_n$.
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - quantifier: FORALL \alpha in the range is not stated in the source at this point
    - assumption (SOURCE_EXPLICIT): $M_\alpha$ is the Renyi entropy of the distribution $\Xi(\psi)=\{x^2/d_n:x\in{\rm Spec}(\psi)\}$
    - normalization: $\log$ base 2; $M_\alpha\geq0$ with equality exactly on the free set
- `component:conclusion-2` [CONCLUSION / MAPPED] $M_\alpha(C\ket{\psi})=M_\alpha(\psi)$ for all $C\in\mathcal{C}l_n$.
  - target (EXACT): $M_{\alpha}(C\ket{\psi})=M_{\alpha}(\psi)$ for every $C\in\mathcal{C}l_n$.
    - quantifier: FORALL C in $\mathcal{C}l_n$
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_RECONSTRUCTED): Clifford conjugation permutes and sign-flips the Pauli spectrum, leaving even moments fixed
    - normalization: $\log$ base 2
- `component:conclusion-3` [CONCLUSION / MAPPED] $M_\alpha(\psi\otimes\phi)=M_\alpha(\psi)+M_\alpha(\phi)$.
  - target (EXACT): $M_{\alpha}(\psi\otimes\phi)=M_{\alpha}(\psi)+M_{\alpha}(\phi)$.
    - quantifier: FORALL \psi,\phi in pure states of the respective factors
    - assumption (SOURCE_RECONSTRUCTED): the Pauli spectrum is multiplicative under tensor products
    - normalization: $\log$ base 2, which makes the Renyi entropy additive rather than multiplicative
- `component:attribution-1` [ATTRIBUTION / NON_CLAIM] For $\alpha\geq2$ the stabilizer Renyi entropy is a magic monotone, attributed to Leone, Oliviero and Hamma (2024).
  - residual: Attribution to cited work. No cited paper was retrieved in this run, so the monotonicity statement is recorded as the citing text reports it.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · source_fidelity] Faithfulness, Clifford stability and additivity are asserted without an admissible range of $\alpha$, while monotonicity is explicitly restricted to $\alpha\geq2$.
  - what could settle it: Author clarification or a check against the cited source; not attempted in this run.

Records: `claim:opus5-v11-2608.14798v1/A07`, `map:opus5-v11-2608.14798v1/A07`.

## A08 — Renyi hierarchy and bounds from RoM and T-count

*Sec. II C 2 · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** While the $\nu$-nullity captures the size of the stabilizer group, the stabilizer Renyi entropies capture finitely many moments and obey the Renyi hierarchy $M_\alpha(\psi)\leq M_0=\log(|{\rm Supp}(\psi)|/d_n)\leq\nu(\psi)$; in a similar fashion ${\rm RoM}$ and $T$-count provide upper bounds, $M_\alpha(\psi)\leq2\log({\rm RoM}(\psi))$ and $M_\alpha(\psi)\leq T(\psi)$ for all $\alpha\geq\frac12$.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the stabilizer nullity $\nu$, the robustness of magic, and the $T$-count

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) The RoM and $T$-count bounds are stated for $\alpha\geq\frac12$
- `condition:source-2` (SOURCE_EXPLICIT) The hierarchy $M_\alpha\leq M_0\leq\nu$ is stated without an admissible range of $\alpha$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $M_\alpha(\psi)\leq M_0=\log(|{\rm Supp}(\psi)|/d_n)\leq\nu(\psi)$.
  - target (EXACT): $M_{\alpha}(\psi)\leq M_{0}(\psi)=\log(|{\rm Supp}(\psi)|/d_n)\leq\nu(\psi)$.
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - quantifier: FORALL \alpha in the source states no range here; the inequality is used later for $\alpha\geq2$
    - assumption (SOURCE_RECONSTRUCTED): $M_\alpha$ is non-increasing in $\alpha$
    - assumption (SOURCE_EXPLICIT): $M_0(\psi)=\log(|{\rm Supp}(\psi)|/d_n)$
    - normalization: $M_0$ is defined here through the support cardinality rather than as the $\alpha\to0$ limit of the Renyi family
- `component:attribution-1` [ATTRIBUTION / NON_CLAIM] $M_\alpha(\psi)\leq2\log({\rm RoM}(\psi))$ and $M_\alpha(\psi)\leq T(\psi)$ for all $\alpha\geq\frac12$.
  - residual: Asserted without proof or citation at this point in the text; recorded as printed. The RoM and $T$-count are not defined in this paper.

**Open items recorded against this claim.**

- [MISSING_SOURCE · mathematical_correctness] The RoM and $T$-count upper bounds on $M_\alpha$ are stated with an explicit index range but no proof or citation.
  - what could settle it: Citation check against the RoM and $T$-count literature; not attempted in this run.

Records: `claim:opus5-v11-2608.14798v1/A08`, `map:opus5-v11-2608.14798v1/A08`.

## A09 — Pauli gas: stabilizer Hamiltonian and the spectral dictionary

*Sec. III A · Prose · modality INTERPRETIVE · coverage PARTIAL*

**What the authors said.** The Pauli spectrum is reinterpreted as the energy spectrum of a fictitious many-body system. Perspective 1 builds the stabilizer Hamiltonian $\hat H(\psi)=\sum_{P,s}(1-s\tr(\psi P))|P,s)(P,s|$ on $\mathcal{H}_n^{\otimes2}\otimes\mathbb{C}^2$, with a $|{\rm Stab}(\psi)|$-fold degenerate ground level and parity block structure. Perspective 2 treats each Pauli as a two-level particle with energies $E_{P,\pm}=1\pm\tr[\psi_AP]$, giving $d^2$ particles in the band $[0,2]$, the Pauli gas. The asserted dictionary is that stabilizer states give a maximally rigid gas with only the integer levels $\{0,1,2\}$ and ground degeneracy $d$, while magic populates fractional interior levels and depletes the degenerate ground level.

**System.** a fictitious ensemble of $d^2=4^n$ two-level particles derived from an $n$-qubit pure state

**Comparison baseline.** a pure stabilizer state, for which only the integer levels $\{0,1,2\}$ occur

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\psi_A$ is pure
- `condition:source-2` (SOURCE_EXPLICIT) Perspective 1 is set up from the POVM $\{\Pi_P^\pm=\tfrac{1}{2d_n^2}(\mathbb{I}\pm P)\}$ and its empirical distribution in the large-shot limit
- `condition:source-3` (SOURCE_EXPLICIT) the states $|P,s)=(\mathbb{I}\otimes P)\ket{\Phi^+}\otimes\ket{s}$ are orthonormal and form a basis of $\mathcal{H}_n^{\otimes2}\otimes\mathbb{C}^2$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / RESIDUAL] Nonstabilizerness, a microscopic property of $\psi_A$, is recast as a macroscopic spectral property of a thermodynamically large ensemble: stabilizer states give a rigid integer spectrum, magic populates fractional levels and depletes the ground degeneracy.
  - residual: An interpretive dictionary, not an equation. 'Thermodynamically large' refers to $d^2=4^n$ formal particles, and the source states explicitly that $\beta$ has no connection to a physical temperature. No representation is proposed because the claim is the reading itself.
- `component:model-1` [MODEL / MAPPED] $\hat H(\psi)=\sum_{P\in\mathcal{P}_n,s\in\{-1,1\}}(1-s\,\tr(\psi P))|P,s)(P,s|$ has a $|{\rm Stab}(\psi)|$-fold degenerate ground level; it commutes with $\mathbb{I}^{\otimes2}\otimes\hat n$ and is block diagonal, $\hat H=\hat H_+\oplus\hat H_-$.
  - target (EXACT): $\hat H(\psi)|P,s)=(1-s\,\tr(\psi P))|P,s)$, so the eigenvalue $0$ occurs exactly for pairs $(P,s)$ with $s\,\tr(\psi P)=1$, whose number is $|{\rm Stab}(\psi)|$.
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - quantifier: FORALL (P,s) in $\mathcal{P}_n\times\{-1,1\}$
    - assumption (SOURCE_EXPLICIT): $\{|P,s)\}$ is an orthonormal basis of $\mathcal{H}_n^{\otimes2}\otimes\mathbb{C}^2$
    - assumption (SOURCE_RECONSTRUCTED): $\tr(\psi P)=\pm1$ exactly for $P\in{\rm Stab}(\psi)$ up to sign
    - normalization: the ground level is at energy $0$, not at $-|{\rm Stab}|$; details of the block decomposition are deferred to the supplement
  - residual: The parity operator $\hat n$ and the block decomposition are stated with details deferred; the claimed degeneracy count is reproduced here from the eigenvalue structure, not from a proof in the text.
- `component:model-2` [MODEL / UNRESOLVED] Each Pauli labels a two-level particle with energies $E_{P,\pm}=1\pm\tr[\psi_AP]$, giving $d^2$ particles confined to the band $[0,2]$; the identity Pauli contributes the pinned edges $E=0$ and $E=2$ and plays the role of a vacuum mode.
  - residual: A bookkeeping device over $\mathcal{P}_n$, not an operator statement. The two perspectives are introduced as complementary but are never shown to be equivalent, and the SPF definition uses Perspective 1's $\hat H(\psi)$ while all later reasoning uses Perspective 2's band picture.
- `component:model-3` [MODEL / UNRESOLVED] For every non-identity Pauli, $E_{P,\pm}$ is claimed to be 'the energy of a genuine physical state $(I\pm P)/d$'.
  - residual: No Hamiltonian is exhibited for which $(I\pm P)/d$ is an eigenstate with that eigenvalue, and $(I\pm P)/d$ is a mixed operator rather than a state of the doubled space. Read as interpretive; not represented.
- `component:model-4` [MODEL / RESIDUAL] The zero-temperature limit is presented as a third-law statement: the free-energy difference reduces to a difference of residual entropies, which equals the stabilizer nullity.
  - residual: Anticipates the stabilizer-work limit claimed in Sec. V. Nothing is derived here, and the $\beta\to\infty$ limit is stated inconsistently between Sec. III B and Sec. VI C.

**Open items recorded against this claim.**

- [MODEL_ASSUMPTION · semantic_applicability] Two Pauli-gas constructions are introduced as complementary but never reconciled; the SPF is defined from Perspective 1 while every later argument uses Perspective 2.
  - what could settle it: An explicit isomorphism between the two constructions, or a statement that only one is load-bearing.
- [MODEL_ASSUMPTION · semantic_applicability] The claim that $E_{P,\pm}$ is the energy of a 'genuine physical state' is not supported by any equation in the text.
  - what could settle it: Identification of the operator whose spectrum this is, or restatement as an analogy.

Records: `claim:opus5-v11-2608.14798v1/A09`, `map:opus5-v11-2608.14798v1/A09`.

## A10 — SPF limits and the support/kernel rewrite

*Sec. III B · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** For small $\beta$ all micro-states contribute and $\lim_{\beta\to0}\mathcal{Z}_\beta(\psi)=d_n^2$; for large $\beta$ only low-energy states contribute and $\lim_{\beta\to\infty}\mathcal{Z}_\beta(\psi)=|{\rm STAB}(\psi)|/2$. For every $\beta$ the SPF can be rewritten as $\mathcal{Z}_\beta(\psi)=e^{-\beta}(|{\rm Ker}(\psi)|+\sum_{P\in{\rm Supp}(\psi)}\cosh(\tr{P\psi}))$.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** $\mathcal{Z}_0=d_n^2$, the state-independent high-temperature value

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\beta\in\mathbb{R}$ in the definition; the two limits are $\beta\to0$ and $\beta\to\infty$
- `condition:source-2` (SOURCE_EXPLICIT) the rewrite is asserted 'for every $\beta$'

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\lim_{\beta\to0}\mathcal{Z}_\beta(\psi)=d_n^2$ for every state, so the SPF is insensitive to magic at $\beta=0$.
  - target (EXACT): $\lim_{\beta\to0}\mathcal{Z}_{\beta}(\psi)=d_n^{2}$ for every pure $\psi$.
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_EXPLICIT): $\mathcal{Z}_\beta(\psi)=e^{-\beta}\sum_{x\in{\rm Spec}(\psi)}\cosh(\beta x)$ with $|{\rm Spec}(\psi)|=d_n^2$ counted with multiplicity
    - normalization: the SPF is not normalized to $1$; $\mathcal{Z}_0=d_n^2$ is the Pauli count
- `component:conclusion-2` [CONCLUSION / MAPPED] $\lim_{\beta\to\infty}\mathcal{Z}_\beta(\psi)=|{\rm STAB}(\psi)|/2$.
  - target (ASYMPTOTIC): $\lim_{\beta\to\infty}\mathcal{Z}_{\beta}(\psi)=\tfrac12|{\rm STAB}(\psi)|$.
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_EXPLICIT): $\mathcal{Z}_\beta=\frac12\tr e^{-\beta\hat H(\psi)}$ and the ground level of $\hat H(\psi)$ sits at energy $0$ with degeneracy $|{\rm STAB}(\psi)|$
    - assumption (SOURCE_EXPLICIT): the supplemental derivation via the parity decomposition $\hat H=\hat H_+\oplus\hat H_-$
    - normalization: the factor $\tfrac12$ comes from the $\tfrac12$ in the SPF definition; the supplemental derivation confirms this value, so the Sec. VI C statement of the same limit without the factor $\tfrac12$ is the inconsistent one
    - approximation error: the supplement writes the subleading behaviour only as $(1+\dots)$, with no rate; the limit value itself is exact
  - residual: Sec. VI C states the same limit as $|{\rm Stab}(\psi)|$, without the factor $\tfrac12$. The supplemental derivation settles the intended value, so the main-text application section carries the slip.
- `component:conclusion-3` [CONCLUSION / UNRESOLVED] $\mathcal{Z}_\beta(\psi)=e^{-\beta}(|{\rm Ker}(\psi)|+\sum_{P\in{\rm Supp}(\psi)}\cosh(\tr{P\psi}))$ for every $\beta$.
  - residual: Not represented: as printed the hyperbolic cosine has no factor $\beta$ in its argument, which is inconsistent with the definition and with the accompanying $e^{-\beta}|{\rm Ker}|$ term. Read as a typographical omission; not repaired here, since R4 forbids adding the missing factor on the paper's behalf.

**Open items recorded against this claim.**

- [CONFLICT · source_fidelity] The low-temperature limit of the SPF is printed as $|{\rm STAB}(\psi)|/2$ in Sec. III B and as $|{\rm Stab}(\psi)|$ in Sec. VI C; the supplemental derivation gives $\tfrac12|{\rm STAB}(\psi)|$, so the Sec. VI C sentence is the one that disagrees with the rest of the paper.
  - what could settle it: Errata on the Sec. VI C sentence; no author clarification is needed because the supplement already derives the value.
- [SCOPE_AMBIGUITY · source_fidelity] The support/kernel rewrite of the SPF omits $\beta$ inside $\cosh$.
  - what could settle it: Errata or author confirmation that the intended expression is $\cosh(\beta\tr[P\psi])$.

Records: `claim:opus5-v11-2608.14798v1/A10`, `map:opus5-v11-2608.14798v1/A10`.

## A11 — What the SPF retains and discards

*Sec. III C · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The SPF does not retain the full labelled Pauli spectrum; instead it packages the spectrum's Clifford-invariant even statistics into an analytic object, thereby capturing the stabilizer-relevant information accessible through Pauli moments.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the full signed, labelled Pauli spectrum ${\rm Spec}(\psi)$

**Stated conditions.**

- `condition:source-1` (SOURCE_RECONSTRUCTED) only even moments enter, because $\cosh$ is even in $\beta x$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] The map from state to SPF is lossy: Pauli labels, individual signs and all odd statistics are not recoverable from $\mathcal{Z}_\bullet(\psi)$.
  - target (EXACT): $\mathcal{Z}_{\beta}(\psi)$ depends on ${\rm Spec}(\psi)$ only through the even moments $\{\sum_{x\in{\rm Spec}(\psi)}x^{2k}\}_{k\geq0}$; consequently any two pure states whose Pauli spectra agree as multisets up to independent sign flips and relabelling have identical SPF at every $\beta$.
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL \psi,\phi in pure states in $\mathcal{H}_n$ whose spectra agree up to sign and relabelling
    - assumption (SOURCE_RECONSTRUCTED): $\cosh$ is even, so $\mathcal{Z}_\beta$ is invariant under independent sign flips of spectrum entries
    - assumption (SOURCE_RECONSTRUCTED): the sum over ${\rm Spec}(\psi)$ is symmetric, hence invariant under relabelling
    - normalization: the converse direction, that the even moments determine $\mathcal{Z}_\beta$, is immediate from the definition; whether they determine the magic-relevant content is not addressed
- `component:assumption-1` [ASSUMPTION / UNRESOLVED] What survives is asserted to be 'the stabilizer-relevant information accessible through Pauli moments'.
  - residual: 'Stabilizer-relevant information' is not defined, and no statement identifies what magic-relevant content is lost with the discarded labels and signs. This bears directly on the abstract's claim that the temperature profile carries 'strictly more information' than its endpoints.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · semantic_applicability] The SPF is stated to be a lossy compression of the Pauli spectrum, but the equivalence classes it induces are not characterized.
  - what could settle it: An explicit statement of which pairs of states the SPF profile identifies.

Records: `claim:opus5-v11-2608.14798v1/A11`, `map:opus5-v11-2608.14798v1/A11`.

## A12 — Invariance of the SPF under Clifford unitaries

*Sec. III C 1 · Proposition · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Proposition (Invariance): the SPF is invariant under Clifford unitaries, $\mathcal{Z}_\beta(\mathcal{C}(\psi))=\mathcal{Z}_\beta(\psi)$ for all $\mathcal{C}\in\mathcal{C}l_n$.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the non-invariant Pauli spectrum itself

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\mathcal{C}\in\mathcal{C}l_n$
- `condition:source-2` (SOURCE_EXPLICIT) the proof uses that $\cosh$ absorbs the $1\to-1$ sign change and that the decreasingly ordered spectra agree, ${\rm Spec}(\psi)^{\downarrow}={\rm Spec}(\mathcal{C}(\psi))^{\downarrow}$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{Z}_\beta(\mathcal{C}(\psi))=\mathcal{Z}_\beta(\psi)$ for all Clifford $\mathcal{C}$ and all $\beta$.
  - target (EXACT): $\mathcal{Z}_{\beta}(\mathcal{C}(\psi))=\mathcal{Z}_{\beta}(\psi)$ for every $\mathcal{C}\in\mathcal{C}l_n$, every pure $\psi$ and every $\beta\in\mathbb{R}$.
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL \mathcal{C} in $\mathcal{C}l_n$
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_EXPLICIT): Clifford conjugation permutes the Pauli spectrum and may flip signs
    - assumption (SOURCE_EXPLICIT): $\cosh$ is an even function
    - normalization: invariance holds pointwise in $\beta$, so the whole profile is a Clifford invariant
- `component:evidence-1` [EVIDENCE / RESIDUAL] The proof asserts that Clifford unitaries act transitively on Paulis.
  - residual: Transitivity fails for the identity Pauli, which is fixed by every Clifford; the argument needs only transitivity on non-identity Paulis, and the conclusion is unaffected.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · source_fidelity] The invariance proof states that Cliffords 'act transitively on Paulis', which is false for the identity Pauli.
  - what could settle it: Errata; restricting the statement to non-identity Paulis leaves the proof intact.

Records: `claim:opus5-v11-2608.14798v1/A12`, `map:opus5-v11-2608.14798v1/A12`.

## A13 — Uniqueness of the SPF on pure stabilizer states

*Sec. III C 1 · Proposition · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Proposition (Uniqueness for stabilizer states): for any $\ket{\phi}\in{\rm Stab}_n$ the SPF is uniquely $\mathcal{Z}_\beta(\phi)=\mathcal{Z}_\beta({\rm STAB}_n)=e^{-\beta}[(d_n^2-d_n)+d_n\cosh\beta]$; the paper reads this as the SPF being faithful under free operations, and states that both properties extend to the core SPF with $\mathcal{Z}^c_\beta({\rm STAB}_n)=e^{-\beta}d_n(\cosh\beta-1)$.

**System.** $n$-qubit pure stabilizer states ${\rm Stab}_n$

**Comparison baseline.** general pure states, for which the SPF is strictly smaller at every nonzero beta

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\ket{\phi}$ is a pure $n$-qubit stabilizer state
- `condition:source-2` (SOURCE_EXPLICIT) the proof applies Proposition (Stabilizer Pauli Spectrum) and Proposition (Invariance) inside the SPF definition

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{Z}_\beta(\phi)=e^{-\beta}[(d_n^2-d_n)+d_n\cosh\beta]$ for every pure stabilizer state, independently of which one.
  - target (EXACT): $\mathcal{Z}_{\beta}(\phi)=e^{-\beta}\left[(d_{n}^{2}-d_{n})+d_{n}\cosh\beta\right]$ for every $\phi\in{\rm Stab}_n$, with $d_n=2^n$.
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL n in positive integers
    - quantifier: FORALL \phi in ${\rm Stab}_n$
    - assumption (SOURCE_EXPLICIT): the stabilizer spectrum is $d_n$ entries $\pm1$ and $d_n^2-d_n$ zeros
    - assumption (SOURCE_RECONSTRUCTED): $\cosh(\pm\beta)=\cosh\beta$ and $\cosh0=1$
    - normalization: at $\beta=0$ this reduces to $d_n^2$, consistent with the state-independent high-temperature value
- `component:conclusion-2` [CONCLUSION / MAPPED] $\mathcal{Z}^c_\beta({\rm STAB}_n)=e^{-\beta}d_n(\cosh\beta-1)$.
  - target (EXACT): $\mathcal{Z}^{c}_{\beta}(\phi)=e^{-\beta}d_{n}(\cosh\beta-1)$ for every $\phi\in{\rm Stab}_n$.
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL \phi in ${\rm Stab}_n$
    - assumption (SOURCE_EXPLICIT): $\mathcal{Z}^c_\beta=\mathcal{Z}_\beta-d_n^2e^{-\beta}$
    - assumption (SOURCE_EXPLICIT): the stabilizer SPF value of the preceding target
    - normalization: vanishes at $\beta=0$, which is why the stabilizer work is ill-defined there
- `component:model-1` [MODEL / RESIDUAL] Uniqueness is read as the SPF being 'faithful under free operations (Clifford unitaries)'.
  - residual: 'Faithful' is used here for constancy on the free set. Faithfulness in the resource-theoretic sense, vanishing exactly on free states, is established later for the stabilizer work, not for the SPF.

Records: `claim:opus5-v11-2608.14798v1/A13`, `map:opus5-v11-2608.14798v1/A13`.

## A14 — Clifford-orbit representation of the SPF

*Sec. III C 2 · Proposition · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Proposition (Clifford-orbit representation): by the orbit-stabilizer theorem the sum over the $4^n-1$ non-identity Paulis can be replaced by a Clifford average over one arbitrary non-identity Pauli, giving $\mathcal{Z}_\beta(\psi)=e^{-\beta}[\cosh\beta+\frac{d_n^2-1}{|\mathcal{C}l_n|}\sum_{C\in\mathcal{C}l_n}\cosh(\beta\tr[\psi P_C])]$ with $P_C=CPC^\dagger$, and after expanding the hyperbolic cosine an expansion in Clifford $2k$-fold channels acting on $2k$ copies of the state.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the direct Pauli-spectrum sum in the SPF definition

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $P\neq\mathbb{I}$ is an arbitrary non-identity Pauli, for instance $Z^{\otimes n}$
- `condition:source-2` (SOURCE_EXPLICIT) $\Phi^k_{\mathcal{C}l_n}(\cdot)=|\mathcal{C}l_n|^{-1}\sum_{C}C^{\otimes k}(\cdot)C^{\dagger\otimes k}$ is the Clifford $k$-fold twirl

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{Z}_\beta(\psi)=e^{-\beta}[\cosh\beta+\frac{d_n^2-1}{|\mathcal{C}l_n|}\sum_{C}\cosh(\beta\tr[\psi P_C])]$ for any fixed non-identity $P$.
  - target (EXACT): $\mathcal{Z}_{\beta}(\psi)=e^{-\beta}\left[\cosh\beta+\frac{d_{n}^{2}-1}{|\mathcal{C}l_{n}|}\sum_{C\in\mathcal{C}l_{n}}\cosh(\beta\,\tr[\psi P_{C}])\right]$, with $P_{C}=CPC^{\dagger}$ and $P\neq\mathbb{I}$ arbitrary.
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - quantifier: FORALL P in $\mathcal{P}_n\setminus\{\mathbb{I}\}$
    - assumption (SOURCE_EXPLICIT): the Clifford group acts transitively on the non-identity Pauli strings, so the orbit-stabilizer theorem applies
    - assumption (SOURCE_RECONSTRUCTED): the identity Pauli contributes the separated $\cosh\beta$ term
    - normalization: the $\frac{d_n^2-1}{|\mathcal{C}l_n|}$ prefactor makes the Clifford average reproduce the sum over non-identity Paulis
- `component:conclusion-2` [CONCLUSION / MAPPED] Expanding $\cosh$ gives $\mathcal{Z}_\beta(\psi)=e^{-\beta}[\cosh\beta+(d_n^2-1)\sum_{k\geq0}\frac{\beta^{2k}}{(2k)!}\tr[P^{\otimes2k}\Phi^{2k}_{\mathcal{C}l_n}(\psi^{\otimes2k})]]$ for all $P\neq\mathbb{I}$.
  - target (EXACT): $\mathcal{Z}_{\beta}(\psi)=e^{-\beta}\left[\cosh\beta+(d_{n}^{2}-1)\sum_{k\geq0}\frac{\beta^{2k}}{(2k)!}\tr\!\left[P^{\otimes2k}\,\Phi^{2k}_{\mathcal{C}l_{n}}(\psi^{\otimes2k})\right]\right]$.
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - quantifier: FORALL P in $\mathcal{P}_n\setminus\{\mathbb{I}\}$
    - assumption (SOURCE_RECONSTRUCTED): termwise expansion of $\cosh$ and exchange with the finite Clifford average
    - assumption (SOURCE_EXPLICIT): $\Phi^{2k}_{\mathcal{C}l_n}$ is the Clifford $2k$-fold twirling channel
    - normalization: the coefficient is $\beta^{2k}/(2k)!$; the source prints $\beta^{2k}/2k!$ in this equation and $\beta^{2k}/(2k)!$ in the next
  - residual: The printed coefficient $\frac{\beta^{2k}}{2k!}$ in this equation is inconsistent with $\frac{\beta^{2k}}{(2k)!}$ used in the adjacent equations; the target records the intended $1/(2k)!$ and the discrepancy is reported rather than silently normalized.
- `component:evidence-1` [EVIDENCE / RESIDUAL] Because Clifford unitaries form a 2-design, the first genuinely state-dependent contribution appears beyond the universal second-moment sector, which makes the orbit representation the natural starting point for sampling-based access.
  - residual: The design-order bookkeeping is not carried out explicitly, and no complexity statement is made at this point.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · source_fidelity] The Clifford-channel expansion prints the coefficient as $\beta^{2k}/2k!$, inconsistent with $(2k)!$ elsewhere.
  - what could settle it: Errata; the adjacent equations fix the intended value.

Records: `claim:opus5-v11-2608.14798v1/A14`, `map:opus5-v11-2608.14798v1/A14`.

## A15 — Stabilizer Renyi representation of the SPF

*Sec. III C 2 · Proposition · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Proposition (Stabilizer Renyi representation): the SPF admits the expansion $\mathcal{Z}_\beta(\psi)=e^{-\beta}[d_n^2+\frac{d_n}{2}\beta^2+\sum_{k\geq2}\frac{\beta^{2k}}{(2k)!}2^{n-(k-1)M_k(\psi)}]$ in terms of the integer-index stabilizer entropies, exhibiting the SPF as a generating function for the same moment hierarchy that the integer SREs probe, with $\beta$ selecting which part of the hierarchy is emphasized.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** a fixed stabilizer Renyi entropy $M_\alpha$, which isolates one moment

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the expansion uses integer-index stabilizer entropies $M_k$
- `condition:source-2` (SOURCE_EXPLICIT) the proof is stated to follow by expressing the Pauli-spectrum moments in terms of the stabilizer Renyi entropies

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{Z}_\beta(\psi)=e^{-\beta}[d_n^2+\frac{d_n}{2}\beta^2+\sum_{k\geq2}\frac{\beta^{2k}}{(2k)!}2^{n-(k-1)M_k(\psi)}]$.
  - target (EXACT): $\mathcal{Z}_{\beta}(\psi)=e^{-\beta}\left[d_{n}^{2}+\frac{d_{n}}{2}\beta^{2}+\sum_{k\geq2}\frac{\beta^{2k}}{(2k)!}2^{\,n-(k-1)M_{k}(\psi)}\right]$.
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_RECONSTRUCTED): $\sum_{x\in{\rm Spec}(\psi)}x^{2k}=2^{\,n-(k-1)M_k(\psi)}$ for integer $k\geq2$, i.e. the definition of $M_k$ rearranged
    - assumption (SOURCE_RECONSTRUCTED): $\sum_{x\in{\rm Spec}(\psi)}x^{2}=d_n$ for pure states, which fixes the $\beta^2$ term
    - normalization: the $k=0$ term is the Pauli count $d_n^2$ and the $k=1$ term is purity-fixed; neither carries magic
- `component:conclusion-2` [CONCLUSION / RESIDUAL] The SPF is a generating function for the moment hierarchy probed by the integer stabilizer Renyi entropies, and changing $\beta$ changes which part of the hierarchy is emphasized.
  - residual: An interpretation of the displayed expansion rather than a separate mathematical statement; no quantitative notion of 'emphasis' is defined, so no distinct target is proposed.

Records: `claim:opus5-v11-2608.14798v1/A15`, `map:opus5-v11-2608.14798v1/A15`.

## A16 — Lipschitz property of the SPF

*Sec. III C 3 · Theorem · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Theorem (Lipschitz property): for two $n$-qubit pure states, $|\mathcal{Z}_\beta(\psi)-\mathcal{Z}_\beta(\phi)|\leq L_n(\beta)\|\psi-\phi\|_1$ with $L_n(\beta)=e^{-\beta}d_n\beta\sinh\beta$; an immediate corollary noted in the text is that the SPF is Lipschitz with that constant, and the paper reads the property as a physical robustness requirement rather than a convenience.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** magic measures without a continuity guarantee

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\ket{\phi},\ket{\psi}$ are $n$-qubit pure states
- `condition:source-2` (SOURCE_EXPLICIT) the proof is deferred to the supplemental material and rests on the Clifford-orbit representation
- `condition:source-3` (SOURCE_RECONSTRUCTED) no range of $\beta$ is stated in the theorem

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $|\mathcal{Z}_\beta(\psi)-\mathcal{Z}_\beta(\phi)|\leq e^{-\beta}d_n\beta\sinh\beta\,\|\psi-\phi\|_1$.
  - target (EXACT): $|\mathcal{Z}_{\beta}(\psi)-\mathcal{Z}_{\beta}(\phi)|\leq L_{n}(\beta)\,\|\psi-\phi\|_{1}$ with $L_{n}(\beta)=e^{-\beta}d_{n}\beta\sinh\beta$.
    - quantifier: FORALL \psi,\phi in pure states in $\mathcal{H}_n$
    - quantifier: FORALL \beta in range not stated in the source; the constant is not symmetric under $\beta\to-\beta$
    - assumption (SOURCE_RECONSTRUCTED): $\|\cdot\|_1$ is the trace norm of the difference of the two rank-one projectors
    - assumption (SOURCE_EXPLICIT): the proof uses the Clifford-orbit representation
    - normalization: $L_n(\beta)$ grows like $d_n=2^n$, so the bound is extensive in Hilbert-space dimension and an $\mathcal{O}(1)$ bound on the right-hand side does not bound the normalized SPF
- `component:evidence-1` [EVIDENCE / RESIDUAL] The remark states that $\beta\sinh\beta$ inherits the $\mathbb{Z}_2$ symmetry $\beta\to-\beta$ while the global prefactor reflects the energy shift in the SPF definition.
  - residual: The stated constant $L_n(\beta)=e^{-\beta}d_n\beta\sinh\beta$ is not invariant under $\beta\to-\beta$ because of the $e^{-\beta}$ prefactor, and the theorem gives no admissible range of $\beta$.
- `component:model-1` [MODEL / RESIDUAL] Robustness is presented as a fundamental requirement guaranteeing that any $\epsilon$-close state has a close SPF, not merely a convenient upper bound.
  - residual: A physical reading; no operational error model is attached, and the extensive constant is not discussed in this light.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · source_fidelity, mathematical_correctness] The Lipschitz theorem states no admissible range of $\beta$, while the accompanying remark appeals to a $\beta\to-\beta$ symmetry the stated constant does not have.
  - what could settle it: Author clarification, or restriction to $\beta\geq0$ as used throughout the applications.

Records: `claim:opus5-v11-2608.14798v1/A16`, `map:opus5-v11-2608.14798v1/A16`.

## A17 — SPF complexity (informal)

*Sec. III C 4 · Theorem · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Theorem (SPF complexity, informal): for fixed $k$ and $\beta$, the truncated SPF $\mathcal{Z}^{(k)}_\beta(\psi)$ can be estimated to additive error $\epsilon$ and failure probability $\delta$ using the same Bell-sampling primitive that estimates the Pauli-spectrum moments; approximating the normalized full SPF further requires choosing $k$ so that the truncation remainder falls below the target accuracy. The accompanying text states that a single run with additional classical memory suffices for all even moments up to order $2k$, reducing classical runtime from $\mathcal{O}(k^2n)$ to $\mathcal{O}(kn)$, and that the sampling primitive itself is independent of $\beta$.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** estimation of a single stabilizer Renyi entropy by Bell sampling, and running the moment estimator once per moment at $\mathcal{O}(k^2n)$ classical cost

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $k$ and $\beta$ are held fixed
- `condition:source-2` (SOURCE_EXPLICIT) the informal statement carries no explicit sample count; the quantitative version is deferred to the supplement
- `condition:source-3` (SOURCE_EXPLICIT) approximating the FULL SPF is stated only for the NORMALIZED quantity and requires controlling the truncation remainder

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / RESIDUAL] The truncated SPF is estimable with the Bell-sampling primitive at additive error $\epsilon$ and failure probability $\delta$, for fixed $k$ and $\beta$.
  - residual: The theorem is labelled 'informal' in the source and states no sample complexity, so it is not a fully expressible target on its own. The quantitative statements are extracted separately from the supplemental Lemma (Estimator bounds) and Theorem (Estimator Concentration), where the sample counts and the truncation floor appear.
- `component:conclusion-2` [CONCLUSION / MAPPED] One run with additional classical memory estimates all even moments up to order $2k$, reducing the classical runtime from $\mathcal{O}(k^2n)$ to $\mathcal{O}(kn)$.
  - target (EXACT): The classical post-processing cost of obtaining $\widehat{\mathbf m}_k(\psi)$ is $\mathcal{O}(kn)$ rather than $\mathcal{O}(k^{2}n)$, the latter being the cost of running the moment estimator separately for each of the $k$ even moments.
    - quantifier: FORALL k in positive integers, held fixed
    - quantifier: FORALL n in positive integers
    - assumption (SOURCE_EXPLICIT): the moment estimator of the cited Bell-sampling procedure can be implemented with additional classical memory so that one outer sample yields all even moments up to order $2k$
    - normalization: this is a classical post-processing count only; the quantum copy cost per outer sample is $2k+2$ copies of $\ket{\psi}$ and is not included
  - residual: The quantum copy cost per outer sample is not counted in the stated complexity.
- `component:conclusion-3` [CONCLUSION / MAPPED] The sampling primitive is independent of $\beta$; $\beta$ enters only at the final classical post-processing step, although the accuracy and sample complexity depend on $\beta$, $\epsilon$, $\delta$ and the truncation error.
  - target (EXACT): $\widehat{\mathcal{Z}}^{(k)}_{\beta}(\psi)=e^{-\beta}(d_{n}^{2}+d_{n}\,\mathbf{c}^{(k)}(\beta)\cdot\widehat{\mathbf m}_{k}(\psi))$, so $\beta$ enters only through the deterministic coefficient vector $\mathbf{c}^{(k)}(\beta)$ contracted with the $\beta$-independent estimate $\widehat{\mathbf m}_{k}(\psi)$.
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL k in positive integers
    - assumption (SOURCE_EXPLICIT): $\mathbf{c}^{(k)}(\beta)=(\beta^2/2!,\dots,\beta^{2k}/(2k)!)$ is computed classically after sampling
    - assumption (SOURCE_EXPLICIT): $\widehat{\mathbf m}_k$ is obtained from Bell sampling without reference to $\beta$
    - normalization: the reconstruction is exact given $\widehat{\mathbf m}_k$; only the required truncation order $k$ depends on $\beta$, through the truncation floor
  - residual: 'Independent of $\beta$' holds for the sampling primitive; the truncation order $k$ needed to make the guarantee non-vacuous does depend on $\beta$.
- `component:limitation-1` [LIMITATION / RESIDUAL] The abstract and the discussion state without qualification that the SPF and the stabilizer work are 'efficiently estimable via Bell sampling'.
  - residual: The in-text guarantee is an additive error on the normalized truncated SPF above a truncation floor that itself scales as $2^n$, and no statement in the paper propagates the estimator error to $\mathcal{W}_\beta$, which is a logarithm of a difference of the estimated quantity. Recorded as a gap between the abstract and Sec. III C 4 rather than represented.

**Open items recorded against this claim.**

- [ALIGNMENT_GAP · mathematical_correctness, empirical_support] The efficiency claim is established for an additive error on the normalized truncated SPF, whereas the abstract and discussion state it for the SPF and for the stabilizer work without qualification.
  - what could settle it: An explicit $k(n,\beta,\epsilon)$ making the truncation floor smaller than $\epsilon$, plus an error-propagation argument from $\widehat{\mathcal{Z}}$ to $\mathcal{W}_\beta$.
- [RESOURCE_LIMIT · computational_reproducibility] No code, seed or measured sample count is supplied for any estimator claim, and nothing was executed in this run.
  - what could settle it: Author-supplied implementation, or an independent reproduction with declared parameters.

Records: `claim:opus5-v11-2608.14798v1/A17`, `map:opus5-v11-2608.14798v1/A17`.

## A18 — SPF state-independent bounds

*Sec. III C 5 · Theorem · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Theorem (SPF state-independent bounds): for $\beta\in\mathbb{R}$ and any $n$-qubit pure state, $e^{-\beta}[(d_n^2-d_n)+d_n\cosh\beta]\geq\mathcal{Z}_\beta(\psi)\geq e^{-\beta}[\cosh\beta+(d_n^2-1)\cosh(\beta/\sqrt{d+1})]$, the upper bound being the stabilizer value and the lower bound obtained from a spectrum uniform over all non-trivial Pauli operators.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** $\mathcal{Z}_\beta({\rm STAB}_n)$, the unique stabilizer value

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\beta\in\mathbb{R}$
- `condition:source-2` (SOURCE_EXPLICIT) $\ket{\psi}$ is an $n$-qubit pure state
- `condition:source-3` (SOURCE_EXPLICIT) the proof expands $\mathcal{Z}_\beta=d^2+d\beta^2/2!+\sum_{k>1}(\beta^{2k}/(2k)!)\sum_P\tr[\psi P]^{2k}$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{Z}_\beta(\psi)\leq e^{-\beta}[(d_n^2-d_n)+d_n\cosh\beta]$, i.e. stabilizer states maximize the SPF.
  - target (EXACT): $\mathcal{Z}_{\beta}(\psi)\leq e^{-\beta}[(d_{n}^{2}-d_{n})+d_{n}\cosh\beta]=\mathcal{Z}_{\beta}({\rm STAB}_n)$ for every pure $\psi$.
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_RECONSTRUCTED): $\sum_{P\in\mathcal{P}_n}\tr[\psi P]^{2}=d_n$ for pure states
    - assumption (SOURCE_EXPLICIT): the upper bound follows from the SPF of the $n$-qubit stabilizer states
    - normalization: equality holds exactly on the free set and at $\beta=0$ for every state
- `component:conclusion-2` [CONCLUSION / MAPPED] $\mathcal{Z}_\beta(\psi)\geq e^{-\beta}[\cosh\beta+(d_n^2-1)\cosh(\beta/\sqrt{d_n+1})]$.
  - target (EXACT): $\mathcal{Z}_{\beta}(\psi)\geq e^{-\beta}\left[\cosh\beta+(d_{n}^{2}-1)\cosh\!\left(\beta/\sqrt{d_{n}+1}\right)\right]$.
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_RECONSTRUCTED): the extremal lower configuration assigns every non-identity Pauli the value $\pm1/\sqrt{d_n+1}$, consistent with $\sum_{P\neq\mathbb{I}}\tr[\psi P]^2=d_n-1$
    - assumption (SOURCE_RECONSTRUCTED): the identity Pauli contributes $\cosh\beta$
    - normalization: subscript on $d$ omitted in the displayed lower bound; read as $d_n=2^n$
- `component:model-1` [MODEL / RESIDUAL] The lower bound is attained by a hypothetical Pauli spectrum uniform over all non-identity Paulis.
  - residual: Realizability of that spectrum by an actual pure state is not established, so as stated the result is a bound over admissible moment sequences rather than over states. The source does not draw the distinction.

**Open items recorded against this claim.**

- [MODEL_ASSUMPTION · mathematical_correctness] The state-independent lower bound is derived from an extremal Pauli-spectrum profile whose realizability by a pure state is not established.
  - what could settle it: An explicit state achieving the profile, or restatement of the result as a bound over moment sequences.

Records: `claim:opus5-v11-2608.14798v1/A18`, `map:opus5-v11-2608.14798v1/A18`.

## A19 — SPF state-dependent bounds in terms of M2

*Sec. III C 5 · Theorem · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Theorem (SPF state-dependent bounds): $e^{-\beta}[(d_n^2-d_n)+d_n\cosh\beta]\geq\mathcal{Z}_\beta(\psi)\geq e^{-\beta}[d_n(d_n-2^{M_2(\psi)})+d_n2^{M_2(\psi)}\cosh(\beta2^{-M_2(\psi)/2})]$, with the two extremal configurations identified and a variational refinement of the lower bound deferred to the supplement.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the state-independent bounds of the preceding theorem

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\beta\in\mathbb{R}$
- `condition:source-2` (SOURCE_EXPLICIT) the proof uses the stabilizer Renyi representation together with $M_2(\psi)\geq M_{k>2}(\psi)\geq M_\infty(\psi)=0$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{Z}_\beta(\psi)\geq e^{-\beta}[d_n(d_n-2^{M_2(\psi)})+d_n2^{M_2(\psi)}\cosh(\beta2^{-M_2(\psi)/2})]$.
  - target (EXACT): $\mathcal{Z}_{\beta}(\psi)\geq e^{-\beta}\left[d_{n}(d_{n}-2^{M_{2}(\psi)})+d_{n}2^{M_{2}(\psi)}\cosh\!\left(\beta\,2^{-M_{2}(\psi)/2}\right)\right]$.
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$ with order-2 stabilizer Renyi entropy $M_2(\psi)$
    - assumption (SOURCE_EXPLICIT): $M_2(\psi)\geq M_k(\psi)$ for $k>2$ and $M_\infty(\psi)=0$
    - assumption (SOURCE_EXPLICIT): the stabilizer Renyi representation of the SPF
    - normalization: $\log$ base 2 throughout, so $2^{M_2}$ is dimensionless
- `component:model-1` [MODEL / RESIDUAL] The lower bound is achieved by a spectrum of $d_n^2-d_n2^{M_2}$ zeros and $d_n2^{M_2}$ entries $\pm2^{-M_2/2}$, while the upper bound is achieved by the stabilizer spectrum.
  - residual: Realizability of the extremal spectra by actual pure states is asserted rather than shown, as in the state-independent theorem.
- `component:evidence-1` [EVIDENCE / RESIDUAL] A variational refinement of the lower bound is provided in the supplemental material.
  - residual: Not reproduced in the main text; its statement and its relation to the displayed bound are not given at this point.

**Open items recorded against this claim.**

- [UNPROVED_LEMMA · mathematical_correctness] The proof uses $M_\infty(\psi)=0$ without qualification, but for a general pure state $M_\infty$ need not vanish.
  - what could settle it: Author clarification of the convention for $M_\infty$, or a corrected chain of inequalities.

Records: `claim:opus5-v11-2608.14798v1/A19`, `map:opus5-v11-2608.14798v1/A19`.

## A20 — SPF of highly magical states (n copies of |T>)

*Sec. IV A · Proposition · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Proposition (SPF of highly magical states): for $n$ copies of the magic $T$ state, $\mathcal{Z}_\beta(\ket{T}^{\otimes n})=e^{-\beta}[(4^n-3^n)+\sum_{k=0}^{n}\binom{n}{k}2^k\cosh(\beta2^{-k/2})]$. The accompanying text notes that at $\beta=0$ the SPF is insensitive to magic, $\mathcal{Z}_0(\ket{T}^{\otimes n})=4^n$, whereas for every $\beta\neq0$ it already separates the magic state from stabilizer states.

**System.** product states $\bigotimes_{l=1}^{n}\ket{\psi_l}$ of single-qubit pure states: magic without entanglement across any cut

**Comparison baseline.** $\mathcal{Z}_\beta({\rm STAB}_n)$, the unique stabilizer value

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the state is the $n$-fold tensor power of $\ket{T}=T\ket{+}$
- `condition:source-2` (SOURCE_EXPLICIT) $\braket{T|X|T}=\braket{T|Y|T}=1/\sqrt2$ and $\braket{T|Z|T}=0$
- `condition:source-3` (SOURCE_EXPLICIT) the weight of a Pauli string is the number of non-identity factors

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Closed form for $\mathcal{Z}_\beta(\ket{T}^{\otimes n})$.
  - target (EXACT): $\mathcal{Z}_{\beta}(\ket{T}^{\otimes n})=e^{-\beta}\left[(4^{n}-3^{n})+\sum_{k=0}^{n}\binom{n}{k}2^{k}\cosh(\beta\,2^{-k/2})\right]$.
    - quantifier: FORALL n in positive integers
    - quantifier: FORALL \beta in $\mathbb{R}$
    - assumption (SOURCE_EXPLICIT): the Pauli spectrum factorizes over tensor factors
    - assumption (SOURCE_EXPLICIT): the $4^n-3^n$ strings containing at least one $Z$ contribute zero
    - assumption (SOURCE_EXPLICIT): there are $\binom{n}{k}2^k$ weight-$k$ strings over $\{X,Y\}$, each contributing $2^{-k/2}$
    - normalization: $\mathcal{Z}_0=4^n$ recovers the Pauli count
- `component:conclusion-2` [CONCLUSION / MAPPED] $\mathcal{Z}_\beta(\ket{T}^{\otimes n})<\mathcal{Z}_\beta({\rm STAB}_n)$ for every $\beta\neq0$, with equality only at $\beta=0$.
  - target (EXACT): $\mathcal{Z}_{\beta}(\ket{T}^{\otimes n})<\mathcal{Z}_{\beta}({\rm STAB}_n)$ for every $\beta\neq0$, and $\mathcal{Z}_{0}(\ket{T}^{\otimes n})=\mathcal{Z}_{0}({\rm STAB}_n)=4^{n}$.
    - quantifier: FORALL n in positive integers
    - quantifier: FORALL \beta in $\mathbb{R}\setminus\{0\}$ for the strict inequality
    - assumption (SOURCE_EXPLICIT): the closed form of the preceding target
    - assumption (SOURCE_EXPLICIT): the stabilizer value from Proposition (Uniqueness for stabilizer states)
    - normalization: strictness is asserted for all $\beta\neq0$; no rate of separation is given
  - residual: Strictness is asserted; the source gives no derivation and no quantitative separation.

Records: `claim:opus5-v11-2608.14798v1/A20`, `map:opus5-v11-2608.14798v1/A20`.

## A21 — n-copy SPF for an arbitrary single-qubit state

*Sec. IV A · Theorem · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Theorem ($n$-copy SPF): for a single-qubit pure state $\ket{\psi}$, $\mathcal{Z}_\beta(\ket{\psi}^{\otimes n})=e^{-\beta}\sum_{w=0}^{n}\binom{n}{w}\sum_{n_x,n_y,n_z}\binom{w}{n_x\,n_y\,n_z}\cosh(\beta V_\psi[\vec n])$ with $V_\psi[\vec n]=\prod_{\sigma\in\{X,Y,Z\}}\braket{\sigma}^{n_\sigma}$.

**System.** $n$-fold tensor powers of a single-qubit pure state

**Comparison baseline.** the $\ket{T}^{\otimes n}$ special case

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\ket{\psi}$ is a single-qubit pure state and the $n$-copy state is $\ket{\psi}^{\otimes n}$
- `condition:source-2` (SOURCE_EXPLICIT) each weight-$w$ Pauli string with $n_x$ factors $X$, $n_y$ factors $Y$ and $n_z$ factors $Z$ contributes $\braket{X}^{n_x}\braket{Y}^{n_y}\braket{Z}^{n_z}$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Closed form for $\mathcal{Z}_\beta(\ket{\psi}^{\otimes n})$ for arbitrary single-qubit $\ket{\psi}$.
  - target (EXACT): $\mathcal{Z}_{\beta}(\ket{\psi}^{\otimes n})=e^{-\beta}\sum_{w=0}^{n}\binom{n}{w}\sum_{n_{x}+n_{y}+n_{z}=w}\binom{w}{n_{x}\,n_{y}\,n_{z}}\cosh(\beta\,V_{\psi}[\vec n])$, $V_{\psi}[\vec n]=\braket{X}^{n_{x}}\braket{Y}^{n_{y}}\braket{Z}^{n_{z}}$.
    - quantifier: FORALL n in positive integers
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL \psi in pure states of one qubit
    - assumption (SOURCE_EXPLICIT): the Pauli spectrum is multiplicative under tensor products
    - assumption (SOURCE_RECONSTRUCTED): the inner sum runs over $n_x+n_y+n_z=w$; the source prints the constraint implicitly
    - normalization: the $w=0$ term is the identity string contributing $\cosh0=1$ per factor
  - residual: The multi-index summation constraint $n_x+n_y+n_z=w$ is left implicit in the printed equation; it is recorded here as a SOURCE_RECONSTRUCTED assumption rather than silently inserted into the conclusion.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · source_fidelity] The $n$-copy theorem omits the summation constraint on the multinomial index.
  - what could settle it: Errata; the $\ket{T}^{\otimes n}$ special case fixes the intended reading.

Records: `claim:opus5-v11-2608.14798v1/A21`, `map:opus5-v11-2608.14798v1/A21`.

## A22 — Pauli spectrum factorizes but the SPF is not multiplicative

*Sec. IV B · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** While the Pauli spectrum factorizes under tensor products, ${\rm Spec}(\ket{\psi_A}\otimes\ket{\phi_B})={\rm Spec}(\ket{\psi_A})\times{\rm Spec}(\ket{\phi_B})$, the SPF is not multiplicative, $\mathcal{Z}_\beta(\psi_A\otimes\phi_B)\neq\mathcal{Z}_\beta(\psi_A)\mathcal{Z}_\beta(\phi_B)$; the failure is stated to be highly structured.

**System.** bipartite product pure states

**Comparison baseline.** exact multiplicativity under tensor products

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the state is a product across the $A|B$ cut

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] ${\rm Spec}(\ket{\psi_A}\otimes\ket{\phi_B})={\rm Spec}(\ket{\psi_A})\times{\rm Spec}(\ket{\phi_B})$ as a Cartesian product of multisets.
  - target (EXACT): ${\rm Spec}(\ket{\psi_{A}}\otimes\ket{\phi_{B}})=\{xy:x\in{\rm Spec}(\ket{\psi_{A}}),\,y\in{\rm Spec}(\ket{\phi_{B}})\}$ as multisets, of cardinality $d_A^2d_B^2$.
    - quantifier: FORALL \psi_A,\phi_B in pure states of the two subsystems
    - assumption (SOURCE_RECONSTRUCTED): $\tr[(\psi_A\otimes\phi_B)(P^A\otimes P^B)]=\tr[\psi_AP^A]\tr[\phi_BP^B]$
    - normalization: the product is over multisets, so multiplicities multiply
- `component:conclusion-2` [CONCLUSION / RESIDUAL] $\mathcal{Z}_\beta(\psi_A\otimes\phi_B)\neq\mathcal{Z}_\beta(\psi_A)\mathcal{Z}_\beta(\phi_B)$ in general.
  - residual: Stated as a non-identity without a quantitative gap. The quantitative replacement appears later as cSPF supermultiplicativity; at this point no inequality direction is asserted, so no target is proposed.

Records: `claim:opus5-v11-2608.14798v1/A22`, `map:opus5-v11-2608.14798v1/A22`.

## A23 — SPF for separable bipartite states

*Sec. IV B · Theorem · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Theorem (SPF for separable states): for $\ket{\psi_{AB}}=\ket{\psi_A}\otimes\ket{\psi_B}$, $\mathcal{Z}_\beta(\psi_{AB})=2^{|B|-\nu_B}\mathcal{Z}_\beta(\psi_A)+2^{|A|-\nu_A}\mathcal{Z}_\beta(\psi_B)-e^{-\beta}2^{n-\nu_A-\nu_B}\cosh\beta+\mathcal{Z}^{(R)}_\beta(\psi_{AB})$, where $\mathcal{Z}^{(R)}_\beta(\psi_{AB})=e^{-\beta}\sum_{x\in{\rm Spec}_R(\psi_A)\times{\rm Spec}_R(\psi_B)}\cosh(\beta x)$ is the residual non-stabilizer contribution obtained from the Cartesian product of the residual Pauli spectra.

**System.** bipartite separable pure states across an $A|B$ cut

**Comparison baseline.** multiplicativity, explicitly denied in the preceding paragraph

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\ket{\psi_{AB}}=\ket{\psi_A}\otimes\ket{\psi_B}$ is separable across the $A|B$ bipartition
- `condition:source-2` (SOURCE_EXPLICIT) $\nu_{A(B)}$ is the stabilizer nullity of the corresponding subsystem state

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Four-term decomposition of the joint SPF into two subsystem SPFs, a subtraction, and a residual non-stabilizer term.
  - target (EXACT): $\mathcal{Z}_{\beta}(\psi_{AB})=2^{\,n_{B}-\nu_{B}}\mathcal{Z}_{\beta}(\psi_{A})+2^{\,n_{A}-\nu_{A}}\mathcal{Z}_{\beta}(\psi_{B})-e^{-\beta}2^{\,n-\nu_{A}-\nu_{B}}\cosh\beta+\mathcal{Z}^{(R)}_{\beta}(\psi_{AB})$, where $n_{A},n_{B}$ are the qubit counts of the two factors and $\mathcal{Z}^{(R)}_{\beta}(\psi_{AB})=e^{-\beta}\sum_{x\in{\rm Spec}_{R}(\psi_{A})\times{\rm Spec}_{R}(\psi_{B})}\cosh(\beta x)$.
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL \psi_A,\psi_B in pure states of the two subsystems
    - assumption (SOURCE_EXPLICIT): ${\rm Spec}_R$ is the residual part of the canonical splitting, defined in the supplemental material
    - assumption (SOURCE_EXPLICIT): $2^{|A|-\nu_A}$ is glossed in the supplemental proof as the cardinality of ${\rm Spec}_S(\ket{\psi_A})$, which fixes $|A|$ as the QUBIT COUNT $n_A$ and not as $d_A$
    - assumption (SOURCE_EXPLICIT): the joint SPF is obtained as the average of the two equivalent splittings $i)$ and $ii)$ of ${\rm Spec}(\psi_{AB})$
    - assumption (SOURCE_EXPLICIT): the SPF is invariant under ${\rm Spec}(\psi)\mapsto-{\rm Spec}(\psi)$
    - assumption (SOURCE_EXPLICIT): $e^{-\beta}\sum_{t\in{\rm Spec}_R(\phi)}\cosh(\beta t)=\mathcal{Z}_\beta(\phi)-e^{-\beta}|{\rm Spec}_S(\phi)|\cosh\beta$ for every state
    - normalization: the exponents are rewritten here with explicit qubit counts $n_A,n_B$ because the supplemental proof's own gloss fixes that reading; the printed $|A|,|B|$ contradict the Sec. II convention and the discrepancy is reported rather than silently normalized
  - residual: The theorem is printed with $|A|,|B|$ where the supplemental proof's gloss requires qubit counts, contradicting the Sec. II convention $d_A=|A|=2^n$. Whether the decomposition reduces to Lemma (SPF for Stabilizer + Pure) when $B$ is a stabilizer state is not displayed anywhere; checking that reduction is proof work, not extraction, and was not attempted.
- `component:definition-1` [DEFINITION / RESIDUAL] $\mathcal{Z}^{(R)}_\beta(\psi_{AB})=e^{-\beta}\sum_{x\in{\rm Spec}_R(\psi_A)\times{\rm Spec}_R(\psi_B)}\cosh(\beta x)$, built from the Cartesian product of the residual Pauli spectra.
  - residual: ${\rm Spec}_R(\cdot)$ is used here without definition; it is defined only later, in the supplemental Proposition (Canonical Splitting), with no forward reference from this point.
- `component:evidence-1` [EVIDENCE / RESIDUAL] The supplemental proof derives the decomposition by averaging two equivalent splittings of ${\rm Spec}(\psi_{AB})$ and closes with the identity $e^{-\beta}\sum_{t\in{\rm Spec}_R(\phi)}\cosh(\beta t)=\mathcal{Z}_\beta(\phi)-e^{-\beta}|{\rm Spec}_S(\phi)|\cosh\beta$.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · source_fidelity] ${\rm Spec}_R(\cdot)$ is used in this main-text theorem but defined only in the supplemental material, with no forward reference at the point of use.
  - what could settle it: A cross-reference from the theorem to the supplemental Proposition (Canonical Splitting).
- [CONFLICT · source_fidelity, mathematical_correctness] The theorem writes $2^{|A|-\nu_A}$ and $2^{|B|-\nu_B}$, and its supplemental proof glosses these as cardinalities of the stabilizer spectra, which forces $|A|,|B|$ to be qubit counts; Sec. II instead fixes $|A|=d_A=2^n$.
  - what could settle it: Errata fixing one notation throughout; the supplemental gloss shows which reading the proof uses.

Records: `claim:opus5-v11-2608.14798v1/A23`, `map:opus5-v11-2608.14798v1/A23`.

## A24 — SPF for Stabilizer + Pure

*Sec. IV B · Lemma · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Lemma (SPF for Stabilizer + Pure): for $\ket{\psi_A}\otimes\ket{{\rm Stab}_B}$ with $B$ in a stabilizer state, $\mathcal{Z}_\beta(\psi_A\otimes{\rm Stab}_B)=d_B\mathcal{Z}_\beta(\psi_A)+e^{-\beta}(d_B^2-d_B)d_A^2$; for the core SPF the same case simplifies to $\mathcal{Z}^c_\beta(\psi_A\otimes{\rm Stab}_B)=d_B\mathcal{Z}^c_\beta(\psi_A)$.

**System.** separable states with one stabilizer factor

**Comparison baseline.** the general separable decomposition of the preceding theorem

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $B$ is in a pure stabilizer state and $\ket{\psi_A}$ is an arbitrary, not necessarily stabilizer, pure state
- `condition:source-2` (SOURCE_EXPLICIT) the proof uses that the stabilizer spectrum of $B$ is $d_B$ ones and $d_B^2-d_B$ zeros, and multiplicativity of the Pauli spectrum

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{Z}_\beta(\psi_A\otimes{\rm Stab}_B)=d_B\mathcal{Z}_\beta(\psi_A)+e^{-\beta}(d_B^2-d_B)d_A^2$.
  - target (EXACT): $\mathcal{Z}_{\beta}(\psi_{A}\otimes{\rm Stab}_{B})=d_{B}\,\mathcal{Z}_{\beta}(\psi_{A})+e^{-\beta}(d_{B}^{2}-d_{B})d_{A}^{2}$.
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL \psi_A in pure states of subsystem $A$
    - quantifier: FORALL {\rm Stab}_B in pure stabilizer states of subsystem $B$
    - assumption (SOURCE_EXPLICIT): the Pauli spectrum of a stabilizer state on $B$ consists of $d_B$ ones and $d_B^2-d_B$ zeros
    - assumption (SOURCE_EXPLICIT): the Pauli spectrum is multiplicative under tensor products
    - normalization: the second term collects the $d_B^2-d_B$ vanishing $B$-entries, each multiplying all $d_A^2$ entries of $A$ with $\cosh0=1$
- `component:conclusion-2` [CONCLUSION / MAPPED] $\mathcal{Z}^c_\beta(\psi_A\otimes{\rm Stab}_B)=d_B\mathcal{Z}^c_\beta(\psi_A)$.
  - target (EXACT): $\mathcal{Z}^{c}_{\beta}(\psi_{A}\otimes{\rm Stab}_{B})=d_{B}\,\mathcal{Z}^{c}_{\beta}(\psi_{A})$.
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL \psi_A in pure states of subsystem $A$
    - quantifier: FORALL {\rm Stab}_B in pure stabilizer states of subsystem $B$
    - assumption (SOURCE_EXPLICIT): $\mathcal{Z}^c_\beta=\mathcal{Z}_\beta-d^2e^{-\beta}$ with $d=d_Ad_B$ on the joint system
    - assumption (SOURCE_EXPLICIT): the SPF identity of the preceding target
    - normalization: exact multiplicativity by the factor $d_B$; this is the property that motivates using the core SPF for the monotone

Records: `claim:opus5-v11-2608.14798v1/A24`, `map:opus5-v11-2608.14798v1/A24`.

## A25 — cSPF supermultiplicativity

*Sec. IV B · Lemma · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Lemma (cSPF supermultiplicativity): for any bipartite separable pure state, $\mathcal{Z}^c_\beta(\psi_A\otimes\phi_B)\geq\frac{\mathcal{Z}^c_\beta(\psi_A)\mathcal{Z}^c_\beta(\phi_B)}{e^{-\beta}(\cosh\beta-1)}$. The proof reduces to a reweighting inequality supplied by the supplemental monotone reweighting (Chebyshev-type) lemma.

**System.** bipartite separable pure states

**Comparison baseline.** exact multiplicativity of the core SPF

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\ket{\psi_{AB}}=\ket{\psi_A}\otimes\ket{\phi_B}$
- `condition:source-2` (SOURCE_EXPLICIT) the proof sets $w_k=\beta^{2k}/(2k)!$, $a_k=\sum_i\tr[\psi_AP^A_i]^{2k}$, $b_k=\sum_j\tr[\phi_BP^B_j]^{2k}$ and uses $\sum_{k\geq1}w_k=\cosh\beta-1$
- `condition:source-3` (SOURCE_EXPLICIT) the proof asserts $p_k/p^\star_k\geq p_{k+1}/p^\star_{k+1}$ for $p_k=w_ka_k/\sum_kw_ka_k$ and $p^\star_k=w_k/\sum_kw_k$
- `condition:source-4` (SOURCE_RECONSTRUCTED) no range of $\beta$ is stated, and the right-hand side is singular at $\beta=0$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{Z}^c_\beta(\psi_A\otimes\phi_B)\geq\mathcal{Z}^c_\beta(\psi_A)\mathcal{Z}^c_\beta(\phi_B)/(e^{-\beta}(\cosh\beta-1))$.
  - target (EXACT): $\mathcal{Z}_{\beta}^{c}(\psi_{A}\otimes\phi_{B})\ \geq\ \dfrac{\mathcal{Z}_{\beta}^{c}(\psi_{A})\,\mathcal{Z}_{\beta}^{c}(\phi_{B})}{e^{-\beta}(\cosh\beta-1)}$.
    - quantifier: FORALL \psi_A,\phi_B in pure states of the two subsystems
    - quantifier: FORALL \beta in $\beta\neq0$; at $\beta=0$ both sides vanish and the quotient is $0/0$
    - assumption (SOURCE_EXPLICIT): $w_k=\beta^{2k}/(2k)!$, $a_k=\sum_i\tr[\psi_AP^A_i]^{2k}$, $b_k=\sum_j\tr[\phi_BP^B_j]^{2k}$
    - assumption (SOURCE_EXPLICIT): $p_k/p^\star_k$ is non-increasing in $k$, which is asserted rather than proved
    - assumption (SOURCE_EXPLICIT): the monotone reweighting lemma: if $a_0\geq a_1\geq\cdots\geq0$ and $p_k/q_k$ is non-increasing then $\sum_kp_ka_k\geq\sum_kq_ka_k$
    - assumption (SOURCE_EXPLICIT): $\sum_{k\geq1}w_k=\cosh\beta-1$
    - normalization: the denominator $e^{-\beta}(\cosh\beta-1)$ equals $\mathcal{Z}^c_\beta$ of a one-dimensional stabilizer reference ($d=1$ in $e^{-\beta}d(\cosh\beta-1)$); the source does not label it as such
  - residual: The load-bearing monotonicity hypothesis $p_k/p^\star_k$ non-increasing is asserted for the constructed $p$ without proof, and no range of $\beta$ is given.
- `component:assumption-1` [ASSUMPTION / RESIDUAL] $p^\star_k=w_k/\sum_kw_k$ is said to be derivable from a stabilizer state in system $A$.
  - residual: Stated without construction; it is the reference distribution against which the reweighting inequality is applied.

**Open items recorded against this claim.**

- [UNPROVED_LEMMA · mathematical_correctness] The hypothesis that $p_k/p^\star_k$ is non-increasing in $k$, on which cSPF supermultiplicativity and therefore subadditivity of the stabilizer work rest, is asserted rather than proved.
  - what could settle it: A proof that the moment sequence $a_k$ has the required log-concavity, or direct verification for the state families used.

Records: `claim:opus5-v11-2608.14798v1/A25`, `map:opus5-v11-2608.14798v1/A25`.

## A26 — Exact ensemble-averaged SPF for Haar-random states

*Sec. IV C · Theorem · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Theorem (SPF for Haar-random states): for $\beta\geq0$ the ensemble average $\mathcal{Z}_\beta({\rm Haar}_n)=\mathbb{E}_{\psi\sim{\rm Haar}_n}[\mathcal{Z}_\beta(\psi)]$ equals $e^{-\beta}[\cosh\beta+(d_n^2-1)\Gamma(\frac{d_n+1}{2})(2/\beta)^{\frac{d_n-1}{2}}I_{\frac{d_n-1}{2}}(\beta)]$, with $I_\nu$ the modified Bessel function of the first kind; the proof is deferred to the supplement and based on the Haar Pauli-spectrum density attributed to Turkeshi et al.

**System.** $\ket{\psi}=U\ket{0}^{\otimes n}$ with $U$ Haar-distributed on $U(2^n)$: maximal magic and volume-law entanglement

**Comparison baseline.** $\mathcal{Z}_\beta({\rm STAB}_n)$ and the $\nu$-compressible family

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\beta\geq0$
- `condition:source-2` (SOURCE_EXPLICIT) the quantity is the ensemble average, not the SPF of any individual state
- `condition:source-3` (SOURCE_EXPLICIT) the proof rests on the imported averaged Pauli-spectrum density of Haar states

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Closed form for the Haar-averaged SPF in terms of a modified Bessel function.
  - target (EXACT): $\mathcal{Z}_{\beta}({\rm Haar}_{n})=e^{-\beta}\left[\cosh\beta+(d_{n}^{2}-1)\,\Gamma\!\left(\tfrac{d_{n}+1}{2}\right)\left(\tfrac{2}{\beta}\right)^{\frac{d_{n}-1}{2}}I_{\frac{d_{n}-1}{2}}(\beta)\right]$.
    - quantifier: FORALL n in positive integers, $d_n=2^n$
    - quantifier: FORALL \beta in $[0,\infty)$
    - assumption (SOURCE_EXPLICIT): $U$ is Haar-distributed on $U(2^n)$ and $\ket{\psi}=U\ket{0}^{\otimes n}$
    - assumption (SOURCE_EXPLICIT): the averaged Pauli-spectrum density of Haar states is the imported proposition attributed to Turkeshi et al. (2025)
    - normalization: this is the mean of $\mathcal{Z}_\beta$, so by Jensen's inequality it is not the exponential of the mean free energy; the paper itself flags that gap in Sec. V A
- `component:evidence-1` [EVIDENCE / RESIDUAL] Consistency check: the $\beta\to0$ limit recovers the normalization $\mathcal{Z}_0=d_n^2$ via the small-argument Bessel expansion.
  - residual: Given via a footnote expansion; the exchange of the $\beta\to0$ limit with the ensemble average is not discussed.

**Open items recorded against this claim.**

- [MODEL_ASSUMPTION · mathematical_correctness] The theorem is an ensemble average but is used throughout as if it characterized individual Haar states; the bridge is the concentration lemma, whose normalization is separately unclear.
  - what could settle it: Explicit statement of which downstream uses need the average and which need per-state concentration.

Records: `claim:opus5-v11-2608.14798v1/A26`, `map:opus5-v11-2608.14798v1/A26`.

## A27 — Asymptotic SPF for Haar states

*Sec. IV C · Lemma · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Lemma (Asymptotic SPF for Haar states): for fixed $\beta$ and large $d_n$, more generally whenever $\beta^2=o(d_n)$, $\mathcal{Z}_\beta({\rm Haar}_n)=e^{-\beta}[\cosh\beta+(d_n^2-1)(1+o(1))]$. The accompanying reading is that Haar spectra narrow to $d_n^2-1$ zeros plus a single one, with $|{\rm Stab}({\rm Haar}_n)|=|{\rm Supp}({\rm Haar}_n)|=1$ almost surely.

**System.** Haar-random $n$-qubit pure states in the large-dimension regime

**Comparison baseline.** the exact Bessel closed form of the preceding theorem

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\beta$ is fixed and $\beta^2=o(d_n)$
- `condition:source-2` (SOURCE_EXPLICIT) the proof substitutes the large-order expansion $\Gamma(\nu+1)(2/\beta)^\nu I_\nu(\beta)=1+\frac{\beta^2}{4(\nu+1)}+O(\beta^4/\nu^2)$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{Z}_\beta({\rm Haar}_n)=e^{-\beta}[\cosh\beta+(d_n^2-1)(1+o(1))]$ whenever $\beta^2=o(d_n)$.
  - target (ASYMPTOTIC): $\mathcal{Z}_{\beta}({\rm Haar}_{n})=e^{-\beta}\left[\cosh\beta+(d_{n}^{2}-1)(1+o(1))\right]$ as $d_n\to\infty$ with $\beta^{2}=o(d_{n})$.
    - quantifier: FORALL \beta in reals with $\beta^{2}=o(d_{n})$ along the limit
    - assumption (SOURCE_EXPLICIT): the large-order Bessel expansion quoted in the source footnote
    - assumption (SOURCE_EXPLICIT): the exact Haar-averaged SPF of the preceding theorem
    - normalization: the correction multiplies $d_n^2-1$, so an unquantified $o(1)$ still leaves an absolute error that may grow with $n$
    - approximation error: $o(1)$ relative to the $(d_n^2-1)$ prefactor, with no stated rate; valid only in the regime $\beta^2=o(d_n)$
- `component:model-1` [MODEL / RESIDUAL] Reading: Haar spectra narrow to $d_n^2-1$ zeros plus a single one, so $|{\rm Ker}_\mathcal{P}({\rm Haar}_n)|=d_n^2-1$ and $|{\rm Stab}|=|{\rm Supp}|=1$ almost surely.
  - residual: An almost-sure statement about individual states used to motivate a formula for the ensemble average; the two are not the same object.

Records: `claim:opus5-v11-2608.14798v1/A27`, `map:opus5-v11-2608.14798v1/A27`.

## A28 — Levy concentration of the SPF for Haar states

*Sec. IV C · Lemma · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Lemma (Levy's Lemma for the SPF): the probability that the properly normalized SPF of a Haar state deviates from the typical value by a factor $\epsilon\geq0$ is doubly exponentially suppressed in the qubit number, ${\rm Prob}(|\mathcal{Z}_\beta(\psi)-\mathcal{Z}_\beta({\rm Haar}_n)|/L_n(\beta)\geq\epsilon)\leq4\exp(-\frac{2}{9\pi^3}2^n\epsilon^2)$.

**System.** Haar-random $n$-qubit pure states

**Comparison baseline.** the ensemble average of the exact theorem

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the deviation is measured after dividing by the Lipschitz constant $L_n(\beta)$ of Theorem (Lipschitz property)
- `condition:source-2` (SOURCE_EXPLICIT) $\epsilon\geq0$
- `condition:source-3` (SOURCE_RECONSTRUCTED) no range of $\beta$ is stated

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Doubly exponential concentration with the explicit constant $\frac{2}{9\pi^3}$.
  - target (EXACT): ${\rm Prob}_{\ket{\psi}\sim{\rm Haar}_{n}}\!\left(\dfrac{|\mathcal{Z}_{\beta}(\psi)-\mathcal{Z}_{\beta}({\rm Haar}_{n})|}{L_{n}(\beta)}\geq\epsilon\right)\leq4\exp\!\left(-\tfrac{2}{9\pi^{3}}2^{n}\epsilon^{2}\right)$.
    - quantifier: FORALL \epsilon in $[0,\infty)$
    - quantifier: FORALL n in positive integers
    - quantifier: FORALL \beta in range not stated in the source
    - assumption (SOURCE_EXPLICIT): $L_n(\beta)=e^{-\beta}d_n\beta\sinh\beta$ from Theorem (Lipschitz property)
    - assumption (SOURCE_RECONSTRUCTED): Levy concentration for Lipschitz functions on the unit sphere of $\mathbb{C}^{2^n}$
    - normalization: deviations are measured in units of $L_n(\beta)\sim2^n$, so an $\epsilon$-level statement here is NOT an $\epsilon$-level statement on $\mathcal{Z}_\beta$ or on $d_n^{-1}\mathcal{Z}_\beta$
- `component:model-1` [MODEL / RESIDUAL] Reading: the SPF is self-averaging, so almost all Haar states share essentially the same SPF and therefore the same stabilizer work.
  - residual: Transfers a concentration statement about $\mathcal{Z}$ to $\mathcal{W}=-\log\mathcal{Z}^c+\text{const}$; the paper itself flags the Jensen gap for ensemble averages in Sec. V A, and no error propagation through the logarithm is given here.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · mathematical_correctness] The concentration lemma normalizes deviations by $L_n(\beta)\sim2^n$, so the 'doubly exponential suppression' is measured against an exponentially large yardstick.
  - what could settle it: Restatement of the deviation in absolute or $d_n^{-1}$-normalized units, which would fix what the concentration certifies.

Records: `claim:opus5-v11-2608.14798v1/A28`, `map:opus5-v11-2608.14798v1/A28`.

## A29 — Algorithm 1, Haar state certification via the SPF

*Sec. IV C · Algorithm · modality ASSERTED · coverage PARTIAL*

**What the authors said.** An algorithm is given that decides, from a single oracle access to $\mathcal{Z}_\beta(\psi)$ at small $\beta$, whether an unknown state is Haar random: compute $\delta_\beta(\psi)=\mathcal{Z}_\beta(\psi)-e^{-\beta}(\cosh\beta+(d_n^2-1))$, normalize by $\frac{e^\beta}{(d_n^2-1)L_n(\beta)}$, and report Haar randomness when $\delta_\beta(\psi)=o(1)$ for $\beta=o(\sqrt{d_n})$.

**System.** an unknown $n$-qubit pure state promised to be Haar random or generated by some alternative process

**Comparison baseline.** the asymptotic Haar value of the preceding lemma

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) oracle access to $\mathcal{Z}_\beta(\psi)$ is assumed
- `condition:source-2` (SOURCE_EXPLICIT) $\beta=o(\sqrt{d_n})$, equivalently small $\beta$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / UNRESOLVED] The normalized deviation $\delta_\beta(\psi)$ being $o(1)$ is used as the decision criterion for Haar randomness.
  - residual: Not represented. The decision rule tests an asymptotic predicate ($\delta_\beta(\psi)=o(1)$) on a single numerical value of a single state; no finite-$n$ threshold, tolerance or failure probability is specified, so the criterion is not a determinate mathematical condition and the algorithm is not executable as written.
- `component:model-1` [MODEL / RESIDUAL] Procedure: form the deviation from the asymptotic Haar value, normalize by $\frac{e^\beta}{(d_n^2-1)L_n(\beta)}$, then branch.
  - residual: The normalization is recorded as printed; combined with the concentration lemma's $L_n(\beta)$ scaling, what the resulting dimensionless quantity certifies is not stated.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · semantic_applicability] Algorithm 1 branches on an asymptotic predicate evaluated on a single instance and assumes exact oracle access to $\mathcal{Z}_\beta$.
  - what could settle it: A finite-sample version with explicit thresholds and failure probabilities, built on the supplemental estimator.

Records: `claim:opus5-v11-2608.14798v1/A29`, `map:opus5-v11-2608.14798v1/A29`.

## A30 — Why the SPF beats a fixed-index SRE as a Haar witness

*Sec. IV C · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The effectiveness of the SPF as a Haar-randomness witness is traced to the stabilizer-entropy representation: individual $\alpha$-stabilizer Renyi entropies are generally insufficient to certify Haar randomness because many ensembles, such as sufficiently high-order unitary designs, reproduce their Haar values, and distinguishing a Haar state from a $k$-design using a fixed $\alpha$ becomes impossible whenever the corresponding moments agree, typically requiring $k<2\alpha$ for discrimination; being a fine-tuned series of the $\alpha$-stabilizer entropies, the SPF is sensitive to true Haar randomness.

**System.** $n$-qubit state ensembles, in particular unitary $k$-designs versus the Haar ensemble

**Comparison baseline.** a fixed-index stabilizer Renyi entropy $M_\alpha$

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the argument is stated in terms of agreement of the corresponding moments

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / UNRESOLVED] A fixed $\alpha$-SRE cannot distinguish a $k$-design from Haar once the corresponding moments agree, typically requiring $k<2\alpha$ for discrimination.
  - residual: Not represented: 'typically requiring $k<2\alpha$' is stated without proof, citation or a precise notion of 'typically', so the quantifier structure of the claim cannot be pinned down.
- `component:conclusion-2` [CONCLUSION / RESIDUAL] Because the SPF is a series in all the integer stabilizer entropies, it is sensitive to true Haar randomness where any fixed index is not.
  - residual: A qualitative comparative claim; no separating ensemble and no sensitivity measure is exhibited, so no determinate target is available.

**Open items recorded against this claim.**

- [MISSING_SOURCE · mathematical_correctness] The design-order condition $k<2\alpha$ for SRE-based discrimination is asserted without proof or citation.
  - what could settle it: Citation or derivation; not attempted in this run.

Records: `claim:opus5-v11-2608.14798v1/A30`, `map:opus5-v11-2608.14798v1/A30`.

## A31 — Exact ensemble SPF for nu-compressible states

*Sec. IV D · Theorem · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Theorem (SPF for $\nu$-compressible states): for $\ket{\psi}=U\ket{0}^{\otimes n}$ with $U\sim\mu_{n,\nu}$, the average SPF is $\mathcal{Z}_\beta({\rm Comp}_\nu)=2^{n-\nu}\mathcal{Z}_\beta({\rm Haar}_\nu)+e^{-\beta}4^\nu(4^{n-\nu}-2^{n-\nu})$, where $\mathcal{Z}_\beta({\rm Haar}_\nu)$ is the SPF of the reduced $\nu$-qubit Haar state.

**System.** $\nu$-compressible states, i.e. states of stabilizer nullity $\nu$, sampled from $\mu_{n,\nu}=\mu_{\mathcal{C}l_n}\ast\mu_{{\rm Haar}_\nu}$

**Comparison baseline.** Haar-random states ($\nu\to n$) and pure stabilizer states ($\nu\to0$)

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) states are sampled from the uniform measure $\mu_{n,\nu}$ on the $\nu$-compressible set
- `condition:source-2` (SOURCE_EXPLICIT) the formula is an ensemble average $\mathbb{E}_{\psi\sim\mu_\nu}[\mathcal{Z}_\beta(\psi)]$
- `condition:source-3` (SOURCE_EXPLICIT) the proof uses Clifford invariance to reach the product form $U\ket{0}^{\otimes\nu}\otimes\ket{0}^{\otimes(n-\nu)}$, then applies Lemma (SPF for Stabilizer + Pure) and the Haar theorem

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Exact ensemble SPF for $\nu$-compressible states.
  - target (EXACT): $\mathcal{Z}_{\beta}({\rm Comp}_{\nu})=2^{n-\nu}\,\mathcal{Z}_{\beta}({\rm Haar}_{\nu})+e^{-\beta}4^{\nu}(4^{n-\nu}-2^{n-\nu})$.
    - quantifier: FORALL n,\nu in integers with $0\leq\nu\leq n$
    - quantifier: FORALL \beta in $[0,\infty)$, inherited from the Haar theorem
    - assumption (SOURCE_EXPLICIT): Clifford invariance of the SPF brings every $\nu$-compressible state to the stated product form
    - assumption (SOURCE_EXPLICIT): Lemma (SPF for Stabilizer + Pure) applies to that product form
    - assumption (SOURCE_EXPLICIT): Theorem (SPF for Haar-random states) supplies $\mathcal{Z}_\beta({\rm Haar}_\nu)$
    - normalization: ensemble mean of $\mathcal{Z}_\beta$; the Jensen gap to the mean stabilizer work is flagged separately in Sec. V A

Records: `claim:opus5-v11-2608.14798v1/A31`, `map:opus5-v11-2608.14798v1/A31`.

## A32 — Limiting consistency checks and the core-SPF form for nu-compressible states

*Sec. IV D · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** As a consistency check the result is said to reduce to the Haar case as $\nu\to n$ and to the stabilizer case as $\nu\to0$; and for the core SPF the expression simplifies to $\mathcal{Z}^c_\beta({\rm Comp}_\nu)=2^{n-\nu}\mathcal{Z}^c_\beta({\rm Haar}_\nu)$.

**System.** $\nu$-compressible ensembles at the two extreme nullities

**Comparison baseline.** Theorem (SPF for Haar-random states) and Proposition (Uniqueness for stabilizer states)

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the check concerns the limits $\nu\to n$ and $\nu\to0$ of the preceding theorem

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{Z}^c_\beta({\rm Comp}_\nu)=2^{n-\nu}\mathcal{Z}^c_\beta({\rm Haar}_\nu)$.
  - target (EXACT): $\mathcal{Z}^{c}_{\beta}({\rm Comp}_{\nu})=2^{\,n-\nu}\,\mathcal{Z}^{c}_{\beta}({\rm Haar}_{\nu})$.
    - quantifier: FORALL n,\nu in integers with $0\leq\nu\leq n$
    - quantifier: FORALL \beta in $[0,\infty)$
    - assumption (SOURCE_EXPLICIT): $\mathcal{Z}^c_\beta=\mathcal{Z}_\beta-d^2e^{-\beta}$ applied on both the $n$-qubit and the $\nu$-qubit system
    - assumption (SOURCE_EXPLICIT): the exact ensemble SPF of the preceding theorem
    - normalization: the $4^n$ constant cancels exactly, which is the structural reason the core SPF is used for the monotone
- `component:evidence-1` [EVIDENCE / RESIDUAL] The stated limiting checks: reduction to the Haar case as $\nu\to n$ and to the stabilizer case as $\nu\to0$.
  - residual: The sentence cites Theorem (SPF for Haar-random states) where it means the $\nu$-compressible theorem itself; a cross-reference slip. The check is recorded as stated rather than re-derived.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · source_fidelity] The consistency-check sentence points to the Haar theorem instead of the $\nu$-compressible theorem.
  - what could settle it: Errata; the intended reference is unambiguous from context.

Records: `claim:opus5-v11-2608.14798v1/A32`, `map:opus5-v11-2608.14798v1/A32`.

## A33 — Concentration for nu-compressible states

*Sec. IV D · Lemma · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Lemma (Concentration for $\nu$-compressible states): the SPF of a $\nu$-compressible state concentrates around its mean $\mathcal{Z}_\beta({\rm Comp}_\nu)$ with Lipschitz constant $L_{{\rm Comp}_\nu}(\beta)=2^{n-\nu}L_n(\beta)$.

**System.** $\nu$-compressible $n$-qubit pure states

**Comparison baseline.** the Haar concentration lemma, $L_n(\beta)$

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the proof uses Lemma (SPF for Stabilizer + Pure) to reduce the difference of two $\nu$-compressible SPFs to a $\nu$-qubit difference scaled by $2^{n-\nu}$
- `condition:source-2` (SOURCE_EXPLICIT) the Haar concentration lemma is invoked on the $\nu$-qubit factor

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $L_{{\rm Comp}_\nu}(\beta)=2^{n-\nu}L_n(\beta)$ is the Lipschitz constant governing concentration for the $\nu$-compressible family.
  - target (EXACT): $|\mathcal{Z}_{\beta}(\psi)-\mathcal{Z}_{\beta}(\phi)|=2^{\,n-\nu}\,|\mathcal{Z}_{\beta}(U_{\psi}\ket{0}^{\otimes\nu})-\mathcal{Z}_{\beta}(U_{\phi}\ket{0}^{\otimes\nu})|$ for two $\nu$-compressible states, whence the concentration constant is $L_{{\rm Comp}_{\nu}}(\beta)=2^{\,n-\nu}L_{n}(\beta)$.
    - quantifier: FORALL \psi,\phi in $\nu$-compressible states of $n$ qubits
    - quantifier: FORALL \beta in range not stated; inherited from the Lipschitz theorem
    - assumption (SOURCE_EXPLICIT): $\mathcal{Z}_\beta$ of a $\nu$-compressible state equals $d_B\mathcal{Z}_\beta$ of its $\nu$-qubit factor plus a state-independent constant, by Lemma (SPF for Stabilizer + Pure)
    - assumption (SOURCE_EXPLICIT): $U_\psi,U_\phi$ are $\nu$-qubit non-Clifford unitaries
    - normalization: the stated constant combines a $2^{n-\nu}$ prefactor with the $n$-qubit Lipschitz constant, which already carries $d_n=2^n$; whether $L_\nu$ or $L_n$ is intended is not stated
  - residual: The reduction yields a $\nu$-qubit SPF difference, so the natural constant would involve $L_\nu(\beta)$; the source writes $2^{n-\nu}L_n(\beta)$. The discrepancy is recorded rather than corrected.
- `component:evidence-1` [EVIDENCE / RESIDUAL] The text stresses that the Haar and $\nu$-compressible concentration statements are what licenses using the SPF to characterize these families, i.e. that the SPF is self-averaging.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · mathematical_correctness] The $\nu$-compressible Lipschitz constant is written $2^{n-\nu}L_n(\beta)$ although the proof reduces the problem to a $\nu$-qubit difference.
  - what could settle it: Author clarification of whether $L_n$ or $L_\nu$ is intended in the prefactor.

Records: `claim:opus5-v11-2608.14798v1/A33`, `map:opus5-v11-2608.14798v1/A33`.

## A34 — Asymptotic SPF for nu-compressible states

*Sec. IV D · Lemma · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Lemma (Asymptotic SPF for $\nu$-compressible states): for fixed $\beta$ and sufficiently large $2^\nu$, specifically when $\beta^2=o(2^\nu)$, $\mathcal{Z}_\beta({\rm Comp}_\nu)=e^{-\beta}[4^n-2^{n-\nu}+2^{n-\nu}\cosh\beta]+e^{-\beta}2^{n-\nu}(4^\nu-1)o(1)$.

**System.** $\nu$-compressible ensembles with large $2^\nu$

**Comparison baseline.** the exact ensemble formula

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\beta$ fixed and $\beta^2=o(2^\nu)$
- `condition:source-2` (SOURCE_EXPLICIT) the result is stated to follow directly from the asymptotic Haar lemma

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Asymptotic form with an explicit correction term carrying the prefactor $2^{n-\nu}(4^\nu-1)$.
  - target (ASYMPTOTIC): $\mathcal{Z}_{\beta}({\rm Comp}_{\nu})=e^{-\beta}\left[4^{n}-2^{\,n-\nu}+2^{\,n-\nu}\cosh\beta\right]+e^{-\beta}2^{\,n-\nu}(4^{\nu}-1)\,o(1)$ when $\beta^{2}=o(2^{\nu})$.
    - quantifier: FORALL n,\nu in integers with $0\leq\nu\leq n$ along the limit
    - quantifier: FORALL \beta in reals with $\beta^{2}=o(2^{\nu})$
    - assumption (SOURCE_EXPLICIT): the asymptotic Haar lemma applied to the $\nu$-qubit factor
    - assumption (SOURCE_EXPLICIT): the exact ensemble formula for $\nu$-compressible states
    - normalization: the leading bracket depends on $\nu$ only through $2^{n-\nu}=|{\rm STAB}({\rm Comp}_\nu)|$
    - approximation error: correction term $e^{-\beta}2^{n-\nu}(4^{\nu}-1)\,o(1)$ with no stated rate; the $o(1)$ is vanishing only relative to that exponentially large prefactor

Records: `claim:opus5-v11-2608.14798v1/A34`, `map:opus5-v11-2608.14798v1/A34`.

## A35 — Asymptotically the SPF sees only the stabilizer-sector size

*Sec. IV D · Prose · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Recalling that $|{\rm STAB}({\rm Comp}_\nu)|=2^{n-\nu}$ and $|{\rm Ker}_\mathcal{P}({\rm Comp}_\nu)|=4^n-2^{n-\nu}$, the asymptotic expression is read as implying that the SPF of a $\nu$-compressible state depends only on the size of its stabilizer sector up to vanishing corrections.

**System.** $\nu$-compressible ensembles in the asymptotic regime

**Comparison baseline.** the exact ensemble formula, which depends on the full $\nu$-qubit Haar SPF

**Stated conditions.**

- `condition:source-1` (SOURCE_RECONSTRUCTED) the reading applies in the regime $\beta^2=o(2^\nu)$ of the asymptotic lemma

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Up to vanishing corrections the $\nu$-compressible SPF is a function of $|{\rm STAB}|=2^{n-\nu}$ alone.
  - target (ASYMPTOTIC): In the regime $\beta^{2}=o(2^{\nu})$, $\mathcal{Z}_{\beta}({\rm Comp}_{\nu})=e^{-\beta}\left[4^{n}-|{\rm STAB}|+|{\rm STAB}|\cosh\beta\right]$ up to the stated correction, with $|{\rm STAB}|=2^{\,n-\nu}$.
    - quantifier: FORALL n,\nu in integers with $0\leq\nu\leq n$ along the limit
    - assumption (SOURCE_EXPLICIT): $|{\rm STAB}({\rm Comp}_\nu)|=2^{n-\nu}$ and $|{\rm Ker}_{\mathcal{P}}({\rm Comp}_\nu)|=4^n-2^{n-\nu}$
    - assumption (SOURCE_EXPLICIT): the asymptotic lemma of the preceding assertion
    - normalization: this is precisely the stabilizer functional form with $d_n$ replaced by $|{\rm STAB}|$
    - approximation error: inherited from the asymptotic lemma: $e^{-\beta}2^{n-\nu}(4^{\nu}-1)\,o(1)$, unquantified

Records: `claim:opus5-v11-2608.14798v1/A35`, `map:opus5-v11-2608.14798v1/A35`.

## A36 — Algorithm 2, nu-compressible state verification

*Sec. IV D · Algorithm · modality ASSERTED · coverage PARTIAL*

**What the authors said.** An algorithm is given that, with oracle access to $\mathcal{Z}_\beta(\psi)$ for $\beta=o(\sqrt{2^\nu})$ and a candidate integer $\nu\leq n$, forms $\delta_\beta(\psi)=\mathcal{Z}_\beta(\psi)-e^{-\beta}[4^n-2^{n-\nu}+2^{n-\nu}\cosh\beta]$, normalizes by $\frac{e^\beta}{2^{n-\nu}(4^\nu-1)L_{{\rm Comp}_\nu}(\beta)}$, and reports $\nu$-compressibility when the result is $o(1)$. Compared with the algorithm of Gu et al. it is stated to do so at the cost of oracle access.

**System.** an unknown $n$-qubit state tested for stabilizer nullity $\nu$

**Comparison baseline.** the $\nu$-certification algorithm of Gu, Oliviero and Leone

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) oracle access to the SPF is assumed
- `condition:source-2` (SOURCE_EXPLICIT) $\beta=o(\sqrt{2^\nu})$ and the candidate $\nu$ is an integer at most $n$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / UNRESOLVED] The normalized deviation being $o(1)$ is used as the criterion for $\nu$-compressibility.
  - residual: Not represented, for the same reason as Algorithm 1: the decision rule is an asymptotic predicate evaluated on a single instance, with no finite-$n$ threshold, tolerance or failure probability, so it is not a determinate condition.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · semantic_applicability] Algorithm 2 branches on an asymptotic predicate and assumes exact oracle access.
  - what could settle it: A finite-sample version with explicit thresholds and failure probabilities.

Records: `claim:opus5-v11-2608.14798v1/A36`, `map:opus5-v11-2608.14798v1/A36`.

## A37 — SPF of t-doped states

*Sec. IV D · Corollary · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Corollary (SPF of $t$-doped states): for a $t$-doped state the SPF is given by Theorem (SPF for $\nu$-compressible states) for some $\nu\leq2lt$, because $t$-doped states are a subset of the $\nu$-compressible states.

**System.** $t$-doped states: an $n$-qubit stabilizer state acted on by at most $t$ non-Clifford gates of size $l=O(1)$

**Comparison baseline.** the general $\nu$-compressible family

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) each non-Clifford gate has constant size $l$, giving $\nu\leq2lt$
- `condition:source-2` (SOURCE_EXPLICIT) $t$-doped $\subseteq$ $\nu$-compressible

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $t$-doped states inherit the $\nu$-compressible ensemble SPF with $\nu\leq2lt$.
  - target (EXACT): For a $t$-doped state $\psi$ there exists $\nu\leq2lt$ with $\mathcal{Z}_{\beta}(\psi)$ given by the $\nu$-compressible expression $2^{n-\nu}\mathcal{Z}_{\beta}({\rm Haar}_{\nu})+e^{-\beta}4^{\nu}(4^{n-\nu}-2^{n-\nu})$.
    - quantifier: FORALL \psi in $t$-doped $n$-qubit states with gate size $l=O(1)$
    - quantifier: EXISTS \nu in integers with $\nu\leq2lt$
    - assumption (SOURCE_EXPLICIT): $t$-doped states are $\nu$-compressible with $\nu\leq2lt$
    - assumption (SOURCE_EXPLICIT): the exact ensemble SPF for $\nu$-compressible states
    - normalization: the ensemble formula is an average over $\mu_{n,\nu}$; applying it to a single $t$-doped state requires the concentration lemma, which the corollary does not restate
  - residual: The corollary transfers an ensemble-average formula to a membership statement about individual $t$-doped states; the bridging concentration argument is mentioned only in the following paragraph.

Records: `claim:opus5-v11-2608.14798v1/A37`, `map:opus5-v11-2608.14798v1/A37`.

## A38 — Oracle access is a conceptual simplification

*Sec. IV D · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The oracle-access assumption is stated to be primarily a conceptual simplification: by Theorem (SPF complexity) the SPF itself can be estimated efficiently in polynomial time, so the certification and inference procedures are efficiently implementable and do not require direct oracle access in practice.

**System.** the certification and inference algorithms of Sec. IV

**Comparison baseline.** exact oracle access to the SPF, assumed in Algorithms 1 to 3

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the replacement relies on the complexity theorem of Sec. III C 4

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / UNRESOLVED] The algorithms are efficiently implementable without oracle access because the SPF is estimable in polynomial time.
  - residual: Not represented. The cited guarantee is an additive-error guarantee on the NORMALIZED TRUNCATED SPF above a truncation floor that scales as $2^n$, whereas the algorithms branch on asymptotic predicates applied to $\mathcal{Z}_\beta$ itself. The step from the former to the latter is not made in the paper, so the claim has no determinate content to represent.

**Open items recorded against this claim.**

- [ALIGNMENT_GAP · mathematical_correctness] Replacing oracle access by the estimator is asserted without transferring the estimator's normalization and truncation conditions to the algorithms' decision rules.
  - what could settle it: A finite-sample restatement of Algorithms 1 to 3 built on the supplemental estimator, with the truncation order made explicit.

Records: `claim:opus5-v11-2608.14798v1/A38`, `map:opus5-v11-2608.14798v1/A38`.

## A39 — Exact ensemble SPF for pseudomagic subset phase states

*Sec. IV E · Theorem · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Theorem (SPF for pseudomagic states): for an $n$-qubit subset phase state sampled uniformly with $|S|=2^n$, the average SPF is $\mathcal{Z}_\beta({\rm SPS})=e^{-\beta}[\frac{(d_n-1)(d_n+2)}{2}+\cosh\beta+\frac{d_n(d_n-1)}{2}(\cosh(2\beta/d_n))^{d_n/2}]$.

**System.** subset phase states $\ket{\psi_{f,S}}$ with pseudorandom $f$ and $S=\{0,1\}^n$

**Comparison baseline.** Haar-random states and $\nu$-compressible states

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the theorem is restricted to $|S|=2^n$, the single-subset case, where only the average over the pseudorandom function remains
- `condition:source-2` (SOURCE_EXPLICIT) the result is an ensemble average over the SPS family
- `condition:source-3` (SOURCE_EXPLICIT) the proof is deferred to the supplement and rests on exact evaluation of the SPS Pauli-spectrum moments

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Closed-form ensemble SPF for subset phase states at $|S|=2^n$.
  - target (EXACT): $\mathcal{Z}_{\beta}({\rm SPS})=e^{-\beta}\left[\tfrac{(d_{n}-1)(d_{n}+2)}{2}+\cosh\beta+\tfrac{d_{n}(d_{n}-1)}{2}\left(\cosh\!\left(\tfrac{2\beta}{d_{n}}\right)\right)^{d_{n}/2}\right]$.
    - quantifier: FORALL n in positive integers with $d_n=2^n$
    - quantifier: FORALL \beta in range not stated in the theorem
    - assumption (SOURCE_EXPLICIT): $|S|=2^n$, so $S=\{0,1\}^n$ and the average is over the pseudorandom function $f$ only
    - assumption (SOURCE_EXPLICIT): exact evaluation of the SPS Pauli-spectrum moments, deferred to the supplement
    - normalization: the $\beta\to0$ value should recover $d_n^2$; the source does not verify this normalization for the SPS formula

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · semantic_applicability] The SPS closed form covers only $|S|=2^n$, whereas the pseudomagic gap construction is stated for $\log n\leq k\leq n$, so the regime of primary cryptographic interest is excluded 'by simplicity'.
  - what could settle it: Extension of the moment computation to general $|S|=2^k$, or an explicit statement of what the $|S|=2^n$ case establishes about pseudomagic.

Records: `claim:opus5-v11-2608.14798v1/A39`, `map:opus5-v11-2608.14798v1/A39`.

## A40 — Nullity fails to quantify pseudomagic; SPF curves become exponentially close

*Sec. IV E · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The result is said to make explicit that the stabilizer nullity is not a good measure of pseudomagic, since by nullity alone SPS would be as magical as Haar-random states, both having $\nu=\mathcal{O}(n)$; the $\nu$-extraction algorithm still detects a pseudomagic state efficiently, but this efficiency must not translate into efficiently distinguishing SPS from Haar states, else the definition of pseudomagic would be contradicted, and the SPF curves are stated to get exponentially close so that distinguishing them becomes challenging.

**System.** subset phase states compared with Haar-random states

**Comparison baseline.** the stabilizer nullity $\nu$, and the SPF as a finer classifier

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) both families have $\nu=\mathcal{O}(n)$
- `condition:source-2` (SOURCE_EXPLICIT) computational indistinguishability of the pseudomagic pair is the defining property assumed

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Stabilizer nullity does not separate SPS from Haar-random states, because both have $\nu=\mathcal{O}(n)$.
  - target (ASYMPTOTIC): $\nu({\rm SPS})=\mathcal{O}(n)$ and $\nu({\rm Haar}_{n})=\mathcal{O}(n)$, so $\nu$ assigns the two families the same order of magic despite the assumed magic gap.
    - quantifier: FORALL n in positive integers along the asymptotic statement
    - assumption (SOURCE_EXPLICIT): Haar states have trivial stabilizer group almost surely, hence $\nu=n$
    - assumption (SOURCE_EXPLICIT): the SPS family has $\nu=\mathcal{O}(n)$, as stated with attribution to Gu et al.
    - normalization: order-of-magnitude statement in $n$; no constants are given
    - approximation error: stated only to order $\mathcal{O}(n)$ with no constants, so it does not by itself quantify the failure to separate
- `component:limitation-1` [LIMITATION / RESIDUAL] Efficient $\nu$-extraction must not translate into efficiently distinguishing SPS from Haar states, or the pseudomagic definition would be contradicted.
  - residual: The tension is raised but not resolved; no quantitative gap statement reconciles efficient $\nu$-extraction with computational indistinguishability.
- `component:conclusion-2` [CONCLUSION / UNRESOLVED] The SPF curves of the two families get exponentially close, so distinguishing them becomes challenging.
  - residual: Not represented: 'exponentially close' is asserted without a displayed comparison of the two closed forms and without specifying in which variable or norm the closeness holds.
- `component:conclusion-3` [CONCLUSION / MAPPED] Applying Cliffords to an SPS tunes its entanglement while leaving its magic content unchanged.
  - target (EXACT): $\mathcal{Z}_{\beta}(C\ket{\rm SPS})=\mathcal{Z}_{\beta}(\ket{\rm SPS})$ for every $C\in\mathcal{C}l_n$, so every SPF-derived magic quantity is unchanged while the entanglement across a cut may be tuned.
    - quantifier: FORALL C in $\mathcal{C}l_n$
    - quantifier: FORALL \beta in $\mathbb{R}$
    - assumption (SOURCE_EXPLICIT): Clifford invariance of the SPF, Proposition (Invariance)
    - normalization: the invariance is exact; 'magic content unchanged' is read as 'unchanged as measured by any SPF-derived quantity'
  - residual: The source says 'keeping the magic content of the SPS unchanged'. Only SPF-derived invariance follows from Clifford invariance; invariance of magic content in a measure-independent sense is not established.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · mathematical_correctness] 'The SPF curves get exponentially close' is asserted without a norm, a variable, or a displayed comparison of the two closed forms.
  - what could settle it: An explicit bound on $|\mathcal{Z}_\beta({\rm SPS})-\mathcal{Z}_\beta({\rm Haar}_n)|$ in a stated regime of $\beta$ and $n$.

Records: `claim:opus5-v11-2608.14798v1/A40`, `map:opus5-v11-2608.14798v1/A40`.

## A41 — Free states and free operations of the resource theory

*Sec. V · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Taking the convex hull of pure stabilizer states as the free states, the maximal set of free operations $\mathcal{F}_{\rm all}$ is defined as the channels that preserve the SPF of stabilizer states, i.e. $\mathcal{O}\in\mathcal{F}_{\rm all}(A\to B)$ iff $\mathcal{Z}_\beta(\mathcal{O}(\phi_A))=\mathcal{Z}_\beta({\rm STAB}_B)$ for all $\phi_A\in{\rm STAB}_A$; by uniqueness this is equivalent to mapping stabilizer states to stabilizer states, so $\mathcal{F}_{\rm all}$ coincides with the stabilizer-preserving operations and contains the stabilizer operations and the completely stabilizer-preserving operations. The pure-state development instead uses $\mathcal{F}$: deterministic pure-state stabilizer protocols, namely Clifford unitaries, stabilizer-state preparation, computational-basis measurement and partial trace, provided the resulting state remains pure.

**System.** quantum channels between multi-qubit systems

**Comparison baseline.** stabilizer operations and completely stabilizer-preserving operations (CSPO)

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the mixed-state case uses the convex-roof extension of the SPF
- `condition:source-2` (SOURCE_EXPLICIT) the monotone statements of Sec. V are restricted to pure-state transformations
- `condition:source-3` (SOURCE_EXPLICIT) $\mathcal{F}$ consists of deterministic protocols whose output remains pure

**Components, their accounting and their targets.**

- `component:definition-1` [DEFINITION / RESIDUAL] $\mathcal{F}_{\rm all}(A\to B)=\{\mathcal{O}:\mathcal{Z}_\beta(\mathcal{O}(\phi_A))=\mathcal{Z}_\beta({\rm STAB}_B)\ \forall\phi_A\in{\rm STAB}_A\}$.
  - residual: Registered as a Definition record. The condition is stated for $\beta$ without a quantifier; whether one $\beta$ or all $\beta$ is intended is not said, though uniqueness makes the two coincide.
- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{F}_{\rm all}$ is equivalent to requiring that stabilizer states be mapped to stabilizer states, hence coincides with the stabilizer-preserving operations.
  - target (EXACT): $\mathcal{O}\in\mathcal{F}_{\rm all}(A\to B)\iff\mathcal{O}({\rm STAB}_A)\subseteq{\rm STAB}_B$, i.e. $\mathcal{F}_{\rm all}$ equals the class of stabilizer-preserving operations.
    - quantifier: FORALL \mathcal{O} in ${\rm CPTP}(A\to B)$
    - quantifier: FORALL \phi_A in ${\rm STAB}_A$
    - assumption (SOURCE_EXPLICIT): Proposition (Uniqueness for stabilizer states): the SPF takes one common value on ${\rm STAB}_B$ and, by the state-independent bounds, is strictly smaller elsewhere
    - assumption (SOURCE_EXPLICIT): the convex-roof extension is used so the criterion also applies to mixed outputs
    - normalization: the equivalence uses that the stabilizer SPF value is attained only on stabilizer states, which is the strict part of the state-independent upper bound
- `component:definition-2` [DEFINITION / RESIDUAL] $\mathcal{F}$ is the class of deterministic pure-state stabilizer protocols: Clifford unitaries, stabilizer-state preparation, computational-basis measurement and partial trace with pure output.
  - residual: Registered as a Definition record. This smaller class, not $\mathcal{F}_{\rm all}$, is the one under which the stabilizer work is later claimed to be a monotone.

Records: `claim:opus5-v11-2608.14798v1/A41`, `map:opus5-v11-2608.14798v1/A41`.

## A42 — Why the monotone is built from the core SPF

*Sec. V · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The monotone is built from the core SPF because the SPF contains a leading-order contribution that scales with the number of Pauli operators and therefore does not combine multiplicatively under tensor products in a manner consistent with the higher-order moment structure; removing it yields a quantity with more natural compositional properties.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the unfiltered SPF $\mathcal{Z}_\beta$

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the removed term is the Pauli count $d^2e^{-\beta}$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / RESIDUAL] The leading SPF term spoils multiplicative composition under tensor products, and the core SPF has more natural compositional properties.
  - residual: Not represented: a motivation rather than a proved statement. No counterexample or inequality is given at this point; the quantitative version appears later as cSPF supermultiplicativity and as the exact factor $d_B$ in the Stabilizer + Pure lemma.

Records: `claim:opus5-v11-2608.14798v1/A42`, `map:opus5-v11-2608.14798v1/A42`.

## A43 — Stated properties of the stabilizer work

*Sec. V · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The stabilizer work is stated to have five properties: (1) faithfulness, $\mathcal{W}_\beta(\psi_A)=0\iff\psi_A\in{\rm STAB}_A$; (2) Clifford invariance; (3) sub-additivity; (4) monotonicity under discarding systems, $\mathcal{W}_\beta(\psi_A\otimes\chi_B)\geq\mathcal{W}_\beta(\psi_A)$; (5) invariance under appending a stabilizer state.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** $M_\alpha$ at fixed $\alpha$, a monotone only for $\alpha\geq2$, and the integer-valued stabilizer nullity

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the list is stated for the class $\mathcal{F}$ of deterministic pure-state stabilizer protocols
- `condition:source-2` (SOURCE_RECONSTRUCTED) no range of $\beta$ is attached to the list, although $\mathcal{Z}^c_0\equiv0$ makes $\mathcal{W}_0$ ill-defined

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Faithfulness: $\mathcal{W}_\beta(\psi_A)=0$ if and only if $\psi_A$ is a stabilizer state.
  - target (EXACT): $\mathcal{W}_{\beta}(\psi)=0\iff\psi\in{\rm STAB}$.
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - quantifier: FORALL \beta in $\beta\neq0$; at $\beta=0$ the definition degenerates
    - assumption (SOURCE_EXPLICIT): uniqueness of the core SPF on pure stabilizer states
    - assumption (SOURCE_RECONSTRUCTED): the core SPF is strictly smaller than the stabilizer value off the free set, from the state-independent bounds
    - normalization: $\mathcal{W}_\beta\geq0$ with equality exactly on the free set, for $\beta\neq0$
  - residual: The $\beta=0$ case is degenerate: $\mathcal{Z}^c_0\equiv0$ for every state, so $\mathcal{W}_0$ is a logarithm of $0/0$ and faithfulness is vacuous there. The source does not exclude $\beta=0$ from the list.
- `component:conclusion-2` [CONCLUSION / MAPPED] Clifford invariance: $\mathcal{W}_\beta(\mathcal{U}(\psi))=\mathcal{W}_\beta(\psi)$ for all $\mathcal{U}\in\mathcal{C}l_n$.
  - target (EXACT): $\mathcal{W}_{\beta}(\mathcal{U}(\psi))=\mathcal{W}_{\beta}(\psi)$ for every $\mathcal{U}\in\mathcal{C}l_n$.
    - quantifier: FORALL \mathcal{U} in $\mathcal{C}l_n$
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - quantifier: FORALL \beta in $\beta\neq0$
    - assumption (SOURCE_EXPLICIT): Proposition (Invariance) for the SPF, which extends to the core SPF
    - normalization: invariance holds pointwise in $\beta$; the source writes $\mathcal{W}(\psi)$ without subscript on the right-hand side of property 2
  - residual: Property 2 is printed as $\mathcal{W}_\beta(\mathcal{U}(\psi))=\mathcal{W}(\psi)$, dropping the $\beta$ subscript on the right; read as a typographical slip.
- `component:conclusion-3` [CONCLUSION / MAPPED] Invariance under appending a stabilizer state: $\mathcal{W}_\beta(\psi_A)=\mathcal{W}_\beta(\psi\otimes\phi_B)$ for any $\phi_B\in{\rm STAB}_B$.
  - target (EXACT): $\mathcal{W}_{\beta}(\psi_{A}\otimes\phi_{B})=\mathcal{W}_{\beta}(\psi_{A})$ for every $\phi_{B}\in{\rm STAB}_{B}$.
    - quantifier: FORALL \psi_A in pure states of subsystem $A$
    - quantifier: FORALL \phi_B in ${\rm STAB}_B$
    - quantifier: FORALL \beta in $\beta\neq0$
    - assumption (SOURCE_EXPLICIT): $\mathcal{Z}^c_\beta(\psi_A\otimes{\rm Stab}_B)=d_B\mathcal{Z}^c_\beta(\psi_A)$, from Lemma (SPF for Stabilizer + Pure)
    - assumption (SOURCE_RECONSTRUCTED): $\mathcal{Z}^c_\beta({\rm STAB}_{AB})=d_B\mathcal{Z}^c_\beta({\rm STAB}_A)$ for the reference value
    - normalization: the cancellation of $d_B$ is exactly why the core SPF, not the SPF, is used
- `component:limitation-1` [LIMITATION / UNRESOLVED] The abstract, the introduction and the discussion additionally call $\mathcal{W}_\beta$ a magic monotone at every temperature.
  - residual: The five listed properties do not include monotonicity under the free class $\mathcal{F}$. Property 4 is monotonicity under DISCARDING SUBSYSTEMS only; no statement or proof in the paper covers computational-basis measurement or stabilizer-state preparation, which are declared elements of $\mathcal{F}$. The abstract's unqualified 'magic monotone' is therefore stronger than what Sec. V establishes, and the discussion's own open-problem list asks for monotonicity to be established for the mixed-state and channel extensions.

**Open items recorded against this claim.**

- [ALIGNMENT_GAP · mathematical_correctness, source_fidelity] The abstract asserts that the stabilizer work is a magic monotone for every $\beta$, while Sec. V proves only faithfulness, Clifford invariance, sub-additivity, monotonicity under discarding subsystems and stabilizer-ancilla invariance; monotonicity under the declared free class $\mathcal{F}$ is not proved anywhere in the paper.
  - what could settle it: A proof of monotonicity under every element of $\mathcal{F}$, or a weakening of the abstract. Theorem (One-shot interconversion bound) explicitly invokes the unproved monotonicity as its first inequality.
- [SCOPE_AMBIGUITY · mathematical_correctness] $\beta=0$ is not excluded from the property list although $\mathcal{Z}^c_0\equiv0$ makes $\mathcal{W}_0$ ill-defined.
  - what could settle it: Explicit restriction to $\beta\neq0$, as is done in Sec. VI B for the interconversion family.

Records: `claim:opus5-v11-2608.14798v1/A43`, `map:opus5-v11-2608.14798v1/A43`.

## A44 — Sub-additivity of the stabilizer work

*Sec. V · Theorem · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Theorem (Sub-additivity of Stabilizer work): for arbitrary pure states, $\mathcal{W}_\beta(\psi_A\otimes\phi_B)\leq\mathcal{W}_\beta(\psi_A)+\mathcal{W}_\beta(\phi_B)$, following from supermultiplicativity of the core SPF together with uniqueness on stabilizer states. The following paragraph adds that $b_k\leq d_B$, so any probabilistic combination gives $\sum_kp_kb_k\leq d_B$, which is stated to establish monotonicity under subsystem discarding, with equality only when the discarded subsystem is a stabilizer state.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** additivity, which the stabilizer Renyi entropies satisfy exactly

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\psi_A,\phi_B$ are arbitrary pure states of the two subsystems
- `condition:source-2` (SOURCE_EXPLICIT) the proof rests on Lemma (cSPF supermultiplicativity), whose reweighting hypothesis is asserted rather than proved
- `condition:source-3` (SOURCE_EXPLICIT) the discarding argument uses the notation of the supermultiplicativity proof, where $b_k\leq d_B$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{W}_\beta(\psi_A\otimes\phi_B)\leq\mathcal{W}_\beta(\psi_A)+\mathcal{W}_\beta(\phi_B)$.
  - target (EXACT): $\mathcal{W}_{\beta}(\psi_{A}\otimes\phi_{B})\leq\mathcal{W}_{\beta}(\psi_{A})+\mathcal{W}_{\beta}(\phi_{B})$.
    - quantifier: FORALL \psi_A,\phi_B in pure states of the two subsystems
    - quantifier: FORALL \beta in $\beta\neq0$
    - assumption (SOURCE_EXPLICIT): Lemma (cSPF supermultiplicativity)
    - assumption (SOURCE_EXPLICIT): uniqueness of the core SPF on pure stabilizer states, supplying the reference value
    - normalization: the inequality direction follows from supermultiplicativity after taking $-\log$, which reverses it
  - residual: Inherits the unproved reweighting hypothesis of Lemma (cSPF supermultiplicativity).
- `component:conclusion-2` [CONCLUSION / MAPPED] Monotonicity under discarding systems: $\mathcal{W}_\beta(\psi_A\otimes\chi_B)\geq\mathcal{W}_\beta(\psi_A)$, with equality only when the discarded subsystem is a stabilizer state.
  - target (EXACT): $\mathcal{W}_{\beta}(\psi_{A}\otimes\chi_{B})\geq\mathcal{W}_{\beta}(\psi_{A})$, with equality iff $\chi_{B}\in{\rm STAB}_{B}$.
    - quantifier: FORALL \psi_A,\chi_B in pure states of the two subsystems
    - quantifier: FORALL \beta in $\beta\neq0$
    - assumption (SOURCE_EXPLICIT): $b_k\leq d_B$ in the notation of the supermultiplicativity proof
    - assumption (SOURCE_EXPLICIT): any probabilistic combination therefore satisfies $\sum_kp_kb_k\leq d_B$
    - normalization: the bound $b_k\leq d_B$ is saturated exactly on stabilizer states, which gives the equality condition
  - residual: Presented as a two-line consequence in running text rather than as a proved statement; the step from $\sum_kp_kb_k\leq d_B$ to the displayed inequality is not written out. This is the property that the abstract's 'magic monotone' claim rests on, and it covers only discarding, not measurement or preparation.

Records: `claim:opus5-v11-2608.14798v1/A44`, `map:opus5-v11-2608.14798v1/A44`.

## A45 — Bounds on the stabilizer work

*Sec. V · Corollary · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Corollary (Bounds on Stabilizer Work): for any $\beta\in\mathbb{R}$ and any $n$-qubit state, $0\leq\mathcal{W}_\beta(\psi)\leq\log\frac{d_A\cosh\beta-d_A}{\cosh\beta+(d_A^2-1)\cosh(\beta/\sqrt{d_A+1})-d_A^2}$, following directly from the state-independent SPF bounds.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the state-independent SPF bounds from which it is derived

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\beta\in\mathbb{R}$ as stated, although the expression is singular at $\beta=0$
- `condition:source-2` (SOURCE_EXPLICIT) $d_A=2^n$ is the dimension of the $n$-qubit system

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Explicit two-sided bound on $\mathcal{W}_\beta$.
  - target (EXACT): $0\leq\mathcal{W}_{\beta}(\psi)\leq\log\!\left(\dfrac{d_{A}\cosh\beta-d_{A}}{\cosh\beta+(d_{A}^{2}-1)\cosh\!\left(\beta/\sqrt{d_{A}+1}\right)-d_{A}^{2}}\right)$.
    - quantifier: FORALL \beta in $\mathbb{R}\setminus\{0\}$; the displayed expression is $0/0$ at $\beta=0$
    - quantifier: FORALL \psi in pure states of an $n$-qubit system $A$, $d_A=2^n$
    - assumption (SOURCE_EXPLICIT): Theorem (SPF state-independent bounds) applied to $\mathcal{Z}^c_\beta=\mathcal{Z}_\beta-d_A^2e^{-\beta}$
    - assumption (SOURCE_EXPLICIT): $\mathcal{Z}^c_\beta({\rm STAB}_n)=e^{-\beta}d_A(\cosh\beta-1)$ supplies the numerator
    - normalization: $\log$ base 2; numerator and denominator are $e^{\beta}\mathcal{Z}^c_\beta$ of the stabilizer reference and of the extremal lower configuration respectively, so the common factor $e^{-\beta}$ cancels
  - residual: The upper bound inherits the unestablished realizability of the extremal lower configuration from the state-independent bounds, and is singular at $\beta=0$.

Records: `claim:opus5-v11-2608.14798v1/A45`, `map:opus5-v11-2608.14798v1/A45`.

## A46 — Limiting regimes and the interpolation claim

*Sec. V · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** In the limit $\beta\to\infty$ the stabilizer work is stated to converge to the stabilizer nullity, recovering the support-based characterization of magic; for $\beta\to0$ the core SPF becomes insensitive to higher moments and the stabilizer work is said to reduce, at leading order, to a function of the second-order stabilizer Renyi entropy alone; hence the stabilizer work is claimed to provide a continuous and monotonic interpolation between two previously independent measures.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the stabilizer nullity $\nu$ and the 2-stabilizer Renyi entropy $M_2$

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the two statements concern the limits $\beta\to\infty$ and $\beta\to0$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / UNRESOLVED] $\beta\to\infty$: the stabilizer work converges to the stabilizer nullity.
  - residual: Not represented. No derivation, rate or error term is given anywhere in the paper, and the underlying SPF limit is itself printed inconsistently (with and without a factor $\tfrac12$) in Sec. III B and Sec. VI C, so the limiting value cannot be pinned down from the source.
- `component:approximation-1` [APPROXIMATION / UNRESOLVED] $\beta\to0$: the stabilizer work reduces, at leading order, to a function of $M_2$ alone.
  - residual: Not represented: the function, the expansion order and the error term are all unstated, so there is no determinate conclusion to express. R6 forbids manufacturing the missing error information.
- `component:conclusion-2` [CONCLUSION / UNRESOLVED] The stabilizer work provides a continuous and monotonic interpolation between $M_2$ and $\nu$.
  - residual: Not represented: 'monotonic' asserts monotonicity of $\beta\mapsto\mathcal{W}_\beta(\psi)$, which is nowhere proved; the numerical figures are consistent with it but no argument is supplied.

**Open items recorded against this claim.**

- [UNPROVED_LEMMA · mathematical_correctness] Monotonicity of $\beta\mapsto\mathcal{W}_\beta(\psi)$ is used verbally here and in the applications ('grows monotonically with $\beta$') but never proved.
  - what could settle it: A proof that $\partial_\beta\mathcal{W}_\beta\geq0$, or restriction of the interpolation claim to the two endpoint limits.
- [UNPROVED_LEMMA · mathematical_correctness] Both limiting statements are asserted with no derivation and no error terms, and no supplementary section supplies them.
  - what could settle it: Explicit expansions with remainder for $\beta\to0$ and $\beta\to\infty$.

Records: `claim:opus5-v11-2608.14798v1/A46`, `map:opus5-v11-2608.14798v1/A46`.

## A47 — Continuity of the stabilizer work

*Sec. V A · Lemma · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Lemma (Continuity of the Stabilizer Work): $|\mathcal{W}_\beta(\psi)-\mathcal{W}_\beta(\phi)|\leq K_n(\beta)\|\psi-\phi\|_1$ with $K_n(\beta)=2\beta^{-1}\sinh\beta$. The proof passes through the tighter intermediate estimate $|\mathcal{W}_\beta(\psi)-\mathcal{W}_\beta(\phi)|\leq d_ne^{-\beta}\beta\sinh\beta/\min\{\mathcal{Z}^c_\beta(\psi),\mathcal{Z}^c_\beta(\phi)\}\,\|\phi-\psi\|_1$, which is reused later for the stabilizer-fidelity bound.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the SPF Lipschitz constant $L_n(\beta)=e^{-\beta}d_n\beta\sinh\beta$, which is extensive in $d_n$

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\psi,\phi$ are pure $n$-qubit states
- `condition:source-2` (SOURCE_EXPLICIT) the proof uses $|\log a-\log b|\leq|a-b|/\min\{a,b\}$ for positive $a,b$
- `condition:source-3` (SOURCE_EXPLICIT) the proof then applies $\cosh x-1\geq x^2/2$ inside the state-independent SPF lower bound
- `condition:source-4` (SOURCE_RECONSTRUCTED) $\beta>0$ is required for $\beta^{-1}$ to be defined; the lemma does not say so

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $|\mathcal{W}_\beta(\psi)-\mathcal{W}_\beta(\phi)|\leq2\beta^{-1}\sinh\beta\,\|\psi-\phi\|_1$.
  - target (EXACT): $|\mathcal{W}_{\beta}(\psi)-\mathcal{W}_{\beta}(\phi)|\leq K_{n}(\beta)\,\|\psi-\phi\|_{1}$ with $K_{n}(\beta)=2\beta^{-1}\sinh\beta$, a constant independent of $n$ despite the subscript.
    - quantifier: FORALL \psi,\phi in pure states in $\mathcal{H}_n$
    - quantifier: FORALL \beta in $(0,\infty)$; the lemma states no range but $\beta^{-1}$ requires $\beta\neq0$
    - assumption (SOURCE_EXPLICIT): $|\log a-\log b|\leq|a-b|/\min\{a,b\}$ for positive $a,b$
    - assumption (SOURCE_EXPLICIT): Theorem (Lipschitz property) for the SPF
    - assumption (SOURCE_EXPLICIT): $\cosh x-1\geq x^2/2$ applied inside the state-independent SPF lower bound
    - normalization: $\mathcal{W}_\beta$ is defined with $\log$ base 2 while the inequality used in the proof is for the natural logarithm; the resulting factor $1/\ln2$ is not visible in the stated constant
  - residual: The logarithm base is not reconciled between the definition of $\mathcal{W}_\beta$ (base 2) and the inequality used in the proof (natural log), so a factor $1/\ln2$ is unaccounted for. The constant is written $K_n(\beta)$ although its stated value has no $n$ dependence.
- `component:evidence-1` [EVIDENCE / RESIDUAL] The tighter intermediate bound $d_ne^{-\beta}\beta\sinh\beta/\min\{\mathcal{Z}^c_\beta(\psi),\mathcal{Z}^c_\beta(\phi)\}$ is displayed and later reused for the stabilizer-fidelity upper bound.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · mathematical_correctness] The continuity constant is derived with a natural-logarithm inequality while $\mathcal{W}_\beta$ is defined with $\log_2$; the factor $1/\ln2$ does not appear in $K_n(\beta)$.
  - what could settle it: Recomputation with an explicit logarithm base, which also affects the constant in Proposition (Stabilizer fidelity upper bounds).

Records: `claim:opus5-v11-2608.14798v1/A47`, `map:opus5-v11-2608.14798v1/A47`.

## A48 — The continuity constant is dimension-free

*Sec. V A · Prose · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Unlike the Lipschitz constant of the SPF, the Lipschitz constant of the stabilizer work does not grow exponentially with the number of qubits, so any concentration statement about the stabilizer work is stronger than the corresponding statement obtained from the SPF; the improvement is attributed to $\mathcal{W}_\beta$ being a logarithmic ratio of partition functions and hence insensitive to the extensive normalization present in the SPF.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** $L_n(\beta)=e^{-\beta}d_n\beta\sinh\beta$

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the comparison is between $K_n(\beta)=2\beta^{-1}\sinh\beta$ and $L_n(\beta)$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $K_n(\beta)$ carries no factor $d_n$, so stabilizer-work concentration statements are stronger than SPF ones.
  - target (EXACT): $K_{n}(\beta)/L_{n}(\beta)=2e^{\beta}/(d_{n}\beta^{2})$, so the stabilizer-work Lipschitz constant is smaller than the SPF one by a factor growing like $d_{n}=2^{n}$ at fixed $\beta$.
    - quantifier: FORALL n in positive integers
    - quantifier: FORALL \beta in $(0,\infty)$
    - assumption (SOURCE_EXPLICIT): $K_n(\beta)=2\beta^{-1}\sinh\beta$ from Lemma (Continuity of the Stabilizer Work)
    - assumption (SOURCE_EXPLICIT): $L_n(\beta)=e^{-\beta}d_n\beta\sinh\beta$ from Theorem (Lipschitz property)
    - normalization: the ratio is computed here from the two stated constants; the source states the qualitative comparison only
  - residual: The displayed ratio is an immediate consequence of the two stated constants and is recorded as the representation of the qualitative comparison; the source itself gives no formula.

Records: `claim:opus5-v11-2608.14798v1/A48`, `map:opus5-v11-2608.14798v1/A48`.

## A49 — Jensen gap between ensemble-averaged SPF and averaged work

*Sec. V A · Prose · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Because $-\log$ is convex, Jensen's inequality implies that the average of $-\log X$ is not the $-\log$ of the average; in the present case $\mathbb{E}_{\psi\sim\mathcal{E}}[\mathcal{W}_\beta(\psi)]\neq\log(\mathbb{E}_{\psi\sim\mathcal{E}}[\mathcal{Z}^c_\beta(\psi)])$, so although the continuity lemma guarantees that typical values lie exponentially close to the ensemble average of the stabilizer work, determining that average still requires an independent analysis.

**System.** ensembles of $n$-qubit pure states

**Comparison baseline.** the exact ensemble-averaged SPF closed forms of Sec. IV

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $-\log$ is convex

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] The ensemble mean of the stabilizer work is not obtained from the ensemble mean of the core SPF.
  - target (EXACT): $\mathbb{E}_{\psi\sim\mathcal{E}}[\mathcal{W}_{\beta}(\psi)]\neq-\log\mathbb{E}_{\psi\sim\mathcal{E}}[\mathcal{Z}^{c}_{\beta}(\psi)]+\log\mathcal{Z}^{c}_{\beta}({\rm STAB}_n)$ in general, by convexity of $-\log$.
    - quantifier: FORALL \mathcal{E} in ensembles of $n$-qubit pure states
    - quantifier: FORALL \beta in $\beta\neq0$
    - assumption (SOURCE_EXPLICIT): $-\log$ is convex, so Jensen's inequality applies
    - normalization: the source writes the right-hand side as $\log(\mathbb{E}[\mathcal{Z}^c])$ without the minus sign and without the stabilizer reference term; recorded here with the reference term as required by the definition of $\mathcal{W}_\beta$
  - residual: The displayed inequality in the source omits the minus sign and the stabilizer reference term present in the definition of $\mathcal{W}_\beta$; Jensen also gives a definite direction, which the source does not use.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · source_fidelity] The Jensen statement is printed as $\mathbb{E}[\mathcal{W}_\beta]\neq\log(\mathbb{E}[\mathcal{Z}^c_\beta])$, dropping the minus sign and the stabilizer reference term of the definition.
  - what could settle it: Errata; the intended statement follows from the definition of the stabilizer work.

Records: `claim:opus5-v11-2608.14798v1/A49`, `map:opus5-v11-2608.14798v1/A49`.

## A50 — Small gap between individual and ensemble stabilizer work

*Sec. V A · Lemma · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Lemma (Small gap): for an ensemble $\mathcal{E}$ with $\mathcal{W}_\beta(\mathcal{E})=-\log\mathcal{Z}^c_\beta(\mathcal{E})+\log\mathcal{Z}^c_\beta({\rm Stab}_n)$, with negligible failure probability every $\psi\in\mathcal{E}$ satisfies $\mathcal{W}_\beta(\psi)-\mathcal{W}_\beta(\mathcal{E})\in[-{\rm logPoly}(n),\mathcal{O}(\beta^4)+{\rm LogPoly}(n)]$; the proof is stated to follow by controlling the tails with Markov's inequality, with details in the supplement.

**System.** ensembles of $n$-qubit pure states

**Comparison baseline.** the ensemble-averaged stabilizer work

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) 'with negligible failure probability' is not quantified
- `condition:source-2` (SOURCE_EXPLICIT) the proof is stated to control tails with Markov's inequality, details deferred
- `condition:source-3` (SOURCE_EXPLICIT) $\mathcal{Z}_\beta(\mathcal{E})$ denotes the ensemble-averaged SPF

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Two-sided confidence interval with asymmetric width, the upper side carrying an $n$-independent $\mathcal{O}(\beta^4)$ term.
  - target (ASYMPTOTIC): $\mathcal{W}_{\beta}(\psi)-\mathcal{W}_{\beta}(\mathcal{E})\in\left[-{\rm logPoly}(n),\ \mathcal{O}(\beta^{4})+{\rm LogPoly}(n)\right]$ for every $\psi\in\mathcal{E}$ outside a set of negligible probability.
    - quantifier: FORALL \mathcal{E} in ensembles of $n$-qubit pure states; no structural restriction is imposed
    - quantifier: FORALL \psi in $\mathcal{E}$, outside a set of negligible probability
    - quantifier: FORALL \beta in range not stated
    - assumption (SOURCE_EXPLICIT): $\mathcal{W}_\beta(\mathcal{E})=-\log\mathcal{Z}^c_\beta(\mathcal{E})+\log\mathcal{Z}^c_\beta({\rm Stab}_n)$ with $\mathcal{Z}_\beta(\mathcal{E})$ the ensemble-averaged SPF
    - assumption (SOURCE_EXPLICIT): Markov-type tail control, deferred to the supplement
    - normalization: the interval mixes ${\rm logPoly}(n)$ and ${\rm LogPoly}(n)$; neither is defined, and no polynomial degree or implied constant is given
    - approximation error: ${\rm logPoly}(n)$ on the lower side and $\mathcal{O}(\beta^{4})+{\rm LogPoly}(n)$ on the upper side, with unspecified constants; 'negligible failure probability' is not quantified, so the statement cannot be evaluated at any concrete $n$
  - residual: The two notations ${\rm logPoly}(n)$ and ${\rm LogPoly}(n)$ appear in the same displayed formula and neither is defined.
- `component:evidence-1` [EVIDENCE / RESIDUAL] The supplemental proof applies Markov's inequality twice, to $\exp(-\mathcal{W}_\beta)$ for the lower side and to $\exp(+\mathcal{W}_\beta)$ for the upper side, bounds the reciprocal average with the Kantorovich inequality using the state-independent SPF bounds, and closes with a union bound over the two events.
  - residual: The proof fixes the thresholds as $\epsilon=\frac{\mathcal{Z}^c_\beta(\mathcal{E})}{\mathcal{Z}^c_\beta({\rm Stab}_n)}{\rm poly}(n)$ and its reciprocal times $({\rm AM}/{\rm GM})^2$, which is where the undefined ${\rm Poly}(n)$ enters; the failure probability is therefore $1/{\rm poly}(n)$ rather than 'negligible' in the cryptographic sense used elsewhere in the paper.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · mathematical_correctness, semantic_applicability] Lemma (Small gap) is stated with undefined ${\rm logPoly}(n)$/${\rm LogPoly}(n)$ terms and an unquantified 'negligible failure probability'.
  - what could settle it: Explicit constants and polynomial degrees, or a worked instance at fixed $n$ and $\beta$.

Records: `claim:opus5-v11-2608.14798v1/A50`, `map:opus5-v11-2608.14798v1/A50`.

## A51 — Separating SPS from nu-compressible ensembles by stabilizer work

*Sec. V A · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Applying the small-gap lemma to SPS versus $\nu$-compressible states, the stabilizer work is said to distinguish the two ensembles whenever $|\mathcal{W}_\beta({\rm SPS})-\mathcal{W}_\beta({\rm Comp}_\nu)|>{\rm LogPoly}(n)$; under that condition the two confidence intervals are disjoint and any threshold between them classifies the ensembles with negligible failure probability, so the exact average SPF formulas provide a stabilizer-work quantification of pseudomagic whenever the induced work gap exceeds the combined confidence correction.

**System.** SPS versus $\nu$-compressible ensembles of $n$-qubit states

**Comparison baseline.** the stabilizer nullity, which does not separate the two families

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the upper side of the small-gap interval is controlled by an $n$-independent $\mathcal{O}(\beta^4)$ term
- `condition:source-2` (SOURCE_RECONSTRUCTED) the criterion inherits the undefined ${\rm LogPoly}(n)$ from the small-gap lemma

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / UNRESOLVED] Disjoint confidence intervals give a threshold classifier separating the two ensembles when the work gap exceeds ${\rm LogPoly}(n)$.
  - residual: Not represented: the separation criterion is stated in terms of an unquantified ${\rm LogPoly}(n)$, so it cannot be evaluated for any concrete $n$, $\nu$ or $\beta$ from the paper alone. R6 forbids supplying the missing quantification.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · semantic_applicability] The SPS-versus-compressible separation criterion depends on an undefined ${\rm LogPoly}(n)$ term.
  - what could settle it: Explicit constants, or a worked instance at fixed $n,\nu,\beta$ using the two exact closed forms.

Records: `claim:opus5-v11-2608.14798v1/A51`, `map:opus5-v11-2608.14798v1/A51`.

## A52 — Stabilizer fidelity upper bounds from the stabilizer work

*Sec. VI A · Proposition · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Proposition (Stabilizer fidelity upper bounds): for a pure state, $F_{\rm Stab}(\psi)\leq1-\frac12(\frac{\cosh\beta-1}{\beta\sinh\beta}2^{-\mathcal{W}_\beta(\psi)}\mathcal{W}_\beta(\psi))^2$ for all $\beta\geq0$, the tightest bound being obtained by optimizing over $\beta$; the proof combines faithfulness with the tighter continuity estimate, and notes that the right-hand side is independent of which stabilizer state is used so the maximization over ${\rm STAB}$ is legitimate.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** stabilizer extent $\xi$ and approximate stabilizer rank $\chi_\delta$, via the quoted $\xi\geq F_{\rm Stab}^{-1}$ and $\chi_\delta\leq1+\delta^{-2}\xi$

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\beta\geq0$
- `condition:source-2` (SOURCE_EXPLICIT) the proof uses faithfulness of $\mathcal{W}_\beta$ plus the tighter intermediate continuity estimate of Lemma (Continuity of the Stabilizer Work)
- `condition:source-3` (SOURCE_EXPLICIT) the right-hand side does not depend on the chosen stabilizer state, so maximizing over all pure stabilizer states is legitimate

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] A $\beta$-parameterized family of upper bounds on the stabilizer fidelity.
  - target (EXACT): $F_{\rm Stab}(\psi)\leq1-\tfrac12\left(\dfrac{\cosh\beta-1}{\beta\sinh\beta}\,2^{-\mathcal{W}_{\beta}(\psi)}\,\mathcal{W}_{\beta}(\psi)\right)^{2}$ for every $\beta\geq0$.
    - quantifier: FORALL \beta in $[0,\infty)$ as stated; the prefactor is $0/0$ at $\beta=0$
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_EXPLICIT): faithfulness of $\mathcal{W}_\beta$ and the tighter continuity estimate of the continuity lemma
    - assumption (SOURCE_RECONSTRUCTED): $\|\psi-\phi\|_1=2\sqrt{1-|\braket{\phi|\psi}|^2}$ for pure states
    - assumption (SOURCE_EXPLICIT): the bound $\mathcal{W}_\beta(\psi)\leq4\beta^{-1}\sinh\beta\sqrt{1-|\braket{\phi|\psi}|^2}$ for any pure stabilizer $\phi$
    - normalization: $\log$ base 2 in $\mathcal{W}$ and in the factor $2^{-\mathcal{W}_\beta}$; the unreconciled logarithm base flagged for the continuity constant propagates into this prefactor
  - residual: Inherits the logarithm-base ambiguity of the continuity lemma, and is singular at the stated endpoint $\beta=0$.
- `component:conclusion-2` [CONCLUSION / RESIDUAL] An upper bound on $F_{\rm Stab}$ lower-bounds the stabilizer extent and thereby constrains the approximate stabilizer rank.
  - residual: The chain $\xi\geq F_{\rm Stab}^{-1}$ and $\chi_\delta\leq1+\delta^{-2}\xi$ is quoted from Bravyi et al.; no new bound on the stabilizer rank is derived here, so no separate target is proposed.

Records: `claim:opus5-v11-2608.14798v1/A52`, `map:opus5-v11-2608.14798v1/A52`.

## A53 — Quoted stabilizer-fidelity lower bound from the SRE

*Sec. VI A · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The upper bound is said to be complemented by a known lower bound provided by the stabilizer Renyi entropy, printed as $\frac{\alpha-1}{2\alpha}M_\alpha(\psi)\leq F_{\rm Stab}(\psi)$.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the paper's own upper bound on the stabilizer fidelity

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) attributed to prior work on tolerant stabilizer testing and pseudomagic

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / UNRESOLVED] The paper's upper bound on the stabilizer fidelity is complemented by a known lower bound expressed through the stabilizer Renyi entropy, so the stabilizer fidelity is claimed to be sandwiched between an SRE-based and a stabilizer-work-based estimate.
  - residual: Not represented. The sandwich claim depends on the quoted inequality, which as printed is inconsistent with $F_{\rm Stab}\leq1$; with the inequality unusable as printed, the two-sided statement has no determinate content. R4 forbids substituting a repaired version on the paper's behalf.
- `component:attribution-1` [ATTRIBUTION / NON_CLAIM] $\frac{\alpha-1}{2\alpha}M_\alpha(\psi)\leq F_{\rm Stab}(\psi)$, quoted as a known complementary bound.
  - residual: Recorded exactly as printed. As printed the inequality is inconsistent with $F_{\rm Stab}\leq1$: for a four-qubit state with $M_2=2$ and $\alpha=2$ it would force $F_{\rm Stab}\geq\tfrac12$, contradicting the paper's own upper bound for magic states. The cited literature states a bound on $\log(1/F_{\rm Stab})$ rather than on $F_{\rm Stab}$, but no cited work was retrieved in this run, so the conflict is recorded and no repaired version is substituted.

**Open items recorded against this claim.**

- [CONFLICT · source_fidelity, mathematical_correctness] The quoted stabilizer-fidelity lower bound is dimensionally inconsistent with $F_{\rm Stab}\leq1$ for states with appreciable $M_\alpha$.
  - what could settle it: Check against the cited sources, which state a bound on $\log(1/F_{\rm Stab})$; this run consulted no cited paper.

Records: `claim:opus5-v11-2608.14798v1/A53`, `map:opus5-v11-2608.14798v1/A53`.

## A54 — Worked example: no stabilizer state is epsilon-close to n copies of |T>

*Sec. VI A · Prose · modality OBSERVED · coverage PARTIAL*

**What the authors said.** As a worked example, using the discarding property $\mathcal{W}_\beta(\ket{T}^{\otimes n})\geq\mathcal{W}_\beta(\ket{T})$ the paper obtains the copy-independent upper bound $F_{\rm Stab}(\ket{T}^{\otimes n})\leq1-\frac12(\frac{\cosh\beta-1}{\beta\sinh\beta}2^{-\mathcal{W}_\beta(\ket{T})}\mathcal{W}_\beta(\ket{T}))^2$ for all $\beta>0$, and reports that by straightforward numerical maximization no pure stabilizer state can be $\epsilon$-close in fidelity to $\ket{T}^{\otimes n}$ for any $\epsilon\lesssim2.27\times10^{-3}$, a bound independent of the number of copies and therefore valid as $n\to\infty$.

**System.** $n$ copies of the magic state $\ket{T}$, for arbitrary $n$

**Comparison baseline.** the $n$-dependent bounds obtainable from single-copy measures without the discarding property

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\beta>0$
- `condition:source-2` (SOURCE_EXPLICIT) the reduction to a single copy uses the discarding property $\mathcal{W}_\beta(\ket{T}^{\otimes n})\geq\mathcal{W}_\beta(\ket{T})$
- `condition:source-3` (SOURCE_EXPLICIT) the numerical value comes from a 'straightforward numerical maximization' over $\beta$, with no procedure, grid or optimizing $\beta$ reported

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Copy-independent fidelity bound for $\ket{T}^{\otimes n}$ obtained from the single-copy stabilizer work.
  - target (EXACT): $F_{\rm Stab}(\ket{T}^{\otimes n})\leq1-\tfrac12\left(\dfrac{\cosh\beta-1}{\beta\sinh\beta}\,2^{-\mathcal{W}_{\beta}(\ket{T})}\,\mathcal{W}_{\beta}(\ket{T})\right)^{2}$ for every $\beta>0$ and every $n$.
    - quantifier: FORALL n in positive integers
    - quantifier: FORALL \beta in $(0,\infty)$
    - assumption (SOURCE_EXPLICIT): Proposition (Stabilizer fidelity upper bounds)
    - assumption (SOURCE_EXPLICIT): monotonicity under discarding: $\mathcal{W}_\beta(\ket{T}^{\otimes n})\geq\mathcal{W}_\beta(\ket{T})$
    - assumption (SOURCE_RECONSTRUCTED): the right-hand side of the fidelity bound is decreasing in $\mathcal{W}_\beta$ in the relevant range
    - normalization: the bound is uniform in $n$ because only the single-copy work enters
  - residual: The step requiring the right-hand side to be monotone in $\mathcal{W}_\beta$ is not stated in the source; it is recorded as a reconstructed assumption rather than inserted into the conclusion.
- `component:evidence-1` [EVIDENCE / RESIDUAL] Numerical result: no pure stabilizer state is $\epsilon$-close in fidelity to $\ket{T}^{\otimes n}$ for $\epsilon\lesssim2.27\times10^{-3}$, independently of $n$.
  - residual: Reported from a 'straightforward numerical maximization' with no code, grid, tolerance or optimizing $\beta$ given, so the number is not reproducible from the paper. Nothing was recomputed in this run.

**Open items recorded against this claim.**

- [DATA_MISSING · computational_reproducibility] The threshold $\epsilon\lesssim2.27\times10^{-3}$ is reported without the optimizing $\beta$, the numerical procedure or code.
  - what could settle it: Author-supplied script or the optimizing $\beta$; recomputation was not attempted in this run.

Records: `claim:opus5-v11-2608.14798v1/A54`, `map:opus5-v11-2608.14798v1/A54`.

## A55 — One-shot interconversion bound

*Sec. VI B · Theorem · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Theorem (One-shot interconversion bound): if $n$ copies of $\psi$ can be converted into $m$ copies of $\phi$ by free operations then for every $\beta\neq0$, $\mathcal{W}_\beta(\phi^{\otimes m})\leq\mathcal{W}_\beta(\psi^{\otimes n})\leq n\mathcal{W}_\beta(\psi)$, the first inequality being monotonicity and the second sub-additivity; the paper remarks that the bound is strictly stronger than any single-copy bound whenever sub-additivity is strict, that is whenever neither state is a stabilizer state.

**System.** pure-state resource interconversion under the class $\mathcal{F}$

**Comparison baseline.** single-copy ratio bounds and single-monotone obstructions

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) conversion is by operations in $\mathcal{F}$, the protocol class of Sec. V
- `condition:source-2` (SOURCE_EXPLICIT) $\beta\neq0$
- `condition:source-3` (SOURCE_RECONSTRUCTED) the first inequality IS monotonicity of $\mathcal{W}_\beta$ under $\mathcal{F}$, which the paper asserts but does not prove

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{W}_\beta(\phi^{\otimes m})\leq\mathcal{W}_\beta(\psi^{\otimes n})\leq n\mathcal{W}_\beta(\psi)$ for every $\beta\neq0$.
  - target (EXACT): If there exists $\Lambda\in\mathcal{F}$ with $\Lambda(\psi^{\otimes n})=\phi^{\otimes m}$, then $\mathcal{W}_{\beta}(\phi^{\otimes m})\leq\mathcal{W}_{\beta}(\psi^{\otimes n})\leq n\,\mathcal{W}_{\beta}(\psi)$ for every $\beta\neq0$.
    - quantifier: FORALL \beta in $\mathbb{R}\setminus\{0\}$
    - quantifier: FORALL n,m in positive integers
    - quantifier: EXISTS \Lambda in $\mathcal{F}$, with $\Lambda(\psi^{\otimes n})=\phi^{\otimes m}$: the hypothesis of the theorem
    - assumption (SOURCE_RECONSTRUCTED): $\mathcal{W}_\beta$ is monotone under $\mathcal{F}$; asserted in Sec. V, not proved in the paper
    - assumption (SOURCE_EXPLICIT): sub-additivity of $\mathcal{W}_\beta$, itself resting on Lemma (cSPF supermultiplicativity)
    - normalization: conversion is exact and deterministic; no error tolerance, catalysis or asymptotic rate is involved
  - residual: The load-bearing first inequality is exactly the monotonicity property that Sec. V lists but does not prove for $\mathcal{F}$.
- `component:conclusion-2` [CONCLUSION / UNRESOLVED] The tensor-power form is strictly stronger than any single-copy bound whenever sub-additivity is strict, i.e. whenever neither state is a stabilizer state.
  - residual: Not represented: 'strictly stronger whenever sub-additivity is strict' presumes strictness for all non-stabilizer states, which is not established anywhere; the source also does not define the single-copy ratio bound it is compared against.

**Open items recorded against this claim.**

- [ALIGNMENT_GAP · mathematical_correctness] The interconversion theorem uses monotonicity of $\mathcal{W}_\beta$ under $\mathcal{F}$ as its first inequality, but that monotonicity is not established anywhere in the paper.
  - what could settle it: Same resolution as the monotonicity gap recorded against the stabilizer-work property list.

Records: `claim:opus5-v11-2608.14798v1/A55`, `map:opus5-v11-2608.14798v1/A55`.

## A56 — Incomparability from crossing curves, and the H-to-T counterexample

*Sec. VI B · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The family supplies no-go statements: if $\mathcal{W}_\beta(\psi)<\mathcal{W}_\beta(\phi)$ for all $\beta\neq0$ then $\psi$ cannot be converted into $\phi$; and because each $\beta$ is an independent witness, crossing curves certify incomparability in both directions, a statement no single monotone can express. The paper stresses that the conditions are necessary but not sufficient, exhibiting $\ket{H}$ and $\ket{T}$ as a case where $\mathcal{W}_\beta(\ket{H})\geq\mathcal{W}_\beta(\ket{T})$ for all $\beta\neq0$, so the bound does not obstruct $\ket{H}\to\ket{T}$, yet no deterministic stabilizer protocol in $\mathcal{F}$ realizes that conversion.

**System.** pure-state convertibility under $\mathcal{F}$

**Comparison baseline.** a single magic monotone, which cannot express two-directional incomparability

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\ketbra{H}{H}=(I+(X+Y+Z)/\sqrt3)/2$ and $\ketbra{T}{T}=(I+(X+Y)/\sqrt2)/2$
- `condition:source-2` (SOURCE_EXPLICIT) conversion means a deterministic protocol in $\mathcal{F}$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] If $\mathcal{W}_{\beta_1}(\psi)>\mathcal{W}_{\beta_1}(\phi)$ and $\mathcal{W}_{\beta_2}(\psi)<\mathcal{W}_{\beta_2}(\phi)$ for some $\beta_1,\beta_2\neq0$, then neither state can be converted into the other.
  - target (EXACT): If there exist $\beta_{1},\beta_{2}\neq0$ with $\mathcal{W}_{\beta_{1}}(\psi)>\mathcal{W}_{\beta_{1}}(\phi)$ and $\mathcal{W}_{\beta_{2}}(\psi)<\mathcal{W}_{\beta_{2}}(\phi)$, then no $\Lambda\in\mathcal{F}$ satisfies $\Lambda(\psi)=\phi$ and no $\Lambda'\in\mathcal{F}$ satisfies $\Lambda'(\phi)=\psi$.
    - quantifier: EXISTS \beta_1,\beta_2 in $\mathbb{R}\setminus\{0\}$ with the stated strict inequalities
    - quantifier: FORALL \Lambda in $\mathcal{F}$
    - assumption (SOURCE_RECONSTRUCTED): monotonicity of $\mathcal{W}_\beta$ under $\mathcal{F}$ at each fixed $\beta$; asserted, not proved
    - assumption (SOURCE_EXPLICIT): each value of $\beta$ furnishes an independent conversion witness
    - normalization: the two-directional conclusion uses the same monotonicity twice, once in each direction; it inherits the unproved monotonicity
  - residual: Inherits the unproved monotonicity of $\mathcal{W}_\beta$ under $\mathcal{F}$.
- `component:limitation-1` [LIMITATION / UNRESOLVED] $\ket{H}$ dominates $\ket{T}$ at every $\beta\neq0$, so the bound does not obstruct $\ket{H}\to\ket{T}$; nevertheless no deterministic stabilizer protocol realizes it, so the condition is necessary but not sufficient.
  - residual: The assertion that no deterministic stabilizer protocol converts $\ket{H}$ into $\ket{T}$ is stated without proof or citation. It is the only evidence offered for insufficiency, so the insufficiency claim rests on an unsupported statement.

**Open items recorded against this claim.**

- [MISSING_SOURCE · mathematical_correctness] The claim that no deterministic stabilizer protocol converts $\ket{H}$ into $\ket{T}$ has no proof or citation in the text, yet it carries the necessary-but-not-sufficient conclusion.
  - what could settle it: A citation or an argument; this run did not attempt to verify it.

Records: `claim:opus5-v11-2608.14798v1/A56`, `map:opus5-v11-2608.14798v1/A56`.

## A57 — Magic cost lower bound

*Sec. VI B · Corollary · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Corollary (Magic cost): defining the $T$-cost of $\phi$ as the least number $t$ of $\ket{T}$ states convertible to $\phi$ by free operations, setting $\psi=\ket{T}$, $n=t$, $m=1$ in the interconversion bound gives $t\geq\sup_{\beta\neq0}\min\{t':\mathcal{W}_\beta(T^{\otimes t'})\geq\mathcal{W}_\beta(\phi)\}$, with the weaker simpler bound $t\geq\sup_\beta\mathcal{W}_\beta(\phi)/\mathcal{W}_\beta(T)$ following by sub-additivity.

**System.** magic-state cost of preparing a target pure state under $\mathcal{F}$

**Comparison baseline.** the simpler single-copy ratio bound $\sup_\beta\mathcal{W}_\beta(\phi)/\mathcal{W}_\beta(T)$

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the $T$-cost is defined as the least $t$ with $T^{\otimes t}\to\phi$ under free operations
- `condition:source-2` (SOURCE_EXPLICIT) the corollary is an instance of the interconversion bound and inherits its hypotheses

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $t\geq\sup_{\beta\neq0}\min\{t':\mathcal{W}_\beta(T^{\otimes t'})\geq\mathcal{W}_\beta(\phi)\}$, weakening to $t\geq\sup_\beta\mathcal{W}_\beta(\phi)/\mathcal{W}_\beta(T)$.
  - target (EXACT): $t\ \geq\ \sup_{\beta\neq0}\ \min\{t'\,:\,\mathcal{W}_{\beta}(T^{\otimes t'})\geq\mathcal{W}_{\beta}(\phi)\}\ \geq\ \sup_{\beta\neq0}\ \mathcal{W}_{\beta}(\phi)/\mathcal{W}_{\beta}(T)$.
    - quantifier: FORALL \phi in pure target states
    - quantifier: FORALL \beta in $\mathbb{R}\setminus\{0\}$ in the supremum
    - assumption (SOURCE_EXPLICIT): Theorem (One-shot interconversion bound) with $\psi=\ket{T}$, $n=t$, $m=1$
    - assumption (SOURCE_EXPLICIT): sub-additivity of $\mathcal{W}_\beta$ gives the weaker ratio form
    - normalization: both bounds inherit the unproved monotonicity through the interconversion theorem
  - residual: Inherits the unproved monotonicity. The source does not state that the minimum is attained or that the set is non-empty for every $\phi$ and $\beta$.
- `component:evidence-1` [EVIDENCE / RESIDUAL] The source additionally contains, as LaTeX comments only, a closed form for $\mathcal{W}_\beta(\chi^{\otimes r})$ and the endpoint bounds $t\geq M_2(\phi)/\ln\frac43$ at $\beta\to0$ and $t\geq\nu(\phi)$ at $\beta\to\infty$.
  - residual: These appear in the source only inside commented-out LaTeX and are therefore not claims of the published text. Recorded here as a source observation so the inventory is traceable, not as an assertion of the paper.

Records: `claim:opus5-v11-2608.14798v1/A57`, `map:opus5-v11-2608.14798v1/A57`.

## A58 — Distillation converse

*Sec. VI B · Corollary · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Corollary (Distillation Converse): if $n$ copies of $\psi$ can be converted into $m$ copies of $\ket{T}$ by free operations, then for every $\beta\neq0$, $\mathcal{W}_\beta(T^{\otimes m})\leq n\mathcal{W}_\beta(\psi)$, a few-copy converse bound on distillation.

**System.** magic-state distillation under $\mathcal{F}$, at finite copy number

**Comparison baseline.** asymptotic distillation rates, which the corollary does not address

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the hypothesis is exact deterministic conversion $\psi^{\otimes n}\to T^{\otimes m}$ in $\mathcal{F}$
- `condition:source-2` (SOURCE_EXPLICIT) $\beta\neq0$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{W}_\beta(T^{\otimes m})\leq n\mathcal{W}_\beta(\psi)$ for every $\beta\neq0$.
  - target (EXACT): If there exists $\Lambda\in\mathcal{F}$ with $\Lambda(\psi^{\otimes n})=T^{\otimes m}$, then $\mathcal{W}_{\beta}(T^{\otimes m})\leq n\,\mathcal{W}_{\beta}(\psi)$ for every $\beta\neq0$.
    - quantifier: FORALL \beta in $\mathbb{R}\setminus\{0\}$
    - quantifier: FORALL n,m in positive integers
    - quantifier: EXISTS \Lambda in $\mathcal{F}$, with $\Lambda(\psi^{\otimes n})=T^{\otimes m}$
    - assumption (SOURCE_EXPLICIT): Theorem (One-shot interconversion bound) with $\phi=\ket{T}$
    - normalization: exact deterministic conversion only; no error tolerance and no asymptotic rate
  - residual: Inherits the unproved monotonicity through the interconversion theorem.

Records: `claim:opus5-v11-2608.14798v1/A58`, `map:opus5-v11-2608.14798v1/A58`.

## A59 — H2 method and the explicit basis-dependence caveat

*Sec. VI C · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The $\mathrm{H}_2$ study is set up in the minimal STO-3G basis, giving four spin-orbitals and hence a four-qubit problem under Jordan-Wigner; the ground state at each bond length is obtained by exact diagonalization, equivalently full configuration interaction within the basis, and the SPF and stabilizer work are then evaluated directly. The paper stresses at the outset that the minimal basis is far from the complete-basis-set limit and that the nonstabilizerness of a molecular ground state is not an intrinsic, basis-independent property, depending on the one-particle basis and the fermion-to-qubit encoding, so the results should be understood as a demonstration rather than converged statements about the molecule.

**System.** electronic ground state of $\mathrm{H}_2$ along the dissociation coordinate, Jordan-Wigner encoded

**Comparison baseline.** the larger 6-31G basis, repeated in the supplemental material

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) STO-3G gives four spin-orbitals and a four-qubit problem under Jordan-Wigner
- `condition:source-2` (SOURCE_EXPLICIT) ground states are obtained by exact diagonalization within the chosen basis
- `condition:source-3` (SOURCE_EXPLICIT) nonstabilizerness of a molecular ground state is stated to depend on the one-particle basis and the fermion-to-qubit encoding

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / RESIDUAL] The computed quantity is a property of a basis-and-encoding-dependent representation, not an intrinsic property of the molecule, so the section is a framework demonstration rather than a converged physical statement.
  - residual: A methodological self-limitation rather than a mathematical claim; no representation is proposed because the content is the scope restriction itself. It is retained because it bounds every numerical claim in the section.
- `component:model-1` [MODEL / MAPPED] Method: second-quantized electronic Hamiltonian mapped to qubits by Jordan-Wigner, ground state by exact diagonalization at each internuclear distance $R$, yielding $\ket{\psi(R)}$ from which $\mathcal{Z}_\beta$ and $\mathcal{W}_\beta$ are evaluated for arbitrary $\beta$.

**Semantic context.** molecular hydrogen along the H-H dissociation coordinate, mapped to four qubits (STO-3G) by the Jordan-Wigner transformation

- approximation regime: finite one-particle basis (STO-3G in the main text, 6-31G in the supplement); the paper states neither approaches the complete-basis-set limit
- error bound: none reported: no error bars, convergence study or basis extrapolation
- limitation: Nonstabilizerness here is a property of an encoding, which the authors state explicitly while still using it as the section's framing.
- limitation: No geometries, integrals, statevectors, code or numerical tolerances are provided, so no curve is reproducible from the paper alone.
- limitation: The paper declines to draw physical conclusions, so the section supports the framework's flexibility, not any statement about $\mathrm{H}_2$.

**Open items recorded against this claim.**

- [MODEL_ASSUMPTION · semantic_applicability] The mapping from 'stabilizer work of the encoded statevector' to 'nonstabilizerness of the molecule' is disclaimed by the authors but still frames the section.
  - what could settle it: An encoding-invariance study, or restriction of every claim to the encoded state.
- [DATA_MISSING · computational_reproducibility, empirical_support] No geometries, integrals, statevectors, code or numerical values are supplied for the $\mathrm{H}_2$ curves in either basis.
  - what could settle it: Author-supplied data or code; an independent recomputation would need the bond-length grid and the integral source. Not attempted in this run.

Records: `claim:opus5-v11-2608.14798v1/A59`, `map:opus5-v11-2608.14798v1/A59`.

## A60 — H2 STO-3G observations and the role of beta

*Sec. VI C · Prose · modality OBSERVED · coverage PARTIAL*

**What the authors said.** In the minimal basis the magic measures remain modest near equilibrium, rise to a clear and pronounced peak in the intermediate strongly correlated region, and then decrease toward the dissociation limit; at small $\beta$ the stabilizer work is a smooth, comparatively featureless probe, while increasing $\beta$ brings in higher-order moments and develops sharper features, and at large $\beta$ the profile tracks the overall shape of $M_{\alpha=2}$ with quantitative differences. The apparent decay of magic at large $R$ is stated to be a feature of the minimal basis that does not survive enlargement of the one-particle space.

**System.** $\mathrm{H}_2$ ground state along the dissociation coordinate in STO-3G, four qubits

**Comparison baseline.** $M_{\alpha=2}$ along the same curve

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\beta\in\{0.5,2,10,50,100\}$ are the values shown
- `condition:source-2` (SOURCE_EXPLICIT) all magic quantities are computed from the exact ground-state statevector obtained by full diagonalization
- `condition:source-3` (SOURCE_RECONSTRUCTED) the results are read off figures; no tabulated values are given

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / RESIDUAL] Nonstabilizerness is small near equilibrium, peaks in the strongly correlated intermediate region, and decreases toward dissociation within STO-3G.
  - residual: A figure-level observation. No peak position, height or tabulated values are given in the text, so the claim cannot be quantified from the source; it is retained as an observation rather than mathematized.
- `component:conclusion-2` [CONCLUSION / RESIDUAL] $\beta$ acts as a resolution knob: small $\beta$ gives a coarse smooth probe, larger $\beta$ resolves finer structure, and at large $\beta$ the profile tracks the shape of $M_{\alpha=2}$.
  - residual: 'Tracks the overall shape' is qualitative; no correlation measure or quantitative comparison is reported.
- `component:limitation-1` [LIMITATION / RESIDUAL] The apparent decay of magic at large $R$ is explicitly attributed to the minimal basis and stated not to survive enlargement of the one-particle space.

Records: `claim:opus5-v11-2608.14798v1/A60`, `map:opus5-v11-2608.14798v1/A60`.

## A61 — H2 6-31G differences and the reversal of geometry ordering

*Supplemental Material · Prose · modality OBSERVED · coverage PARTIAL*

**What the authors said.** Repeating the analysis in the split-valence 6-31G basis (eight spin-orbitals, eight qubits, $d_n=256$), the peak in nonstabilizerness is displaced to a somewhat larger bond length, the magic no longer collapses toward the dissociation limit but remains elevated at large $R$ in the high-$\beta$ curves, and the ordering of stabilizer work between the two representative geometries $R=0.5$ and $R=6$ bohr is inverted relative to STO-3G.

**System.** $\mathrm{H}_2$ ground state in the 6-31G basis, eight qubits

**Comparison baseline.** the STO-3G results of the main text

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) 6-31G gives four spatial molecular orbitals, eight spin-orbitals and an eight-qubit problem with $d_n=256$
- `condition:source-2` (SOURCE_EXPLICIT) the same exact-diagonalization method and the same set of $\beta$ values are used

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / RESIDUAL] The peak position shifts, appreciable magic is retained at large $R$, and the ordering between $R=0.5$ and $R=6$ bohr reverses relative to the minimal basis.
  - residual: Reported from figures only; the underlying statevectors and $\mathcal{W}_\beta$ values are not published, so the reversal cannot be checked. Retained as an observation.
- `component:limitation-1` [LIMITATION / RESIDUAL] Because neither basis approaches the complete-basis-set limit, and part of the basis dependence may reflect representational choices rather than molecular physics, the paper refrains from attaching physical significance to the dissociation-limit magic.

Records: `claim:opus5-v11-2608.14798v1/A61`, `map:opus5-v11-2608.14798v1/A61`.

## A62 — Ising panel (a): small-beta expansion and extensivity in the paramagnetic phase

*Sec. VI D · Prose · modality OBSERVED · coverage COMPLETE*

**What the authors said.** For the transverse-field Ising chain $H=-g\sum_i\sigma^z_i\sigma^z_{i+1}-h\sum_i\sigma^x_i$, a small-$\beta$ expansion gives $\mathcal{Z}_\beta\simeq e^{-\beta}(4^L+\frac{\beta^2}{2}\sum_P\langle P\rangle^2+\frac{\beta^4}{24}\sum_P\langle P\rangle^4+\cdots)$, where the $O(\beta^2)$ term is fixed by purity, $\sum_P\langle P\rangle^2=2^L$, and carries no magic, the first genuinely nonstabilizer information appearing at $O(\beta^4)$ through the participation entering $M_2$. In the paramagnetic phase ($h/g\approx1.5$) the stabilizer work rises monotonically and saturates, with a clean ordering in system size indicating extensivity, the apparent increment close to $1$ per site, which is said to be suggestive of a saturation of order $L-1$ since a pure stabilizer state has $2^L$ unit-weight Paulis.

**System.** ground states of the one-dimensional transverse-field quantum Ising chain of $L$ spins

**Comparison baseline.** $M_\alpha$ for $\alpha=2,5,10$ across the same transition

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) one-dimensional chain with critical point at $h=g$
- `condition:source-2` (SOURCE_EXPLICIT) the extensivity observation is made in the paramagnetic phase at $h/g\approx1.5$
- `condition:source-3` (SOURCE_EXPLICIT) $\beta$ is to be read as a moment-resolving parameter, not a physical temperature

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] The $O(\beta^2)$ term of the small-$\beta$ expansion is purity-fixed and magic-free; the first nonstabilizer information enters at $O(\beta^4)$.
  - target (ASYMPTOTIC): $\mathcal{Z}_{\beta}\simeq e^{-\beta}\left(4^{L}+\tfrac{\beta^{2}}{2}\sum_{P}\langle P\rangle^{2}+\tfrac{\beta^{4}}{24}\sum_{P}\langle P\rangle^{4}+\cdots\right)$ with $\sum_{P}\langle P\rangle^{2}=2^{L}$ state-independent, so the leading state-dependent term is $\tfrac{\beta^{4}}{24}\sum_{P}\langle P\rangle^{4}$.
    - quantifier: FORALL L in positive integers, system size
    - quantifier: FORALL \psi in pure states of the $L$-spin chain
    - assumption (SOURCE_EXPLICIT): purity fixes $\sum_P\langle P\rangle^2=2^L$ for pure states
    - assumption (SOURCE_RECONSTRUCTED): the expansion is the Taylor series of the SPF in $\beta$ about $0$
    - normalization: $\beta^4/24=\beta^4/4!$, consistent with the general $\beta^{2k}/(2k)!$ coefficient
    - approximation error: truncated Taylor expansion about $\beta=0$ with the remainder written as an ellipsis; no bound is given
- `component:evidence-1` [EVIDENCE / MAPPED] In the paramagnetic phase the stabilizer work rises monotonically in $\beta$ and saturates, with a clean ordering in $L$ indicating extensivity and an apparent increment close to $1$ per site.
  - residual: Explicitly hedged in the source ('suggestive'); read off plots at small $L$ with no scaling collapse or fit, and the saturation value $L-1$ is a heuristic rather than a derived result.
- `component:limitation-1` [LIMITATION / MAPPED] The prose introduces the coupling as $J$ while the Hamiltonian and all plots use $g$.
  - residual: Notation slip; only $g$ is used thereafter, so the intended reading is unambiguous.

**Semantic context.** one-dimensional transverse-field Ising chain, $H=-g\sum_i\sigma^z_i\sigma^z_{i+1}-h\sum_i\sigma^x_i$, finite $L$, ground state

- approximation regime: finite-size chains at selected $\beta$; no finite-size scaling analysis is performed
- error bound: none reported
- limitation: Extensivity at increment $1$ per site is read off plots at small $L$ with no scaling collapse or fit, and is hedged as 'suggestive' in the source.
- limitation: No solver, boundary conditions, chain lengths beyond the figures, or numerical tolerances are given, so nothing is reproducible from the paper alone.
- limitation: $\beta$ must not be read as a physical temperature, which the source states explicitly.

**Open items recorded against this claim.**

- [DATA_MISSING · computational_reproducibility] No solver, boundary conditions, system sizes beyond the figures or numerical data are given for the Ising results.
  - what could settle it: Author-supplied code or data tables; recomputation was not attempted in this run.
- [SCOPE_AMBIGUITY · source_fidelity] The Ising coupling is introduced as $J$ in prose but written $g$ in the Hamiltonian and figures.
  - what could settle it: Errata; unambiguous from context.

Records: `claim:opus5-v11-2608.14798v1/A62`, `map:opus5-v11-2608.14798v1/A62`.

## A63 — Ising panel (b): stabilizer endpoints and the interior ridge

*Sec. VI D · Prose · modality OBSERVED · coverage PARTIAL*

**What the authors said.** Over the full $(h/g,\beta)$ landscape the two field extremes are exactly solvable stabilizer points, the ferromagnetic GHZ state at $h/g\to0$ and $\ket{+}^{\otimes L}$ at $h/g\to\infty$, both of vanishing magic, so the surface should approach its stabilizer floor along the $h/g\to0$ edge for every $\beta$ and decrease again as $h/g$ grows beyond the plotted window; the visible ridge of nonstabilizerness lies in the interior near the order-disorder critical point at $h/g=1$, and the same growth-and-saturation pattern along $\beta$ persists at every field, so the ridge is stated to be a robust feature of the converged free energy rather than an artifact of a particular moment.

**System.** transverse-field Ising ground states over the $(h/g,\beta)$ plane, $L=8$ in the displayed surface

**Comparison baseline.** the two exactly solvable stabilizer endpoints

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) panel (b) uses $L=8$
- `condition:source-2` (SOURCE_EXPLICIT) the endpoint states are identified as GHZ at $h/g\to0$ and $\ket{+}^{\otimes L}$ at $h/g\to\infty$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Both field extremes are stabilizer states of vanishing magic, so the stabilizer-work surface attains its floor there for every $\beta$.
  - target (EXACT): $\mathcal{W}_{\beta}=0$ for every $\beta\neq0$ at the two exactly solvable endpoints, since the GHZ state and $\ket{+}^{\otimes L}$ are pure stabilizer states and $\mathcal{W}_\beta$ is faithful.
    - quantifier: FORALL \beta in $\mathbb{R}\setminus\{0\}$
    - quantifier: FORALL L in positive integers
    - assumption (SOURCE_EXPLICIT): the $h/g\to0$ ground state is the ferromagnetic GHZ state and the $h/g\to\infty$ ground state is $\ket{+}^{\otimes L}$
    - assumption (SOURCE_EXPLICIT): faithfulness of the stabilizer work
    - assumption (SOURCE_RECONSTRUCTED): GHZ and $\ket{+}^{\otimes L}$ are pure stabilizer states
    - normalization: the endpoints are limits, so the statement is about the limiting states rather than about any finite $h/g$ in the plotted window
  - residual: The endpoints are limiting states outside the plotted window; the source presents the floor as a consistency check it expects rather than as measured data.
- `component:evidence-1` [EVIDENCE / RESIDUAL] The ridge of nonstabilizerness lies in the interior near $h/g=1$ and persists along $\beta$, which is read as robustness of the converged free energy rather than an artifact of one moment.
  - residual: Figure-level reading at a single system size. The additional expectation that the surface 'should decrease again as $h/g$ grows beyond the window shown' concerns data outside the plotted range and is not shown.

Records: `claim:opus5-v11-2608.14798v1/A63`, `map:opus5-v11-2608.14798v1/A63`.

## A64 — Mixed-state SPF by convex roof and its faithfulness

*Sec. VII · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The SPF is extended to mixed states by $\mathcal{Z}_\beta(\rho):=\sup\sum_ip_i\mathcal{Z}_\beta(\phi_i)$ over all pure-state decompositions. The supremum is said to be the natural choice because the SPF is maximized by stabilizer states, so that the roof extension preserves faithfulness: $\mathcal{Z}_\beta(\rho)$ attains the stabilizer value if and only if $\rho$ admits a decomposition into pure stabilizer states, that is if and only if $\rho$ is a free state.

**System.** mixed states $\rho\in\mathcal{D}(\mathcal{H}_A)$

**Comparison baseline.** the pure-state SPF of Secs. III to VI

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the extension uses a SUPREMUM, justified by the SPF being maximized on stabilizer states
- `condition:source-2` (SOURCE_EXPLICIT) free states are the convex hull of pure stabilizer states

**Components, their accounting and their targets.**

- `component:definition-1` [DEFINITION / RESIDUAL] $\mathcal{Z}_\beta(\rho):=\sup\sum_{i\in[m]}p_i\mathcal{Z}_\beta(\phi_i)$ over pure-state decompositions $\{p_i,\phi_i\}$.
  - residual: Registered as a Definition record. Whether the supremum is attained, and whether $m$ is bounded, is not discussed.
- `component:conclusion-1` [CONCLUSION / MAPPED] The roof extension attains the stabilizer value exactly on the free states.
  - target (EXACT): $\mathcal{Z}_{\beta}(\rho)=\mathcal{Z}_{\beta}({\rm STAB})\iff\rho$ admits a decomposition into pure stabilizer states, i.e. iff $\rho$ is free.
    - quantifier: FORALL \rho in $\mathcal{D}(\mathcal{H}_A)$
    - quantifier: FORALL \beta in range not stated in the source
    - assumption (SOURCE_EXPLICIT): the pure-state SPF is maximized exactly on ${\rm STAB}$, from the state-independent bounds
    - assumption (SOURCE_RECONSTRUCTED): the supremum over decompositions is attained or approached by stabilizer decompositions when $\rho$ is free
    - normalization: the argument is run at a single $\beta$; whether it is uniform in $\beta$, and whether the supremum is attained, is not addressed
  - residual: Attainment of the supremum and uniformity in $\beta$ are not discussed, and the 'only if' direction requires strictness of the pure-state maximization off the free set, which is inherited from the state-independent bounds rather than proved here.

Records: `claim:opus5-v11-2608.14798v1/A64`, `map:opus5-v11-2608.14798v1/A64`.

## A65 — SPF of a quantum channel via the Choi matrix; monotonicity left open

*Sec. VII · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The SPF is generalized to CPTP maps via the Choi-Jamiolkowski isomorphism, $\mathcal{Z}_\beta(\mathcal{N}):=\mathcal{Z}_\beta(\tilde J^{\mathcal{N}}_{AB})$ with $\tilde J^{\mathcal{N}}_{AB}=({\rm id}\otimes\mathcal{N})(\Phi^+_{AA'})$ the normalized Choi matrix and $|A|=|A'|$. This is said to assign a partition function, and thereby a stabilizer work, to operations themselves, opening the door to a thermodynamic accounting of the magic generated or consumed by non-Clifford gates; establishing the monotonicity properties of these extended quantities is explicitly left for future work.

**System.** CPTP maps $\mathcal{N}\in{\rm CPTP}(A\to B)$

**Comparison baseline.** the state-level SPF and stabilizer work

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the normalized Choi matrix is used, with $|A|=|A'|$
- `condition:source-2` (SOURCE_EXPLICIT) monotonicity of the resulting channel stabilizer work is explicitly left open

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Via the Choi-Jamiolkowski isomorphism a partition function, and thereby a stabilizer work, is assigned to quantum operations themselves, opening a thermodynamic accounting of the magic generated or consumed by non-Clifford gates.
  - target (EXACT): $\mathcal{Z}_{\beta}(\mathcal{N}):=\mathcal{Z}_{\beta}(\tilde J^{\mathcal{N}}_{AB})$ is a well-defined real function on ${\rm CPTP}(A\to B)$ for every $\beta$, since $\tilde J^{\mathcal{N}}_{AB}$ is a normalized state on $AB$ and $\mathcal{Z}_\beta$ is defined on states.
    - quantifier: FORALL \mathcal{N} in ${\rm CPTP}(A\to B)$
    - quantifier: FORALL \beta in range not stated in the source
    - assumption (SOURCE_EXPLICIT): $\tilde J^{\mathcal{N}}_{AB}=({\rm id}\otimes\mathcal{N})(\Phi^+_{AA'})$ is the NORMALIZED Choi matrix with $|A|=|A'|$
    - assumption (SOURCE_EXPLICIT): the mixed-state SPF is available by convex roof, since the Choi state is in general mixed
    - normalization: well-definedness is all that is claimed here; no invariance, faithfulness or composition property is asserted, and monotonicity is explicitly deferred
  - residual: Only well-definedness follows from the stated construction. The further reading, that this opens a thermodynamic accounting of magic generated or consumed by gates, is a programmatic statement with no accompanying result.
- `component:definition-1` [DEFINITION / RESIDUAL] $\mathcal{Z}_\beta(\mathcal{N}):=\mathcal{Z}_\beta(\tilde J^{\mathcal{N}}_{AB})$ with $\tilde J^{\mathcal{N}}_{AB}=({\rm id}\otimes\mathcal{N})(\Phi^+_{AA'})$.
  - residual: Registered as a Definition record. No properties of the channel SPF (invariance, faithfulness, behaviour under composition) are established.
- `component:limitation-1` [LIMITATION / RESIDUAL] Establishing monotonicity of the extended stabilizer work for mixed states and channels is left for future work.
  - residual: An explicit deferral by the authors; no partial result is offered.

**Open items recorded against this claim.**

- [UNPROVED_LEMMA · mathematical_correctness] Monotonicity of the mixed-state and channel stabilizer work is stated as open by the authors themselves.
  - what could settle it: Future work identified in the source; the PTM characterization is proposed there as a starting point.

Records: `claim:opus5-v11-2608.14798v1/A65`, `map:opus5-v11-2608.14798v1/A65`.

## A66 — Pauli-transfer-matrix characterization of the free operations

*Sec. VII · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** A channel $\mathcal{O}\in\mathcal{F}$ is stated to satisfy: (1) $\mathcal{O}\in{\rm CPTP}(A\to B)$; (2) $\tr[\mathcal{O}(\phi_A)P]\in\{0,\pm1\}$ for all $P\in\mathcal{P}_B$ and all $\phi_A\in{\rm STAB}_A$; (3) $\sum_{P\in\mathcal{P}_B}|\tr[\mathcal{O}(\phi_A)P]|=d_B$ for all $\phi_A\in{\rm STAB}_A$. Recast through the Pauli transfer matrix $(R^{\mathcal{O}}_{AB})_{i,j}=\frac{1}{d_A}\tr[P^B_i\mathcal{O}(P^A_j)]$, the conditions become constraints on $\sum_j(R^{\mathcal{O}}_{AB})_{i,j}p^\phi_j$, and the paper concludes that the PTM of a deterministic pure-state stabilizer protocol takes one of three forms: a signed permutation matrix (Clifford unitary), a matrix whose first column is the Pauli spectrum of a pure stabilizer state with all other entries zero (discard-and-prepare), or a matrix with exactly one $1$ in every column (subsystem discard). Approximate, $\epsilon$-close PTM conditions are proposed as a route to bounding the magic injected by imperfect Clifford hardware, tied to gate-set tomography.

**System.** CPTP maps between multi-qubit systems, restricted to the free class $\mathcal{F}$

**Comparison baseline.** the definition of $\mathcal{F}$ by what its elements do to stabilizer states

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the conditions are imposed on stabilizer inputs only
- `condition:source-2` (SOURCE_EXPLICIT) $p^\phi_j=\tr[\phi P_j]$ is the Pauli spectrum of the stabilizer input
- `condition:source-3` (SOURCE_EXPLICIT) the PTM is normalized by $1/d_A$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] PTM form of the two defining conditions for $\mathcal{O}\in\mathcal{F}$.
  - target (EXACT): For $\mathcal{O}\in\mathcal{F}$ and every $\phi\in{\rm STAB}_A$ with $p^{\phi}_{j}=\tr[\phi P_{j}]$: $\sum_{j}(R^{\mathcal{O}}_{AB})_{i,j}p^{\phi}_{j}\in\{0,\pm1\}$ for every $i$, and $\sum_{i}\left|\sum_{j}(R^{\mathcal{O}}_{AB})_{i,j}p^{\phi}_{j}\right|=d_{B}$, where $(R^{\mathcal{O}}_{AB})_{i,j}=\tfrac{1}{d_{A}}\tr[P^{B}_{i}\mathcal{O}(P^{A}_{j})]$.
    - quantifier: FORALL \mathcal{O} in $\mathcal{F}$
    - quantifier: FORALL \phi in ${\rm STAB}_A$
    - quantifier: FORALL i in Pauli indices of $\mathcal{P}_B$
    - assumption (SOURCE_EXPLICIT): $\mathcal{O}\in{\rm CPTP}(A\to B)$
    - assumption (SOURCE_RECONSTRUCTED): the output of $\mathcal{O}$ on a stabilizer input is again a pure stabilizer state
    - normalization: the conditions constrain only the action on stabilizer inputs; nothing is asserted about general inputs
- `component:conclusion-2` [CONCLUSION / UNRESOLVED] The PTM of a deterministic pure-state stabilizer protocol takes one of exactly three forms: signed permutation, discard-and-prepare, or one $1$ per column.
  - residual: Not represented: stated as a classification without proof, asserted to follow from the two displayed conditions. R6 requires the conclusion to be determinable; a three-way classification asserted without argument is not, so it is kept as an open item rather than mathematized.
- `component:model-1` [MODEL / RESIDUAL] Approximate, $\epsilon$-close PTM conditions are proposed as a route to bounding the magic injected by imperfect Clifford hardware, using the PTM as reconstructed by gate-set tomography.
  - residual: A proposed direction, not a result; no $\epsilon$-robustness bound is derived.

**Open items recorded against this claim.**

- [UNPROVED_LEMMA · mathematical_correctness] The three-form PTM classification of $\mathcal{F}$ is asserted without proof.
  - what could settle it: A proof, or restriction of the claim to the exhibited examples.

Records: `claim:opus5-v11-2608.14798v1/A66`, `map:opus5-v11-2608.14798v1/A66`.

## A67 — The three stated open problems

*Sec. VII · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The paper closes with three open problems: first, with the mixed-state and channel SPF now defined, establishing monotonicity of the corresponding stabilizer work under the appropriate free operations, for which the PTM characterization is offered as a starting point; second, determining what the extra information in the stabilizer work, beyond any single SRE, reveals about physical systems where SREs have already been applied; third, whether the full profile $\beta\mapsto\mathcal{W}_\beta$ characterizes convertibility in an asymptotic or catalytic setting, given that the interconversion bounds are necessary but not sufficient and the $H$-to-$T$ example shows that curve dominance at every temperature does not guarantee convertibility.

**System.** the framework as a whole

**Comparison baseline.** the results established in the paper

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the third problem is framed against the necessary-but-not-sufficient status of the interconversion bounds

**Components, their accounting and their targets.**

- `component:limitation-1` [LIMITATION / RESIDUAL] Monotonicity for the mixed-state and channel extensions is outstanding.
- `component:limitation-2` [LIMITATION / RESIDUAL] The physical content of the extra information carried by the stabilizer work beyond a single SRE remains to be determined; the Ising and molecular results are described as first steps.
- `component:limitation-3` [LIMITATION / RESIDUAL] Whether the full temperature profile characterizes convertibility in an asymptotic or catalytic setting is open.
- `component:conclusion-1` [CONCLUSION / RESIDUAL] The authors state that curve dominance at every temperature does not guarantee convertibility, so the interconversion conditions are necessary but not sufficient.
  - residual: A restatement of the $\ket{H}\to\ket{T}$ discussion; its evidence is the unsupported claim that no deterministic stabilizer protocol realizes that conversion, recorded against the incomparability assertion.

Records: `claim:opus5-v11-2608.14798v1/A67`, `map:opus5-v11-2608.14798v1/A67`.

## A68 — Framing claims of the abstract

*Abstract · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The abstract asserts as a package: magic admits a partition-function description; the stabilizer work is, for every value of its parameter, a faithful, subadditive magic monotone that remains efficiently accessible on quantum hardware and admits an operational interpretation; both the SPF and the stabilizer work are efficiently estimable via Bell sampling; exact ensemble-averaged partition functions are derived for Haar-random, $\nu$-compressible and pseudomagic states together with concentration guarantees; the stabilizer work is intrinsically moment-generating, unlike measures probing a single moment; and as the temperature is tuned from high to low it interpolates continuously between the stabilizer 2-Renyi entropy and the stabilizer nullity, revealing two previously disconnected monotones as limiting cases of a single object.

**System.** the framework as a whole

**Comparison baseline.** existing magic measures: SREs, nullity, RoM, stabilizer extent, T-count

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the whole development concerns pure states unless otherwise stated

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / UNRESOLVED] 'For every value of its parameter, the stabilizer work is a faithful, subadditive magic monotone.'
  - residual: Not represented. Monotonicity under the declared free class $\mathcal{F}$ is not established anywhere in the paper; Sec. V proves faithfulness, Clifford invariance, sub-additivity, monotonicity under discarding subsystems and stabilizer-ancilla invariance. 'For every value of its parameter' also includes $\beta=0$, where $\mathcal{W}_0$ is ill-defined.
- `component:conclusion-2` [CONCLUSION / UNRESOLVED] Both the SPF and the stabilizer work are 'efficiently estimable via Bell sampling'.
  - residual: Not represented. The in-text guarantee is an additive-error guarantee on the NORMALIZED TRUNCATED SPF above a truncation floor scaling as $2^n$, and no argument in the paper propagates it to $\mathcal{W}_\beta$.
- `component:conclusion-3` [CONCLUSION / UNRESOLVED] The full temperature profile is a magic spectrum 'carrying strictly more information than either endpoint'.
  - residual: Not represented: 'strictly more information' is not defined, and no information measure, sufficiency statement or separating example is given. Sec. III C separately states that the SPF discards the labelled spectrum, so the comparison class is the two endpoint measures, not the spectrum.
- `component:conclusion-4` [CONCLUSION / UNRESOLVED] The $\beta\to\infty$ limit is a third-law statement recovering the stabilizer nullity and the $\beta\to0$ limit recovers the stabilizer 2-Renyi entropy, so two previously disconnected monotones appear as limiting cases of one object.
  - residual: Not represented: both limits are asserted without derivation or error terms anywhere in the paper, and the underlying $\beta\to\infty$ SPF limit is itself printed inconsistently in two places.
- `component:conclusion-5` [CONCLUSION / RESIDUAL] Exact ensemble-averaged partition functions with concentration guarantees make the SPF a classifier of magic classes.
  - residual: The exact ensemble formulas and the concentration lemmas are in the text and are recorded individually; 'classifier' summarizes them together with Algorithms 1 to 3, whose decision rules branch on asymptotic predicates and assume oracle access.

**Open items recorded against this claim.**

- [ALIGNMENT_GAP · source_fidelity] Four abstract-level assertions (monotone at every parameter value; efficiently estimable; strictly more information; the two limits) are stronger than the corresponding in-text results.
  - what could settle it: Either proofs closing the gaps, or restatement of the abstract; each gap is also recorded against the section where it arises.

Records: `claim:opus5-v11-2608.14798v1/A68`, `map:opus5-v11-2608.14798v1/A68`.

## A69 — AI acknowledgement statement

*AI Acknowledgement Statement · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** The authors disclose that they used Claude to refine and sharpen the Introduction and Discussion, giving as an instance that they learnt from it about Li and Haldane's entanglement-spectrum result, which carries a similar flavour to the present work.

**System.** the manuscript's own preparation process

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the disclosed scope is refining and sharpening the Introduction and Discussion

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / RESIDUAL] The authors disclose that an AI assistant was used in preparing the manuscript, with the disclosed scope limited to refining and sharpening the Introduction and Discussion.
  - residual: A process disclosure about the manuscript rather than a claim about magic, so it is not mathematized. The scope is described qualitatively and no statement is made, or excluded, about mathematical content.
- `component:evidence-1` [EVIDENCE / RESIDUAL] The instance given is that the authors learnt from it about Li and Haldane's entanglement-spectrum result, which they say carries a similar flavour to the present work.

Records: `claim:opus5-v11-2608.14798v1/A69`, `map:opus5-v11-2608.14798v1/A69`.

## A70 — Estimator bounds for the truncated SPF

*Supplemental Material · Lemma · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Lemma (Estimator bounds): the Pauli-moments algorithm with additional classical memory learns the normalized $k$th-order truncation $d_n^{-1}\mathcal{Z}^{(k)}_\beta(\psi)$ with failure probability $\delta$ and additive error $\epsilon$ in $\mathcal{N}=\mathcal{O}(\frac{\cosh^2\beta}{e^{2\beta}\epsilon^2}\log\frac1\delta)$ samples; the proof bounds each per-sample contribution by $|X_\alpha|\leq\cosh\beta$ and applies Hoeffding's inequality.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** running the moment estimator separately per moment

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) each entry of the accumulated sign vector $\mathbf{B}_k$ is $\pm1$, giving $|X_\alpha|\leq\sum_{l=1}^{k}\beta^{2l}/(2l)!\leq\cosh\beta$
- `condition:source-2` (SOURCE_EXPLICIT) Bell sampling draws $P$ with probability $|\langle P|\eta\rangle|^2$ from $\ket{\eta}=U_{\rm Bell}^{\otimes n}\ket{\psi^*}\otimes\ket{\psi}$, and repeated eigenbasis measurements of that $P$ give unbiased even moments
- `condition:source-3` (SOURCE_EXPLICIT) the guarantee is additive on the NORMALIZED quantity $d_n^{-1}\mathcal{Z}^{(k)}_\beta$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Hoeffding concentration and the resulting sample count for the normalized truncated SPF.
  - target (EXACT): ${\rm Pr}\!\left(\left|\hat{\mathcal{Z}}^{(k)}_{\beta}(\psi)-\mathcal{Z}^{(k)}_{\beta}(\psi)\right|/d_{n}\geq\epsilon\right)\leq2\exp\!\left(-\dfrac{\mathcal{N}\epsilon^{2}e^{2\beta}}{2\cosh^{2}\beta}\right)$, hence $\mathcal{N}=\mathcal{O}\!\left(\dfrac{\cosh^{2}\beta}{e^{2\beta}\epsilon^{2}}\log\tfrac1\delta\right)$ samples suffice for failure probability $\delta$.
    - quantifier: FORALL \epsilon in $(0,\infty)$
    - quantifier: FORALL \delta in $(0,1)$
    - quantifier: FORALL k in positive integers, held fixed
    - quantifier: FORALL \beta in $\mathbb{R}$, held fixed
    - assumption (SOURCE_EXPLICIT): each entry of $\mathbf{B}_k$ is $\pm1$, so $|X_\alpha|\leq\cosh\beta$
    - assumption (SOURCE_RECONSTRUCTED): the per-sample variables are independent, as required by Hoeffding
    - assumption (SOURCE_EXPLICIT): $\hat{\mathcal{Z}}^{(k)}_\beta=e^{-\beta}(d_n^2+d_n\mathbf{c}^{(k)}(\beta)\cdot\hat{\mathbf m}_k)$
    - normalization: the error is additive on $d_n^{-1}\mathcal{Z}^{(k)}$; since $\mathcal{Z}_\beta\sim d_n^2$, a constant $\epsilon$ here is a relative error of order $2^{-n}$ on $\mathcal{Z}$ and does NOT bound $\mathcal{W}_\beta$ without further argument. Note $\cosh^2\beta/e^{2\beta}\to1/4$ as $\beta\to\infty$, so the stated count is $\beta$-independent at large $\beta$
  - residual: The stated count is for outer-loop samples; each consumes $2k+2$ copies of $\ket{\psi}$, which the complexity does not display.
- `component:model-1` [MODEL / UNRESOLVED] Algorithm 4: for each of $\mathcal{N}$ outer samples, Bell-sample a Pauli $P$, then measure $\ket{\psi}$ in the eigenbasis of $P$ for $\ell=1,\dots,2k$, accumulating the running sign product and storing it at even $\ell$.
  - residual: The index convention is not self-consistent as printed: $\mathbf{m}_k$ is defined to start at $d_n^{-1}\sum x^{2}$, while the first stored accumulator entry $\mathbf{B}^{0}_k$ at $\ell=2$ estimates $d_n^{-1}\sum x^{4}$ after averaging over the Bell-sampled $P$. Since the $k=1$ moment is purity-fixed and state-independent, the off-by-one may be immaterial, but the source does not resolve it.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · source_fidelity] Index convention of the moment vector $\mathbf{m}_k$ versus the accumulator $\mathbf{B}_k$ in Algorithm 4 appears to be off by one.
  - what could settle it: Author clarification; the purity-fixed first moment may make the discrepancy immaterial.

Records: `claim:opus5-v11-2608.14798v1/A70`, `map:opus5-v11-2608.14798v1/A70`.

## A71 — Estimator concentration for the full normalized SPF

*Supplemental Material · Theorem · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Theorem (Estimator Concentration): with $\Delta(\beta,k)=d_ne^{-\beta}(\cosh\beta-\sum_{l=0}^{k}\frac{\beta^{2l}}{(2l)!})$, for every $\epsilon>\Delta(\beta,k)$ the estimator $\hat{\mathcal{Z}}^{(k)}_\beta(\psi)$ approximates the normalized SPF $d_n^{-1}\mathcal{Z}_\beta(\psi)$ with ${\rm Pr}(\frac{\mathcal{Z}_\beta(\psi)-\hat{\mathcal{Z}}^{(k)}_\beta(\psi)}{d_n}\geq\epsilon)\leq2\exp(-\frac{\mathcal{N}(\epsilon-\Delta(\beta,k))^2e^{2\beta}}{2\cosh^2\beta})$ and therefore requires $\mathcal{N}=\mathcal{O}(\frac{\cosh^2\beta}{e^{2\beta}(\epsilon-\Delta(\beta,k))^2}\log\frac1\delta)$ samples.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the truncated-SPF guarantee of the preceding lemma

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\epsilon>\Delta(\beta,k)$ is required for the statement to be non-vacuous
- `condition:source-2` (SOURCE_EXPLICIT) the truncation remainder is non-negative and bounded using $|x|\leq1$ for every $x\in{\rm Spec}(\psi)$
- `condition:source-3` (SOURCE_RECONSTRUCTED) the guarantee is one-sided, bounding $\mathcal{Z}_\beta-\hat{\mathcal{Z}}^{(k)}_\beta$ from above only

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] One-sided concentration for the full normalized SPF above the truncation floor, with the resulting sample count.
  - target (APPROXIMATE): For $\epsilon>\Delta(\beta,k)$: ${\rm Pr}\!\left(\dfrac{\mathcal{Z}_{\beta}(\psi)-\hat{\mathcal{Z}}^{(k)}_{\beta}(\psi)}{d_{n}}\geq\epsilon\right)\leq2\exp\!\left(-\dfrac{\mathcal{N}(\epsilon-\Delta(\beta,k))^{2}e^{2\beta}}{2\cosh^{2}\beta}\right)$, with $\Delta(\beta,k)=d_{n}e^{-\beta}\left(\cosh\beta-\sum_{l=0}^{k}\tfrac{\beta^{2l}}{(2l)!}\right)$.
    - quantifier: FORALL \epsilon in reals with $\epsilon>\Delta(\beta,k)$
    - quantifier: FORALL k in positive integers
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_EXPLICIT): $\Delta\mathcal{Z}^{(k)}_\beta(\psi)/d_n\leq\Delta(\beta,k)$, using $|x|\leq1$ on the $d_n^2$ spectrum entries
    - assumption (SOURCE_EXPLICIT): Lemma (Estimator bounds) applied with error threshold $\epsilon-\Delta(\beta,k)>0$
    - normalization: the guarantee is additive on $d_n^{-1}\mathcal{Z}$ and one-sided. Because $\Delta(\beta,k)$ scales with $d_n=2^n$, the condition $\epsilon>\Delta(\beta,k)$ forces the bracket to be smaller than $\epsilon\,2^{-n}$, i.e. $k$ must grow with $n$; the source states no resulting $k(n,\beta,\epsilon)$
    - approximation error: truncation floor $\Delta(\beta,k)=d_ne^{-\beta}(\cosh\beta-\sum_{l=0}^{k}\beta^{2l}/(2l)!)$, which carries a factor $2^n$; the guarantee is vacuous unless $k$ is chosen large enough that $\Delta(\beta,k)<\epsilon$, and no such $k$ is exhibited
  - residual: The bound is one-sided; the source presents it as an approximation guarantee without noting that the reverse deviation is not controlled.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · mathematical_correctness] The truncation floor $\Delta(\beta,k)$ scales with $2^n$, so the stated guarantee requires a truncation order growing with $n$, which the paper does not exhibit; the bound is also one-sided.
  - what could settle it: An explicit $k(n,\beta,\epsilon)$ and a two-sided statement.

Records: `claim:opus5-v11-2608.14798v1/A71`, `map:opus5-v11-2608.14798v1/A71`.

## A72 — Integer SREs recovered from the SPF by differentiation

*Supplemental Material · Proposition · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Proposition (Stab Renyi from SPF): for integer $\alpha$, $M_\alpha(\psi)=\lim_{\beta\to0}\frac{1}{1-\alpha}\log(\frac{1}{d_n}\frac{\partial^{2\alpha}}{\partial\beta^{2\alpha}}e^\beta\mathcal{Z}_\beta(\psi))$, because $\sum_xx^{2m}=\partial^{2m}_\beta e^\beta\mathcal{Z}_\beta(\psi)|_{\beta\to0}$; the exchange of limits is said to follow from positivity of the SPF derivatives.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the direct definition of $M_\alpha$ from the distribution $\Xi(\psi)$

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\alpha\in\mathbb{N}$
- `condition:source-2` (SOURCE_RECONSTRUCTED) $\alpha=1$ is excluded by the prefactor $1/(1-\alpha)$, which the source does not state

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Differentiation formula recovering the integer-index SREs from the SPF.
  - target (EXACT): $M_{\alpha}(\psi)=\lim_{\beta\to0}\dfrac{1}{1-\alpha}\log\!\left(\dfrac{1}{d_{n}}\dfrac{\partial^{2\alpha}}{\partial\beta^{2\alpha}}e^{\beta}\mathcal{Z}_{\beta}(\psi)\right)$ for $\alpha\in\mathbb{N}$, $\alpha\neq1$.
    - quantifier: FORALL \alpha in $\mathbb{N}\setminus\{1\}$; the source writes $\mathbb{N}$ but $\alpha=1$ makes the prefactor singular
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_EXPLICIT): $e^{\beta}\mathcal{Z}_\beta(\psi)=\sum_{x\in{\rm Spec}(\psi)}\cosh(\beta x)$
    - assumption (SOURCE_EXPLICIT): $\sum_x x^{2m}=\partial^{2m}_\beta e^{\beta}\mathcal{Z}_\beta(\psi)|_{\beta\to0}$
    - assumption (SOURCE_EXPLICIT): the exchange of limit and differentiation is justified in the source by positivity of the SPF derivatives
    - normalization: $\log$ base 2 as elsewhere; the $1/d_n$ inside the logarithm matches the normalization in the definition of $M_\alpha$
  - residual: The admissible integer range is printed as $\mathbb{N}$ although $\alpha=1$ is singular.
- `component:assumption-1` [ASSUMPTION / UNRESOLVED] 'The exchange of the limits comes because the positivity of the SPF derivatives.'
  - residual: Positivity of derivatives does not by itself justify exchanging a limit with differentiation; the available justification is that $\sum_x\cosh(\beta x)$ is entire, which the source does not state.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · mathematical_correctness] The limit-exchange justification offered is positivity of derivatives, which does not establish the exchange.
  - what could settle it: Replace with the analyticity argument, or state the interchange theorem used.

Records: `claim:opus5-v11-2608.14798v1/A72`, `map:opus5-v11-2608.14798v1/A72`.

## A73 — Pauli spectrum density and its relation to the SPF

*Supplemental Material · Prose · modality ASSERTED · coverage PARTIAL*

**What the authors said.** A Pauli spectrum density is introduced as the point measure $d\mu_{\ket{\psi}}(x)=\frac{1}{d_n^2}\sum_{x_l\in{\rm Spec}(\psi)}\delta(x-x_l)dx$ on $x\in[-1,1]$, related to $\Xi$ at the moment level by $\mathbb{E}_{x\sim\Xi}[x^{2\alpha}]=d_n\mathbb{E}_{x\sim d\mu_\psi}[x^{2(\alpha+1)}]$, and giving $\mathcal{Z}_\beta(\psi)=e^{-\beta}d_n^2\mathbb{E}_{x\sim d\mu_\psi}[\cosh(\beta x)]$. The individual density is a non-smooth sequence of delta peaks that becomes smooth after ensemble averaging, as for Haar states, whose averaged density is quoted from Turkeshi et al. Both objects are said to carry the same information about the spectrum statistics, with the SPF preferred because it is smooth for all states and has simple closed forms across state classes.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the purity-weighted distribution $\Xi(\psi)$ used to define the SREs

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the density is a point measure with uniform weight $1/d_n^2$ per spectrum entry
- `condition:source-2` (SOURCE_EXPLICIT) the smoothing statement applies to ensemble averages, not to individual states

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{Z}_\beta(\psi)=e^{-\beta}d_n^2\mathbb{E}_{x\sim d\mu_\psi}[\cosh(\beta x)]$ and $\mathbb{E}_{x\sim\Xi}[x^{2\alpha}]=d_n\mathbb{E}_{x\sim d\mu_\psi}[x^{2(\alpha+1)}]$.
  - target (EXACT): $\mathcal{Z}_{\beta}(\psi)=e^{-\beta}d_{n}^{2}\,\mathbb{E}_{x\sim d\mu_{\psi}}[\cosh(\beta x)]$, and $\mathbb{E}_{x\sim\Xi(\psi)}[x^{2\alpha}]=d_{n}\,\mathbb{E}_{x\sim d\mu_{\psi}}[x^{2(\alpha+1)}]$.
    - quantifier: FORALL \beta in $\mathbb{R}$
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - quantifier: FORALL \alpha in positive integers in the moment identity
    - assumption (SOURCE_EXPLICIT): $d\mu_{\ket{\psi}}(x)=\frac{1}{d_n^2}\sum_{x_l\in{\rm Spec}(\psi)}\delta(x-x_l)dx$
    - assumption (SOURCE_EXPLICIT): $\Xi(\psi)=\{x^2/d_n:x\in{\rm Spec}(\psi)\}$
    - normalization: the two measures differ by the purity reweighting, which is what produces the index shift $\alpha\mapsto\alpha+1$ and the factor $d_n$
- `component:attribution-1` [ATTRIBUTION / NON_CLAIM] The averaged Haar density is quoted as $d\mu_{{\rm Haar}_n}[x]=\frac{1}{d_n^2}\delta(x-1)+\frac{d_n^2-1}{d_n^2}\frac{(1-x^2)^{d_n/2-1}\Gamma(\frac{d_n+1}{2})}{\sqrt\pi\,\Gamma(\frac{d_n}{2})}$, attributed to Turkeshi et al.
  - residual: Imported result underlying the Haar SPF theorem; no cited work was retrieved in this run.
- `component:conclusion-2` [CONCLUSION / UNRESOLVED] The SPF and the density carry the same information about the spectrum statistics, each determinable from the other by an inverse transform, but the SPF is smooth for all states and has simple closed forms across classes.
  - residual: 'Same information content' is asserted without specifying the transform or its domain of validity; it also sits in tension with the Sec. III C statement that the SPF discards labels and signs, which the density likewise discards, so the comparison is between two label-free objects. Not represented.

Records: `claim:opus5-v11-2608.14798v1/A73`, `map:opus5-v11-2608.14798v1/A73`.

## A74 — Monotone reweighting (Chebyshev-type) lemma

*Supplemental Material · Lemma · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Lemma: if $a_0\geq a_1\geq\cdots\geq0$ and two probability distributions $p,q$ are such that $p_k/q_k$ is non-increasing in $k$, then $\sum_kp_ka_k\geq\sum_kq_ka_k$. The proof uses that $p_k-q_k$ changes sign once, at some $k_0$, and that $\sum_k(p_k-q_k)=0$.

**System.** sequences and probability distributions on the non-negative integers

**Comparison baseline.** the unweighted comparison of the two sums

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $a_0\geq a_1\geq\cdots\geq0$ is non-increasing and non-negative
- `condition:source-2` (SOURCE_EXPLICIT) $p,q$ are probability distributions with $p_k/q_k$ non-increasing in $k$
- `condition:source-3` (SOURCE_EXPLICIT) the proof asserts a single sign change of $p_k-q_k$ at some $k_0$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\sum_kp_ka_k\geq\sum_kq_ka_k$ under the stated monotonicity hypotheses.
  - target (EXACT): If $a_{0}\geq a_{1}\geq\cdots\geq0$ and $p,q$ are probability distributions with $k\mapsto p_{k}/q_{k}$ non-increasing, then $\sum_{k}p_{k}a_{k}\geq\sum_{k}q_{k}a_{k}$.
    - quantifier: FORALL a in non-increasing non-negative sequences
    - quantifier: FORALL p,q in probability distributions on the non-negative integers with $p_k/q_k$ non-increasing
    - assumption (SOURCE_EXPLICIT): $\sum_k(p_k-q_k)=0$
    - assumption (SOURCE_EXPLICIT): $p_k-q_k$ has a single sign change, at some index $k_0$
    - assumption (SOURCE_EXPLICIT): $p_k-q_k$ has the same sign as $a_k-a_{k_0}$
    - normalization: the sums are over all $k$; convergence is not discussed, and $q_k>0$ is needed for the ratio hypothesis to be defined

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · mathematical_correctness] The lemma's proof asserts the sign pattern and the same-sign relation between $p_k-q_k$ and $a_k-a_{k_0}$ without deriving either, and does not address convergence or the requirement $q_k>0$.
  - what could settle it: A complete proof; the statement is a standard rearrangement-type inequality but the printed argument is incomplete.

Records: `claim:opus5-v11-2608.14798v1/A74`, `map:opus5-v11-2608.14798v1/A74`.

## A75 — Canonical Splitting of the Pauli spectrum

*Supplemental Material · Proposition · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Proposition (Canonical Splitting): the Pauli spectrum of any state can always be brought into the form ${\rm Spec}(\ket{\psi})={\rm Spec}_S(\ket{\psi})\cup{\rm Spec}_R(\ket{\psi})$, with ${\rm Spec}_S(\ket{\psi})=\{\tr(\psi P)\ \text{for all}\ P\ket{\psi}=\pm\ket{\psi}\}$ the stabilizer part and ${\rm Spec}_R(\ket{\psi})={\rm Spec}(\ket{\psi})\setminus{\rm Spec}_S(\ket{\psi})$ the residual contribution, containing all Paulis with $|\tr(P\psi)|$ strictly less than one.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the support/kernel splitting of Sec. II B, which partitions by vanishing versus non-vanishing expectation

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\ket{\psi}$ is a pure state
- `condition:source-2` (SOURCE_EXPLICIT) ${\rm Spec}_S$ collects the entries of Paulis satisfying $P\ket{\psi}=\pm\ket{\psi}$, i.e. signed stabilizers

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Every pure state admits the canonical splitting of its Pauli spectrum into a stabilizer part and a residual part.
  - target (EXACT): ${\rm Spec}(\ket{\psi})={\rm Spec}_{S}(\ket{\psi})\ \cup\ {\rm Spec}_{R}(\ket{\psi})$ with ${\rm Spec}_{S}(\ket{\psi})=\{\tr(\psi P):P\ket{\psi}=\pm\ket{\psi}\}$, ${\rm Spec}_{R}(\ket{\psi})={\rm Spec}(\ket{\psi})\setminus{\rm Spec}_{S}(\ket{\psi})=\{x\in{\rm Spec}(\ket{\psi}):|x|<1\}$, and $|{\rm Spec}_{S}(\ket{\psi})|=|{\rm Stab}(\psi)|=2^{\,n-\nu(\psi)}$.
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_EXPLICIT): $|\tr(P\psi)|=1$ exactly for signed stabilizers of $\psi$
    - assumption (SOURCE_EXPLICIT): the cardinality of the stabilizer part is $2^{n-\nu}$, as used in the separable-state proof
    - normalization: this is the definition of the symbol ${\rm Spec}_R$ used by Theorem (SPF for separable states) in the main text, where it appears without definition

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · source_fidelity] ${\rm Spec}_R$ is used in the main-text separable-state theorem but defined only here in the supplemental material, with no forward reference at the point of use.
  - what could settle it: A cross-reference from the main text to this proposition.

Records: `claim:opus5-v11-2608.14798v1/A75`, `map:opus5-v11-2608.14798v1/A75`.

## A76 — Variational SPF lower bound

*Supplemental Material · Lemma · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Lemma (variational SPF lower bound): for $\beta\geq0$ and a control parameter $\epsilon\in[0,1)$, $\mathcal{Z}_\beta(\psi)\geq e^{-\beta}d_n^2[\cosh(|\beta|\epsilon)(1-f_\epsilon(\psi))+f_\epsilon(\psi)]$ with $f_\epsilon(\psi)=\frac{1-2^{-M_2(\psi)-n}}{1-\epsilon^4}$, and the tightest such bound is obtained by taking the supremum over $\epsilon\in[0,1)$.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the state-dependent lower bound of the main text, which this refines

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\beta\geq0$ and $\epsilon\in[0,1)$
- `condition:source-2` (SOURCE_EXPLICIT) the proof works with the Pauli-spectrum density $d\mu_{\ket{\psi}}$ and splits the spectrum at $|x|=\epsilon$
- `condition:source-3` (SOURCE_EXPLICIT) the proof uses $\cosh(\beta x)\geq\cosh(|\beta|\epsilon)\mathbf{1}_{\{|x|\geq\epsilon\}}+\mathbf{1}_{\{|x|<\epsilon\}}$ and a Chernoff-type bound on $p_\epsilon(\psi)={\rm Prob}(|x|<\epsilon)$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] A one-parameter family of SPF lower bounds, optimized over the control parameter.
  - target (EXACT): $\mathcal{Z}_{\beta}(\psi)\ \geq\ \sup_{\epsilon\in[0,1)}\,e^{-\beta}d_{n}^{2}\left[\cosh(|\beta|\epsilon)\left(1-f_{\epsilon}(\psi)\right)+f_{\epsilon}(\psi)\right]$, with $f_{\epsilon}(\psi)=\dfrac{1-2^{-M_{2}(\psi)-n}}{1-\epsilon^{4}}$.
    - quantifier: FORALL \beta in $[0,\infty)$
    - quantifier: FORALL \epsilon in $[0,1)$
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_EXPLICIT): $\mathcal{Z}_\beta(\psi)=e^{-\beta}d_n^2\mathbb{E}_{x\sim d\mu_{\ket{\psi}}}[\cosh(\beta x)]$
    - assumption (SOURCE_EXPLICIT): $\cosh$ is monotone increasing on $[0,\infty)$, giving the two-region lower bound on $\cosh(\beta x)$
    - assumption (SOURCE_RECONSTRUCTED): $f_\epsilon(\psi)$ upper-bounds $p_\epsilon(\psi)={\rm Prob}_{x\sim d\mu_\psi}(|x|<\epsilon)$
    - normalization: the prefactor $e^{-\beta}d_n^2$ matches the density representation of the SPF; as $\epsilon\to1^-$ the factor $1-\epsilon^4\to0$ makes $f_\epsilon$ diverge, so the supremum is effectively over an interior range the source does not delimit
  - residual: The source does not state that $f_\epsilon(\psi)\in[0,1]$ over the whole range $\epsilon\in[0,1)$, yet the bound is only meaningful there; as $\epsilon\to1^-$, $f_\epsilon$ diverges.

**Open items recorded against this claim.**

- [SCOPE_AMBIGUITY · mathematical_correctness] The variational lower bound is stated for all $\epsilon\in[0,1)$ but $f_\epsilon(\psi)$ diverges as $\epsilon\to1^-$, so the admissible range over which the supremum is meaningful is not delimited.
  - what could settle it: An explicit admissible interval for $\epsilon$, or a statement that the bound is trivial outside it.

Records: `claim:opus5-v11-2608.14798v1/A76`, `map:opus5-v11-2608.14798v1/A76`.

## A77 — Supporting theorem for sub-additivity

*Supplemental Material · Theorem · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Theorem: let $a,b$ be non-increasing vectors with $a_0=d_A^2$, $b_0=d_B^2$, $a_1=d_A$, $b_1=d_B$, $a_{k>1}\leq a_1$, $b_{k>1}\leq b_1$, and let $w=(1,\frac{x^2}{2!},\frac{x^4}{4!},\dots)$ with $\sum_kw_k=c=\cosh x$. Then $\frac{\sum_kw_ka_kb_k}{(\sum_kw_ka_k)(\sum_kw_kb_k)}\geq\frac{d_Ad_B+c-1}{(d_A+c-1)(d_B+c-1)}$.

**System.** pairs of non-increasing non-negative sequences with fixed leading entries

**Comparison baseline.** the corresponding inequality for the core SPF, where the $k=0$ entry is removed

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $a,b$ non-increasing with the stated leading entries and $a_{k>1}\leq a_1$, $b_{k>1}\leq b_1$
- `condition:source-2` (SOURCE_EXPLICIT) $w_k=x^{2k}/(2k)!$ with $\sum_kw_k=\cosh x$, $x\in\mathbb{R}$
- `condition:source-3` (SOURCE_EXPLICIT) the proof compares against $a'=(d_A^2,d_A,d_A,\dots)$ and $b'=(d_B^2,d_B,d_B,\dots)$ using the monotone reweighting lemma

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] The stated ratio inequality with the extremal value attained at the stabilizer sequences.
  - target (EXACT): $\dfrac{\sum_{k}w_{k}a_{k}b_{k}}{\left(\sum_{k}w_{k}a_{k}\right)\left(\sum_{k}w_{k}b_{k}\right)}\ \geq\ \dfrac{d_{A}d_{B}+c-1}{(d_{A}+c-1)(d_{B}+c-1)}$, with equality at $a=a'=(d_A^2,d_A,d_A,\dots)$ and $b=b'=(d_B^2,d_B,d_B,\dots)$.
    - quantifier: FORALL a,b in non-increasing sequences with the stated leading entries and bounds
    - quantifier: FORALL x in $\mathbb{R}$, through $c=\cosh x$
    - assumption (SOURCE_EXPLICIT): $a_k/a'_k$ is non-increasing in $k$ for the comparison sequence $a'$
    - assumption (SOURCE_EXPLICIT): the monotone reweighting lemma applies to the resulting probability distributions
    - normalization: this version includes the $k=0$ term, so it is the inequality behind the UNFILTERED work rather than behind the core-SPF supermultiplicativity of the main text
  - residual: The hypothesis $a_k/a'_k$ non-increasing is asserted for the comparison sequences, as in the main-text supermultiplicativity lemma; it is the same unproved step.

**Open items recorded against this claim.**

- [UNPROVED_LEMMA · mathematical_correctness] The monotonicity hypothesis $a_k/a'_k$ non-increasing is again asserted rather than proved, so both the filtered and the unfiltered sub-additivity results rest on the same unproved step.
  - what could settle it: A proof that admissible even-moment sequences satisfy the required ratio monotonicity.

Records: `claim:opus5-v11-2608.14798v1/A77`, `map:opus5-v11-2608.14798v1/A77`.

## A78 — Unfiltered stabilizer work and its missing properties

*Supplemental Material · Theorem · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Defining an unfiltered stabilizer work $\mathfrak{u}_\beta(\psi)$ from the SPF rather than the core SPF, the paper states that faithfulness, Clifford invariance and sub-additivity carry over, proves the sub-additivity, and states that the unfiltered work lacks some desirable properties, specifically that it does not always decrease under partial trace for all values of $\beta$.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the core-SPF stabilizer work $\mathcal{W}_\beta$ of the main text

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $\mathfrak{u}_\beta$ is built from $\mathcal{Z}_\beta$ in place of $\mathcal{Z}^c_\beta$
- `condition:source-2` (SOURCE_EXPLICIT) the sub-additivity proof reduces to the supporting theorem with $w_k=\beta^{2k}/(2k)!$, $a_k=\sum_i\tr[\psi_AP_i]^{2k}$, $b_k=\sum_j\tr[\phi_BP_j]^{2k}$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] The unfiltered stabilizer work is sub-additive.
  - target (EXACT): $\mathfrak{u}_{\beta}(\psi_{A}\otimes\phi_{B})\leq\mathfrak{u}_{\beta}(\psi_{A})+\mathfrak{u}_{\beta}(\phi_{B})$, equivalently $\dfrac{\mathcal{Z}_{\beta}(\psi_A\otimes\phi_B)}{\mathcal{Z}_{\beta}(\psi_A)\mathcal{Z}_{\beta}(\phi_B)}\geq\dfrac{\mathcal{Z}_{\beta}({\rm Stab}_{AB})}{\mathcal{Z}_{\beta}({\rm Stab}_{A})\mathcal{Z}_{\beta}({\rm Stab}_{B})}$.
    - quantifier: FORALL \psi_A,\phi_B in pure states of the two subsystems
    - quantifier: FORALL \beta in range not stated
    - assumption (SOURCE_EXPLICIT): the supporting theorem for sub-additivity, applied with the even-moment sequences of the two factors
    - assumption (SOURCE_RECONSTRUCTED): $\mathcal{Z}_\beta({\rm Stab}_A)=e^{-\beta}(d_A-1+d_A\cosh\beta)d_A$ in the form used in the displayed reduction
    - normalization: the definition of $\mathfrak{u}_\beta$ is described in words but never displayed, so its exact normalization is reconstructed from the displayed ratio inequality
  - residual: The explicit definition of $\mathfrak{u}_\beta$ is never displayed; it is reconstructed from the ratio form used in the proof.
- `component:limitation-1` [LIMITATION / UNRESOLVED] The unfiltered work does not always decrease under partial trace for all values of $\beta$, which is stated as the reason the main text uses the core SPF instead.
  - residual: Asserted without a counterexample or a range of $\beta$ where the failure occurs. This is the substantive justification for the core-SPF choice, so the absence of a witness matters.

**Open items recorded against this claim.**

- [MISSING_SOURCE · mathematical_correctness] The failure of monotonicity under partial trace for the unfiltered work is asserted without a counterexample or a specified range of $\beta$, although it is the stated reason for preferring the core SPF.
  - what could settle it: An explicit counterexample state pair and $\beta$ value.

Records: `claim:opus5-v11-2608.14798v1/A78`, `map:opus5-v11-2608.14798v1/A78`.

## A79 — Connected correlations of a uniformly random function

*Supplemental Material · Lemma · modality ASSERTED · coverage COMPLETE*

**What the authors said.** Lemma (connected correlations): for a uniformly random $f:\{0,1\}^n\to\{0,1\}$ and $r_s(x)=(-1)^{f(x)+f(x\oplus s)}$ with fixed $s\neq0^n$, the $2k$-th order cumulant factorizes as $\mathbb{E}_f[r_s(x_1)\cdots r_s(x_{2k})]_c=\kappa_{2k}\prod_{j=2}^{2k}\Delta_s(x_1,x_j)$, where $\Delta_s(x,y)=\delta_{x,y}+\delta_{x\oplus s,y}$ and $\kappa_{2k}$ is the $2k$-th cumulant of a Rademacher variable.

**System.** uniformly random Boolean functions on $\{0,1\}^n$, as used for subset phase states

**Comparison baseline.** the pseudorandom function actually used in the SPS construction

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $f$ is a UNIFORMLY RANDOM function, not merely pseudorandom
- `condition:source-2` (SOURCE_EXPLICIT) $s\neq0^n$ is a fixed bit-string
- `condition:source-3` (SOURCE_EXPLICIT) $\kappa_{2k}=\frac{d^{2k}}{dt^{2k}}\log\cosh t|_{t=0}$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Cumulant factorization for the sign variables of a uniformly random Boolean function.
  - target (EXACT): $\mathbb{E}_{f}\left[r_{s}(x_{1})r_{s}(x_{2})\cdots r_{s}(x_{2k})\right]_{c}=\kappa_{2k}\prod_{j=2}^{2k}\Delta_{s}(x_{1},x_{j})$ with $\Delta_{s}(x,y)=\delta_{x,y}+\delta_{x\oplus s,y}$.
    - quantifier: FORALL k in positive integers
    - quantifier: FORALL s in $\{0,1\}^n\setminus\{0^n\}$, fixed
    - quantifier: FORALL x_1,\dots,x_{2k} in $\{0,1\}^n$
    - assumption (SOURCE_EXPLICIT): $f$ is uniformly random on $\{0,1\}^n\to\{0,1\}$
    - assumption (SOURCE_EXPLICIT): $r_s(x)=(-1)^{f(x)+f(x\oplus s)}$ is a Rademacher variable for each $x$
    - normalization: the subscript $c$ denotes the connected (cumulant) part; the identity is for the uniformly random function, whereas the SPS construction uses a pseudorandom $f$
  - residual: The lemma is for a uniformly random $f$, while the SPS family is defined with a pseudorandom $f$. The step from one to the other is the cryptographic assumption of the construction and is not made explicit at this point.

**Open items recorded against this claim.**

- [MODEL_ASSUMPTION · mathematical_correctness] The moment computation behind the SPS theorem is carried out for a uniformly random Boolean function, while the SPS family is defined with a pseudorandom function; the replacement is not justified in the text.
  - what could settle it: An explicit statement of the indistinguishability step, or of which quantities are computed for the uniform case only.

Records: `claim:opus5-v11-2608.14798v1/A79`, `map:opus5-v11-2608.14798v1/A79`.

## A80 — Average SPS Pauli-spectrum density and its non-smoothness

*Supplemental Material · Corollary · modality ASSERTED · coverage PARTIAL*

**What the authors said.** Corollary (Average SPS Pauli-spectrum density): for $\ket{\psi}\sim{\rm SPS}$ with $|S|=2^n$, the normalized average Pauli-spectrum density is $d\mu_{\rm SPS}(x)=\frac{1}{d_n^2}\delta(x-1)+\frac{(d_n-1)(1+d_n/2)}{d_n^2}\delta(x)+\frac{(d_n-1)d_n}{2d_n^2 2^{d_n/2}}\sum_{k=0}^{d_n/2}\binom{d_n/2}{k}\delta(x-(\frac{4k}{d_n}-1))$. The text notes that, unlike the Haar case, the SPS density does not become smooth after ensemble averaging while both SPFs remain smooth, presented as an advantage of the SPF over the point measure, and reports numerical results supporting the density and its SPF.

**System.** subset phase states with $|S|=2^n$

**Comparison baseline.** the averaged Haar density $d\mu_{{\rm Haar}_n}$, which is smooth

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) $|S|=2^n$
- `condition:source-2` (SOURCE_EXPLICIT) the proof is stated to follow from the inverse Laplace transform of $e^\beta\mathcal{Z}^-_\beta({\rm SPS})$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] Closed form for the ensemble-averaged SPS Pauli-spectrum density as a sum of delta peaks.
  - target (EXACT): $d\mu_{\rm SPS}(x)=\dfrac{1}{d_{n}^{2}}\delta(x-1)+\dfrac{(d_{n}-1)(1+d_{n}/2)}{d_{n}^{2}}\delta(x)+\dfrac{(d_{n}-1)d_{n}}{2d_{n}^{2}\,2^{d_{n}/2}}\sum_{k=0}^{d_{n}/2}\binom{d_{n}/2}{k}\delta\!\left(x-\left(\tfrac{4k}{d_{n}}-1\right)\right)$.
    - quantifier: FORALL n in positive integers with $d_n=2^n$
    - assumption (SOURCE_EXPLICIT): the average SPF for SPS at $|S|=2^n$
    - assumption (SOURCE_EXPLICIT): the density is obtained by inverse Laplace transform of $e^{\beta}\mathcal{Z}^-_\beta({\rm SPS})$
    - normalization: the three weights sum to $1$ only if the binomial sum is normalized; the symbol $\mathcal{Z}^-_\beta$ used in the proof is not defined where it appears
  - residual: The quantity $\mathcal{Z}^-_\beta({\rm SPS})$ invoked in the proof is not defined at the point of use, so the derivation cannot be followed from the text alone.
- `component:conclusion-2` [CONCLUSION / MAPPED] Unlike the Haar case, the averaged SPS density does not reduce to a smooth function, while both SPFs are smooth; this is presented as an advantage of the SPF over the Pauli-spectrum point measure.
  - target (EXACT): $d\mu_{\rm SPS}$ remains purely atomic after ensemble averaging, whereas $d\mu_{{\rm Haar}_{n}}$ acquires an absolutely continuous part; both $\mathcal{Z}_{\beta}({\rm SPS})$ and $\mathcal{Z}_{\beta}({\rm Haar}_{n})$ are smooth in $\beta$.
    - quantifier: FORALL n in positive integers
    - assumption (SOURCE_EXPLICIT): the closed form of the preceding target is a finite sum of delta peaks
    - assumption (SOURCE_EXPLICIT): the averaged Haar density has an absolutely continuous component
    - normalization: smoothness here refers to the $\beta$-dependence of the SPF, not to the $x$-dependence of the measure
- `component:evidence-1` [EVIDENCE / RESIDUAL] Numerical results are reported as supporting the exact SPS density and its SPF.
  - residual: Referenced by figure only; no data, code, system sizes or tolerances are given, and nothing was recomputed in this run.

**Open items recorded against this claim.**

- [DATA_MISSING · computational_reproducibility] The numerical validation of the SPS density and SPF is referenced by figure with no data, code or parameters.
  - what could settle it: Author-supplied data or code.
- [MISSING_SOURCE · source_fidelity] The symbol $\mathcal{Z}^-_\beta({\rm SPS})$ used in the proof is not defined where it appears.
  - what could settle it: A definition or cross-reference for the parity-sector SPF.

Records: `claim:opus5-v11-2608.14798v1/A80`, `map:opus5-v11-2608.14798v1/A80`.

## A81 — Ensemble-averaged stabilizer Hamiltonian is not interchangeable with the averaged SPF

*Supplemental Material · Prose · modality ASSERTED · coverage COMPLETE*

**What the authors said.** The stabilizer-Hamiltonian construction is extended to ensembles: for Haar-random states, or any 1-design, $\mathbb{E}_{\psi\sim{\rm Haar}_n}[\hat H_s(\psi)]=\mathbb{I}^{\otimes2}-s\ket{\Phi^+}\bra{\Phi^+}$. The text emphasizes that $\mathcal{Z}_\beta({\rm Haar}_n)\neq\tr e^{-\beta\mathbb{E}_\psi[\hat H(\psi)]}$, the exact averaged SPF being the one given in the main text, though the approximation still yields qualitative coarse-grained features.

**System.** ensembles of $n$-qubit pure states, in particular Haar-random states and 1-designs

**Comparison baseline.** the exact ensemble-averaged SPF of Theorem (SPF for Haar-random states)

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the identity holds for Haar-random states or any 1-design
- `condition:source-2` (SOURCE_RECONSTRUCTED) $\hat H_s$ denotes the parity-sector component of the stabilizer Hamiltonian

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathbb{E}_{\psi\sim{\rm Haar}_n}[\hat H_s(\psi)]=\mathbb{I}^{\otimes2}-s\ket{\Phi^+}\bra{\Phi^+}$.
  - target (EXACT): $\mathbb{E}_{\psi\sim{\rm Haar}_{n}}\left[\hat{H}_{s}(\psi)\right]=\mathbb{I}^{\otimes2}-s\,\ket{\Phi^{+}}\bra{\Phi^{+}}$ for $s\in\{-1,1\}$, and the same holds for any 1-design.
    - quantifier: FORALL s in $\{-1,1\}$
    - quantifier: FORALL n in positive integers
    - assumption (SOURCE_RECONSTRUCTED): $\mathbb{E}_{\psi\sim{\rm Haar}}[\tr(\psi P)]=0$ for every non-identity Pauli, which holds for any 1-design
    - assumption (SOURCE_EXPLICIT): the stabilizer Hamiltonian construction of Perspective 1
    - normalization: $\hat H_s$ is the parity-sector operator; the subscript convention is used here without being reintroduced
- `component:limitation-1` [LIMITATION / MAPPED] $\mathcal{Z}_\beta({\rm Haar}_n)\neq\tr e^{-\beta\mathbb{E}_\psi[\hat H(\psi)]}$: averaging the Hamiltonian is not the same as averaging the partition function, though the approximation retains coarse-grained features.
  - target (EXACT): $\mathcal{Z}_{\beta}({\rm Haar}_{n})=\mathbb{E}_{\psi}\left[\tfrac12\tr e^{-\beta\hat{H}(\psi)}\right]\neq\tfrac12\tr e^{-\beta\,\mathbb{E}_{\psi}[\hat{H}(\psi)]}$ in general.
    - quantifier: FORALL n in positive integers
    - quantifier: FORALL \beta in range not stated
    - assumption (SOURCE_RECONSTRUCTED): $X\mapsto\tr e^{-\beta X}$ is not affine, so it does not commute with the ensemble average
    - normalization: the source writes $\tr$ without the factor $\tfrac12$ present in the SPF definition; recorded with the definition's normalization
  - residual: 'Qualitative (coarse grained) features' is not quantified, and the displayed non-equality drops the factor $\tfrac12$ of the SPF definition.

Records: `claim:opus5-v11-2608.14798v1/A81`, `map:opus5-v11-2608.14798v1/A81`.

## A82 — Large-beta limit of the SPF derived in the supplement

*Supplemental Material · Prose · modality ASSERTED · coverage COMPLETE*

**What the authors said.** The supplement derives the asymptotic large-$\beta$ behaviour of the SPF in direct analogy with the low-temperature limit of a canonical partition function, $\mathcal{Z}_\beta(\psi)\to\Omega(E_G)e^{-\beta E_G}(1+\dots)$ with $E_G$ the ground-state energy and $\Omega(E_G)$ its degeneracy, and concludes from the parity decomposition of the stabilizer Hamiltonian that $\mathcal{Z}_\beta(\psi)=\frac12\tr e^{-\beta\hat H(\psi)}=\frac12\tr_+e^{-\beta\hat H_+(\psi)}+\frac12\tr_-e^{-\beta\hat H_-(\psi)}\to\frac12|{\rm STAB}(\psi)|$.

**System.** $n$-qubit pure states, $\mathcal{H}_n\cong(\mathbb{C}^2)^{\otimes n}$, $d_n=2^n$, finite $n$

**Comparison baseline.** the low-temperature limit of an ordinary canonical partition function

**Stated conditions.**

- `condition:source-1` (SOURCE_EXPLICIT) the limit is $\beta$ large
- `condition:source-2` (SOURCE_EXPLICIT) the derivation uses the decomposition of the stabilizer Hamiltonian into parity subspaces, $\hat H=\hat H_+\oplus\hat H_-$
- `condition:source-3` (SOURCE_RECONSTRUCTED) the ground-state energy of $\hat H(\psi)$ is $E_G=0$ with degeneracy $|{\rm STAB}(\psi)|$

**Components, their accounting and their targets.**

- `component:conclusion-1` [CONCLUSION / MAPPED] $\mathcal{Z}_\beta(\psi)\to\frac12|{\rm STAB}(\psi)|$ as $\beta$ grows.
  - target (ASYMPTOTIC): $\mathcal{Z}_{\beta}(\psi)\ \xrightarrow[\beta\ \text{large}]{}\ \tfrac12|{\rm STAB}(\psi)|$, consistent with $\Omega(E_{G})e^{-\beta E_{G}}$ at $E_{G}=0$ and $\Omega(E_{G})=|{\rm STAB}(\psi)|$.
    - quantifier: FORALL \psi in pure states in $\mathcal{H}_n$
    - assumption (SOURCE_EXPLICIT): the ground level of $\hat H(\psi)$ sits at energy $0$ with degeneracy $|{\rm STAB}(\psi)|$
    - assumption (SOURCE_EXPLICIT): $\mathcal{Z}_\beta=\frac12\tr e^{-\beta\hat H}$ splits over the two parity sectors
    - normalization: this derivation confirms the Sec. III B value $|{\rm STAB}(\psi)|/2$ and is therefore the one that settles the inconsistency with the Sec. VI C statement, which omits the factor $\tfrac12$
    - approximation error: the subleading terms are written only as $(1+\dots)$, with no rate; the limit itself is stated as exact

**Open items recorded against this claim.**

- [CONFLICT · source_fidelity] This supplementary derivation gives $\lim_{\beta\to\infty}\mathcal{Z}_\beta=\tfrac12|{\rm STAB}(\psi)|$, matching Sec. III B; the Sec. VI C statement of the same limit omits the factor $\tfrac12$.
  - what could settle it: Errata on the Sec. VI C sentence; the supplement settles which value is intended.

Records: `claim:opus5-v11-2608.14798v1/A82`, `map:opus5-v11-2608.14798v1/A82`.

