# 交付边界与后续开发顺序

当前阶段补充（2026-09-15）：六个 Agent 规范与底层图接口已补齐；同日另轮为 Dependency / Review / Utility 增加最小检查器并接入统一 runner。当前轮按用户要求复核已完成的测试日志，并新增 [Pydantic AI 宿主参考版](host_reference/README.md)：固定控制流、模型适配代码及强制后端接口；没有执行新测试、模型、迁移或部署。待设计、待实现及待测项统一在 [PENDING_TESTS.md](PENDING_TESTS.md)。下文验收方法是后续规划，不构成自动运行指令。

已有 `handoff/` 只覆盖从保留 Paper 候选到 Dependency 本地引用扫描的单机路径，不能据此把下文的通用模型调度、独立审阅或 SQL/Git/Neo4j 集成标记完成。

新接口落地时先采用单进程的任务/存储/证据门/关系/归档五个逻辑模块，不先拆微服务。参考版已明确模块间调用和端口，但数据库与身份服务尚未接通。Neo4j 是关系模块的一种后端，缺失时不阻止已知引用上的合法任务。本地封存与 Git 归档分开推进；旧 runtime.sql 的非空 Git 检查点和全局视图头仍需专门适配，新 profile 未接通前不启用。新增图 schema 未注册到根五文件加载器，本次没有可宣称通过的图接口测试。

## 1. 本轮要完成的事情

用户目标是建立 `schema v0.1/`，规范多 agent 最小接口并完成底层系统规划，尤其是 Neo4j 与 GitHub repo 的分工和讨论。**不是本轮部署全部 agent、迁移数据库或完成真实论文的科学验证。**

| 要求 | 交付位置 | 验证方法 |
|---|---|---|
| 独立的新目录，保留旧业务含义 | README、compatibility.lock.json、COMPATIBILITY | 新包只引用 v0.0；校验实际旧 bundle 身份和 64 种类型 |
| 每个 agent 最小输入、输出、原则、停止与交接 | AGENT-CONTRACTS、agents.json | 9 种工作模板、23 项操作；操作唯一，所有业务短类型确实属于 v0.0 |
| 可调用的共同接口 | task/result/common/agent-registry schema | 合法样例与越权类型、未知字段、错误版本、错绑目标等反例 |
| 明确 agent 工作顺序 | SCHEDULING | 依赖与证据门、并行、范围变更、预算、重试、局部阻塞有规范；离线工作图检查无环和完整前置引用 |
| 数据格式固定，杂务不另用模型决策 | Utility 操作与 STORAGE | 模型不获得数据库写权，组包/校验/保存/投影由固定程序负责 |
| SQL 运行与候选存储 | STORAGE、storage/runtime.sql | DDL 在空 SQLite 内实际执行；约束与非法操作测试；现有 V3 业务表不重复创建 |
| GitHub repo 格式、封存与恢复 | STORAGE、archive-batch schema | 完整批次字节/引用检查、非覆盖封存协议、失败恢复和大附件边界 |
| Neo4j 结构与 Docker 规划 | STORAGE、storage/neo4j*.cypher、compose.example.yaml | 依据官方文档核对；静态参考。未在 Docker/Neo4j 中执行，不宣称部署成功 |
| 旧 schema 复用与缺口 | COMPATIBILITY | 业务记录归属映射，已定义/已有本地机制/待接通分开 |
| 可读性与工程反方检查 | 多 agent 独立审阅，结果见 VALIDATION | 不用任务完成替代科学成立；检查主线清晰与接口遗漏 |

`validate.py` 是有界离线参考检查器，不是生产认证器。`valid=true` 只表示所报告范围的检查通过。

## 2. 哪些真的能运行

当前可运行：旧契约 pin 核对、本包 JSON Schema 离线解析、操作输入/输出白名单、最小直接输入检查、任务精确目标与可见输入声明绑定、声明回避名单、工作图结构检查、候选导出字节及批内引用闭包校验，以及 SQL 参考 DDL 的本地测试。

以下仍需后续实现，不能靠更多 schema 字段替代：

- 模型调用的实际接通与 paper/proof/dependency 语义生产验收；`host_reference/` 已有 Pydantic AI 调用代码，但未安装依赖、执行调用或接通运行后端。
- 运行队列、任务领取、预算扣留、租约回收、幂等重试及取消。
- 实际执行身份、生产参与者闭包、权限隔离和反向解释的可见内容控制。
- 目标级 EVIDENCE 判断及有条件结果的安全复用。
- V3 业务记录与 outbox 的同事务接入、Git 导出器和远端读回核验。
- 完整 Neo4j 导入器，包括全部上下文、规则、证据、路线和冲突映射及就绪门。
- 独立 Lean 检查、实际声明/公理检查、原文对齐及需要的科学审阅。
- 正式发布、科学准入、生产容量、故障恢复和真实两论文验收。

离线操作最低输入检查仅覆盖能从工作单直接判定的必要条件；包内引用的具体数学条件、各类审阅的完整证据组合和所有规范权限仍由 v0.0 校验及未来受控运行/科学审阅承担。不能把白名单允许某类输入误解为这类证据已经足够。

## 3. 接下来按什么顺序开发

### 第一步：实现单个可恢复工作单

接入固定 Task 与真实 Result 捕获，保存精确 task 字节、模板/配置/模型/代码身份和所用规则。先使用无科学声明的测试 handler 验证领取、超时、回执、重试与恢复，再接真实 Paper 操作。

验收：重启后知道哪些任务交付、哪些等待；相同输入重送不重复记账；迟到 worker 不能覆盖新 attempt；不能只改 principal 字符串骗过独立性要求。

### 第二步：接一条 claim 的实质工作链

来源取得 → Paper 原文与数学拆解 → 独立确定范围 → Proof 与 Dependency 按缺口协作 → 独立审阅处置。每个节点都能保存、重新读取和继续；缺内容保留缺口而不是填假记录。

验收：读者能从结果追到原文字节、假设、共同前提和未解项。优先补真实语义工作，不以全文词法命中数代替科学主张数。

### 第三步：接入原子候选保存和封存

为现有 LocalStore 加入受控事务边界，让业务记录、运行事件和 outbox 一起提交；实现 self-contained 批次导出、离线验证与不可变本地封存。独立安排 Git commit/push 后精确读回，图投影无需等待远端。先补本地封存回执、检查点及分区头的持久化 profile，不能直接把无 Git 的新流程写入旧 DDL。网络动作不在 SQL 长事务里执行。

验收：在 SQL commit 后、Git commit 后、push 后、回执写入前分别中断，都能恢复且不覆盖旧身份；未封存 SQL 数据有备份恢复边界。

### 第四步：构建可重建的 Neo4j 图

按 GRAPH-INTERFACE 先实现独立 schema 注册/语义检查和同接口的有界封存文件参考查询，再接 Neo4j 适配器。实现完整映射和批次隔离，只在精确集合、共同前提、路线与闭包全部检查后切换分区可见头；查询必须指定批次。图结果回源核验后仅进入新 Task，不扩大正在执行任务的可见输入。

验收：丢弃图投影后，从指定已验证本地/Git 批次重建得到同一逻辑节点/关系集合；参考与 Neo4j 后端在未截断的相同请求上返回相同语义结果。不得从多个批次拼接不存在的证明，不用截断结果宣称完整，候选图不成为正式准入图。具体待测场景统一见中央 NG-*，此处不授权执行。

### 第五步：按需接形式化，并用两篇真实论文验收

已有合适声明先复用；缺少时生成形式代码并独立运行检查、反向解释和对齐。比较一项真正通过用途检查的复用和一项因条件不匹配被拒绝的复用，再做一次上游修订与局部重审。

验收：能说明节省了哪段实际工作、仍承担哪些假设、拒绝复用的具体原因。未测量成本节约时不宣称低价模型一定足够；低成本模型先用于已有评测覆盖的操作。

## 4. 运行层参考选型：Pydantic AI（参考实现，未生产接通）

2026-09-15 按用户要求，将原建议推进为 [host_reference](host_reference/README.md)。这是一个独立目录内的实现参考，不等于决定将 SDK 作为全部底层基础设施，也没有安装到仓库主环境。

### 4.1 框架与宿主各负责什么

| 层 | 本次决定 |
|---|---|
| 业务记录与 Agent 接口 | 原 JSON Schema 和规范继续是真源，不用 Pydantic 重新生成或改变包身份 |
| 模型调用适配 | 使用 Pydantic AI 的结构化输出、输出检查回调、消息与用量接口；参考代码已写，未执行 |
| 宿主控制流 | 自己的固定程序负责前置核对、冻结输入、预算/租约接口、草稿暂存及交付关卡，不是模型 Agent |
| 数据库、身份、固定程序、图、归档 | 显式端口；真实事务和服务适配尚待实现，不由 SDK 自动提供 |

模型只获得工作单明确允许的材料。源码中只有专业任务调用，没有把 SQL、Neo4j、Git 或 shell 暴露为自由选择的模型工具。[Pydantic AI 概览](https://pydantic.dev/docs/ai/overview/)

### 4.2 对原选型说明的纠正

- **工具未注册不等于“物理不可能越权”。** SDK 路由限制之外，仍需受控供应商配置、网络/进程隔离、文件读取授权及固定程序白名单。
- **依赖注入不等于盲审隔离。** brief、记录外壳、附件、工具输出或历史消息都可能泄露原目标；实际发送内容必须单独检查。[依赖注入文档](https://pydantic.dev/docs/ai/core-concepts/dependencies/)
- **Trace 不等于真实身份或独立性。** 必须有可信执行边界及生产参与者闭包；模型自报 principal 或一个追踪 ID 均不够。
- **Durable execution 需要后端集成。** 本参考没有接持久执行服务，也没有实现数据库领取/恢复事务，不能用“支持 durable”替代这些工作。[持久执行文档](https://pydantic.dev/docs/ai/capabilities/durable_execution/overview/)
- **结构化输出不自动完整验证原 schema。** `StructuredDict` 仍需原检查器；草稿的 payload 验证也不覆盖完整业务记录的根级条件与 RecordSet 关系。复杂 schema 能否被特定 provider 接受须另行验证，不能承诺无损自动生成必然可用。[输出文档](https://pydantic.dev/docs/ai/core-concepts/output/)

### 4.3 结合已完成测试落实的宿主关卡

Planner 的 `FOLLOW_UP_BLOCKED` 与 Delta 的多目标检查已有新执行证据；Dependency / Review / Utility 已有最小检查器。参考宿主直接适配这些检查器，而不是另写一套较弱判定。

但“检查通过”仍只属于报告覆盖范围：派单前重新核对真实前置；模型输出后先保存草稿；Delta 全空草稿或未逐目标交代的诊断不能当完整比较；完整记录、独立身份和字节闭包在正式交付前另行检查。旧日志与本轮新增待测项见 [PENDING_TESTS §8–9](PENDING_TESTS.md)。

### 4.4 开发顺序

先落实 `RuntimePort` 的单任务领取、不可变输入/草稿留存和业务/Result/outbox 同事务，再接真实 `EvidenceGate`、Utility 固定程序与专用装配。按中央清单取得用户测试授权后，才验证这一轮的新代码。

参考一次 attempt 最多一个 SDK 请求，SDK 自动重试关闭；宿主另行决定后续修订和计费。依赖版本独立固定为参考选择，尚未解析或锁定；供应商 HTTP 重试、token 上限与实际费用之间的差异不能忽略。[用量文档](https://pydantic.dev/docs/ai/api/pydantic-ai/usage/)

## 5. 以后演进契约的规则

当前包是实验版。运行宿主必须把模板目录、JSON schemas、提示配置、处理规则、代码与模型设置的实际字节身份纳入每次 attempt 的输入清单，不能只记录目录名或 `0.1.0` 字符串；`runtime_attempts.input_manifest_sha256` 为这份受控清单预留绑定位置。

字段含义、输出权限或调度规则改变时，发布新契约版本并保留旧解释器/迁移说明。已有任务字节和业务结果不回写。当前 v0.0 的 pin 变化会让离线检查失败；必须显式审阅兼容性后更新，不允许运行时自动追随最新版。
