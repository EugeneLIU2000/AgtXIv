[project]
name = "agtxiv-host-reference"
version = "0.1.0"
description = "Unexecuted, explicit-port Pydantic AI host reference for AgtXIv"
requires-python = ">=3.12,<3.13"
dependencies = [
  "pydantic==2.13.5",
  "pydantic-ai-slim==2.43.0",
  "jsonschema[format-nongpl]==4.23.0",
  "referencing==0.30.2",
]

[tool.uv]
package = false

# Reference selection only: no install, dependency resolution or lock generation
# has been performed. Provider extras/credentials are deliberately not selected.
