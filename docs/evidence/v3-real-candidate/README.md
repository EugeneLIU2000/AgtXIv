# Real-paper candidate-loop evidence / 真实论文候选闭环证据

**Engineering goal completed within provisional scope; scientific verification remains unperformed.** The selected paper is *Application of a Resource Theory for Magic States to Fault-Tolerant Quantum Computing*. Selection, assumptions and independent static source-grounding boundaries are documented in [source-review.md](source-review.md). See the [bilingual mechanism guide](../../architecture/v3-real-candidate-loop.md) for repeatable commands.

## Actual observation

Capture time: **2026-09-07T19:42:19Z**. The live source directory contained **14 files / 1,542,636 bytes**: six original/archive files and eight local generated `build/` files. These generated files were inventoried, not executed or promoted to scientific evidence.

- **548 main-text lines:** 240 tentative lexical claim-cue lines, 204 nonclaim-cue lines and 104 unknown lines. These are not counts of semantic scientific claims; the semantic denominator remains `null`.
- **1,095 exact text spans** across supported text files, each tied to original bytes.
- **6 proposed interpretations:** definition, channel mixture action, feasible output, contraction, R3 monotonicity and a separate postselection extension.
- **35 candidate records + 1 unadopted proposed policy:** 6 source spans, 6 argument nodes, 4 joint inference steps, 5 dependency bindings, 12 open frontier items, source snapshot and paper structure. The policy grants no authorized principals and provides no identity attestation.
- **9 proposed obligations**, all `OPEN / DEFERRED`, `performed: false`, with no successful evidence and no frozen-scope check.

## Evidence files

| File | Meaning |
|---|---|
| [input-inventory.json](input-inventory.json) | Exact captured file denominator, hashes, categories, generated-file distinction and missing-material notes |
| [candidate-inventory.json](candidate-inventory.json) | All static text spans and cue buckets, six proposed interpretations, exact record references, deferred obligations and unresolved questions |
| [records.json](records.json) | Actual 36 V3 records including the explicitly unadopted context; not synthetic fixtures or an approved package |
| [dependency-impact.json](dependency-impact.json) | Existing potential-impact analysis over exact records: definition dependencies reach R3 but not the separate postselection proposal |
| [archive-correspondence.json](archive-correspondence.json) | Five regular archive members match their local counterparts, without filesystem extraction or source execution |
| [closure-verification.json](closure-verification.json) | Complete record/raw-byte validation; fresh database import; separate read-only process reopen; byte-identical known-observation replay; no knowledge head, freeze or admission |
| [contracts.junit.xml](contracts.junit.xml) | Final full V3 regression run: **710 passed, zero failures/errors/skips** |
| [verification.json](verification.json) | Command outcomes, exact code/test/evidence hashes, pinned-input integrity and scope boundaries |

The full retained databases and build/query/replay outputs are in ignored local directory `tmp/v3-real-candidate/capture-g7f2mr3a/`. Their exact paths, sizes and hashes are recorded in the closure report. Large raw byte stores are not committed. If that ignored directory is removed, the evidence exports remain readable, but the old full-byte historical query requires restoring those retained artifacts; a new capture may differ from the old observation.

## Review and execution boundaries

Independent engineering review found and repaired duplicate record membership, circular proposed support, newline-driven memory amplification, impossible replayed intake limits and insufficient generic main-file identity checking. The final independent candidate suite passed **62 tests**; the parent then ran all **710 V3 tests**. No unresolved major engineering findings remained in that reviewed scope.

Schema generation, the pinned upstream protocol check, offline bundle validation and Python compilation passed. Repository-wide profiles were not run because they can invoke out-of-scope workflows. No paper demo, vibefeld executable, Go, Lean, scientific pipeline, network download, publication or formal admission was run. Existing source-directory build files do not constitute executions by this task.

Earlier attempts exposed engineering issues and are not represented as successful observations: three new adversarial regressions initially failed; the no-symlink writer rejected the macOS `/tmp` alias until a resolved path was used; an evidence-export helper rejected a tuple passed to the strict canonical JSON encoder and was corrected to explicit JSON-compatible lists. Some earlier broad runs hit a 120-second timeout; the final full run completed with sufficient time. None of these required changing the fixed paper inputs.

## 中文说明

这里交付的是一篇真实论文的**可重放候选调查闭环**，不是论文已获验证。全部文件已清点并保留字节身份；全文词法候选线索可审阅，代表性 R3 链可追溯到原文，并保留条件、依赖和未解决前沿。

实际验证了空数据库导入、不同进程只读重开、确切历史包查询，以及在同一已知观察时间和输入下逐字节相同的重放。潜在依赖会到达 R3，不会把独立的后选择断言错误接成已支持的后果。九项候选义务均未完成；没有假造正式冻结、审阅凭据、形式证明或知识准入。

下一步仍需逐条科学主张/非主张审阅、独立政策下的范围冻结、补充材料与外部引用核查，以及实际科学/形式证据。本地哈希和绿色工程测试不能替代这些工作。
