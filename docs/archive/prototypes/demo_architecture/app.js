const nodes = [
  {id:'exact_paper_release',type:'external',label:'Exact Paper Release',sub:'Pinned source bytes and hash',tag:'canonical input',pos:[30,30],size:[220,82],cn:'精确论文版本：固定的来源字节和哈希。',boundary:'固定来源，不等于已经审阅。'},
  {id:'profile_scope',type:'security',label:'Agentization Profile / Inventory Scope',sub:'Exact obligations and inventory',tag:'completion boundary',pos:[30,145],size:[270,92],cn:'处理标准与全文清点范围：规定这次必须交代什么。',boundary:'固定处理分母，不能为了通过而删项。'},
  {id:'paper_analysis',type:'backend',label:'Profile-Scoped Paper Analysis',sub:'Whole release; query-independent',tag:'coordinator',pos:[330,85],size:[230,88],cn:'按固定标准处理整篇论文，而不是只回答某一个问题。',boundary:'Query 只能作为触发原因，不能决定永久范围。'},
  {id:'claims_math_ir',type:'database',label:'Claims & MathClaimIR',sub:'Source-bound claims and mathematical IR',tag:'canonical records',pos:[590,85],size:[220,88],cn:'保存原文主张，并把数学目标整理成可交接的中间表示。',boundary:'作者主张和系统派生命题要分开。'},
  {id:'dependencies_inferences',type:'messagebus',label:'Dependencies & Inferences',sub:'Exact dependency and inference order',tag:'verification order',pos:[840,80],size:[240,98],cn:'明确哪些前提、定义和推理步骤真正承重。',boundary:'联合前提不能拆成互不相关的边。'},
  {id:'formalization',type:'backend',label:'Formalization',sub:'Requests and generated artifacts',tag:'generation only',pos:[1130,85],size:[210,88],cn:'生成形式化请求和文件，但生成本身不是独立核查。',boundary:'生成者不能把自己的构建报告当成独立证据。'},
  {id:'formal_verification',type:'security',label:'Formal Verification',sub:'Independent kernel evidence',tag:'separate axis',pos:[1130,245],size:[210,92],cn:'在固定环境中检查实际参与构建的形式声明。',boundary:'形式通过不自动证明物理含义。'},
  {id:'backtranslation_alignment',type:'security',label:'Backtranslation & Source/IR/Formal Alignment',sub:'Independent round-trip alignment',tag:'separate axis',pos:[770,240],size:[330,102],cn:'检查原文、数学交接单和形式声明是否表达同一件事。',boundary:'量词、前提增强或结论变弱都必须显式记录。'},
  {id:'residual_semantics',type:'backend',label:'Residual Semantics',sub:'Physical and non-mathematical meaning',tag:'preserved residuals',pos:[520,245],size:[220,92],cn:'保留数学之外的物理对象、近似、可观测量和适用范围。',boundary:'不能把剩余科学含义丢给形式系统。'},
  {id:'assessment_selection',type:'database',label:'Assessment Selection',sub:'Exact typed AssessmentRecord revisions',tag:'typed authority',pos:[270,245],size:[220,92],cn:'选择精确版本的评估记录，分别回答六个问题。',boundary:'不生成一个全局 verified 标签。'},
  {id:'release_audit',type:'security',label:'Root Agent Verification',sub:'Ordered audit and recommendation',tag:'accountable authority',pos:[30,245],size:[210,92],cn:'按依赖顺序审计候选包，保留失败、阻塞和混合状态。',boundary:'审计者不能生产自己正在审计的证据。'},
  {id:'release_certifier',type:'security',label:'Release Certifier',sub:'Mechanical release checks',tag:'independent certifier',pos:[30,390],size:[210,92],cn:'只做机械一致性和绑定检查，不重新做科学判断。',boundary:'认证不等于包内每个主张都为真。'},
  {id:'paper_agent_release',type:'cloud',label:'Paper Agent Release',sub:'Certified package; mixed states',tag:'immutable release',pos:[270,390],size:[210,92],cn:'一篇论文的不可变发布包，允许其中保留未解决条目。',boundary:'发布包不是知识库准入本身。'},
  {id:'package_archive',type:'cloud',label:'Package Archive',sub:'Exact package storage target',tag:'V2 target service',pos:[510,390],size:[200,92],cn:'保存发布包、审计、认证和历史检查点。',boundary:'归档不等于逐条知识准入。'},
  {id:'reusable_entry_admission',type:'backend',label:'Reusable Entry Admission',sub:'Atomic KnowledgeIngestionReceipt',tag:'implemented contract',pos:[740,390],size:[220,92],cn:'逐条决定哪些记录可以进入知识快照。',boundary:'接受、拒绝和阻塞必须全部记账。'},
  {id:'knowledge_base',type:'database',label:'Domain-Qualified Knowledge Base',sub:'Verification-aware immutable views',tag:'conflicts preserved',pos:[990,385],size:[350,102],cn:'按领域累积不可变知识视图，并保留冲突两端。',boundary:'知识库里的条目不自动成为无条件真理。'},
  {id:'query_resolution',type:'frontend',label:'Query Resolution',sub:'Read-only conflict-preserving closure',tag:'downstream consumer',pos:[990,525],size:[300,88],cn:'从精确知识快照中读取结论、依据、条件和冲突。',boundary:'Query 只读，不改写正式知识。'},
  {id:'query_intent',type:'external',label:'Query Intent',sub:'Question or knowledge gap',tag:'user input',pos:[30,525],size:[220,82],cn:'用户的问题或需要补足的知识缺口。',boundary:'问题可以触发任务，但不能缩小论文范围。'},
  {id:'provisional_fast_path',type:'backend',label:'Provisional Fast Path',sub:'Candidates plus canonical intake trigger',tag:'no canonical writes',pos:[300,520],size:[250,92],cn:'快速给出候选和入口，但不写入正式知识。',boundary:'临时回答不是正式科学结论。'}
];

const connections = [
  ['exact_paper_release','paper_analysis','exact source'],['profile_scope','paper_analysis','profile + scope'],['paper_analysis','claims_math_ir','claim inventory'],['claims_math_ir','dependencies_inferences','exact claims and IR'],['dependencies_inferences','formalization','ordered targets'],['formalization','formal_verification','verification target'],['formal_verification','backtranslation_alignment','kernel evidence'],['backtranslation_alignment','residual_semantics','alignment result'],['residual_semantics','assessment_selection','residual disposition'],['assessment_selection','release_audit','selected revisions'],['release_audit','release_certifier','audit + recommendation'],['release_certifier','paper_agent_release','mechanical certification'],['paper_agent_release','package_archive','exact certified package'],['package_archive','reusable_entry_admission','admission candidates'],['reusable_entry_admission','knowledge_base','atomic receipt'],['knowledge_base','query_resolution','relation closure'],['query_intent','provisional_fast_path','fast request'],['provisional_fast_path','query_resolution','provisional response']
];

const views = {
  construction:{title:'Construction / 构建',note:'Whole-paper construction is exact-profile, exact-scope, and query-independent.',focus:['exact_paper_release','profile_scope','paper_analysis','claims_math_ir','dependencies_inferences','formalization']},
  verification:{title:'Verification / 核查',note:'Independent producer records precede the accountable Root Agent audit and mechanical certification.',focus:['formal_verification','backtranslation_alignment','residual_semantics','assessment_selection','release_audit','release_certifier']},
  release_knowledge:{title:'Release & Knowledge / 发布与知识',note:'Package archival and reusable entry admission are distinct lifecycle gates.',focus:['paper_agent_release','package_archive','reusable_entry_admission','knowledge_base','query_resolution']},
  provisional:{title:'Provisional Path / 临时路径',note:'Fast assistance remains non-authoritative and links query-independent canonical intake.',focus:['query_intent','provisional_fast_path','query_resolution','profile_scope']}
};

const kindNames = {external:'EXTERNAL',backend:'BACKEND',database:'KNOWLEDGE',security:'GATE / REVIEW',messagebus:'ORDER / ROUTE',cloud:'RELEASE',frontend:'QUERY'};
const nodeMap = Object.fromEntries(nodes.map((node) => [node.id,node]));
const svg = document.querySelector('#architecture-svg');
const NS = 'http://www.w3.org/2000/svg';
let selectedId = 'exact_paper_release';
let activeView = 'construction';
let routePath = [];

function el(name, attrs = {}, text = '') { const node = document.createElementNS(NS, name); Object.entries(attrs).forEach(([key,value]) => node.setAttribute(key,value)); if (text) node.textContent = text; return node; }
function wrap(text, max = 25) { const words = text.split(' '); const lines = []; let line = ''; words.forEach((word) => { if ((line + ' ' + word).trim().length > max && line) { lines.push(line); line = word; } else line = (line + ' ' + word).trim(); }); if (line) lines.push(line); return lines.slice(0,3); }
function center(node) { return {x:node.pos[0] + node.size[0] / 2, y:node.pos[1] + node.size[1] / 2}; }
function endpoint(source, target) { const s = center(source); const t = center(target); const right = source.pos[0] + source.size[0]; const left = target.pos[0]; const fromRight = t.x >= s.x; return {x1:fromRight ? right : source.pos[0], y1:s.y, x2:fromRight ? left : target.pos[0] + target.size[0], y2:t.y}; }

function draw() {
  svg.querySelectorAll('[data-rendered]').forEach((node) => node.remove());
  const defs = el('defs', {'data-rendered':'true'}); const marker = el('marker',{id:'arrow',viewBox:'0 0 8 8',refX:'7',refY:'4',markerWidth:'6',markerHeight:'6',orient:'auto'}); marker.appendChild(el('path',{d:'M0 0 8 4 0 8z',fill:'#6f8da7'})); defs.appendChild(marker); svg.appendChild(defs);
  const canonical = nodes.filter((node) => !['package_archive','reusable_entry_admission','knowledge_base','query_resolution','query_intent','provisional_fast_path'].includes(node.id));
  const rect = (items,pad,label,klass) => { const minX=Math.min(...items.map(n=>n.pos[0]))-pad; const minY=Math.min(...items.map(n=>n.pos[1]))-pad; const maxX=Math.max(...items.map(n=>n.pos[0]+n.size[0]))+pad; const maxY=Math.max(...items.map(n=>n.pos[1]+n.size[1]))+pad; const group=el('g',{'data-rendered':'true'}); group.appendChild(el('rect',{x:minX,y:minY,width:maxX-minX,height:maxY-minY,rx:12,class:`lane ${klass}`})); group.appendChild(el('text',{x:minX+12,y:minY+18,class:`lane-label ${klass}`},label)); return group; };
  svg.appendChild(rect(canonical,18,'CANONICAL PAPER AGENTIZATION / RELEASE','canonical'));
  svg.appendChild(rect(nodes.filter((node)=>['package_archive','reusable_entry_admission','knowledge_base'].includes(node.id)),16,'ARCHIVE / REUSABLE KNOWLEDGE LIFECYCLE','archive'));
  const edges = el('g',{'data-rendered':'true'}); connections.forEach(([from,to,label])=>{const source=nodeMap[from],target=nodeMap[to],p=endpoint(source,target); const d=`M ${p.x1} ${p.y1} C ${(p.x1+p.x2)/2} ${p.y1}, ${(p.x1+p.x2)/2} ${p.y2}, ${p.x2} ${p.y2}`; const path=el('path',{d,class:'edge',id:`edge-${from}-${to}`,'data-from':from,'data-to':to,'marker-end':'url(#arrow)'}); edges.appendChild(path); if (from===selectedId || to===selectedId) { const text=el('text',{x:(p.x1+p.x2)/2,y:(p.y1+p.y2)/2-5,class:'edge-label','data-edge-label':`${from}-${to}`},label); edges.appendChild(text); }}); svg.appendChild(edges);
  const nodeLayer=el('g',{'data-rendered':'true'}); nodes.forEach((node)=>{const group=el('g',{class:`node ${node.type}`,tabindex:'0',role:'button','aria-label':`Select ${node.label}`,'data-node-id':node.id}); const [x,y]=node.pos,[w,h]=node.size; group.appendChild(el('rect',{x,y,width:w,height:h,rx:8})); const titleLines=wrap(node.label,26); const title=el('text',{x:x+13,y:y+25,class:'node-title'}); titleLines.forEach((line,i)=>title.appendChild(el('tspan',{x:x+13,dy:i===0?0:15},line))); group.appendChild(title); const subY=y+25+Math.min(titleLines.length,3)*15+4; wrap(node.sub,32).slice(0,2).forEach((line,i)=>group.appendChild(el('text',{x:x+13,y:subY+i*12,class:'node-sub'},line))); group.appendChild(el('text',{x:x+13,y:y+h-10,class:'node-tag'},node.tag.toUpperCase())); group.addEventListener('click',()=>selectNode(node.id)); group.addEventListener('keydown',(event)=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();selectNode(node.id);}}); nodeLayer.appendChild(group); }); svg.appendChild(nodeLayer); applyFocus(); }

function applyFocus(){ const focus=new Set(views[activeView].focus); svg.querySelectorAll('[data-node-id]').forEach((node)=>node.classList.toggle('dimmed', !focus.has(node.dataset.nodeId))); svg.querySelectorAll('.edge').forEach((edge)=>{const active=focus.has(edge.dataset.from)&&focus.has(edge.dataset.to);edge.classList.toggle('active',active);edge.classList.toggle('dimmed',!active);}); svg.querySelectorAll('[data-node-id]').forEach((node)=>node.classList.toggle('selected',node.dataset.nodeId===selectedId)); applyRoute(); }
function selectNode(id){ selectedId=id; const node=nodeMap[id]; const incoming=connections.filter((edge)=>edge[1]===id).map((edge)=>nodeMap[edge[0]].label); const outgoing=connections.filter((edge)=>edge[0]===id).map((edge)=>nodeMap[edge[1]].label); document.querySelector('#inspector-type').textContent=`${kindNames[node.type]} · ${node.tag.toUpperCase()}`; document.querySelector('#inspector-id').textContent=node.id; document.querySelector('#inspector-title').textContent=node.label; document.querySelector('#inspector-cn').textContent=node.cn; document.querySelector('#inspector-sub').textContent=node.sub; document.querySelector('#inspector-tag').textContent=node.tag; document.querySelector('#inspector-incoming').textContent=incoming.length?incoming.join(' · '):'—'; document.querySelector('#inspector-outgoing').textContent=outgoing.length?outgoing.join(' · '):'—'; document.querySelector('#inspector-boundary').textContent=node.boundary; document.querySelector('#inspector-note').textContent=`${node.label} 是流程中的一个明确边界。点击其他节点，查看它如何接入整条 paper-first 链条。`; applyFocus(); }
function selectView(id){ activeView=id; const view=views[id]; document.querySelector('#view-title').textContent=view.title; document.querySelector('#view-note').textContent=view.note; document.querySelector('#diagram-status').textContent=`VIEW / ${id.replace('_',' ').toUpperCase()}`; document.querySelectorAll('.view-tab').forEach((tab)=>{const active=tab.dataset.view===id;tab.classList.toggle('is-active',active);tab.setAttribute('aria-selected',String(active));}); applyFocus(); }
function findRoute(start, end){ if(start===end) return [start]; const queue=[[start]]; const visited=new Set([start]); while(queue.length){ const path=queue.shift(); const last=path[path.length-1]; for(const [,to] of connections.filter((edge)=>edge[0]===last)){ if(visited.has(to)) continue; const next=[...path,to]; if(to===end) return next; visited.add(to); queue.push(next); } } return []; }
function populateRouteSelects(){ const labels=nodes.map((node)=>`<option value="${node.id}">${node.label}</option>`).join(''); document.querySelector('#route-from').innerHTML=labels; document.querySelector('#route-to').innerHTML=labels; document.querySelector('#route-from').value='exact_paper_release'; document.querySelector('#route-to').value='query_resolution'; }
function applyRoute(){ const routeNodes=new Set(routePath); const routeEdges=new Set(routePath.slice(0,-1).map((id,index)=>`${id}-${routePath[index+1]}`)); svg.querySelectorAll('[data-node-id]').forEach((node)=>node.classList.toggle('route-active',routeNodes.has(node.dataset.nodeId))); svg.querySelectorAll('.edge').forEach((edge)=>edge.classList.toggle('route-active',routeEdges.has(`${edge.dataset.from}-${edge.dataset.to}`))); }
function traceRoute(){ const start=document.querySelector('#route-from').value; const end=document.querySelector('#route-to').value; routePath=findRoute(start,end); const result=document.querySelector('#route-result'); if(!routePath.length){ result.textContent=`没有找到从 “${nodeMap[start].label}” 到 “${nodeMap[end].label}” 的有向路径。方向 matters：图中没有的连接不会被补出来。`; } else { result.innerHTML=`<span class="route-count">${routePath.length - 1} edges</span> ${routePath.map((id)=>`<button type="button" data-route-node="${id}">${nodeMap[id].label}</button>`).join('<b>→</b>')}`; result.querySelectorAll('[data-route-node]').forEach((button)=>button.addEventListener('click',()=>selectNode(button.dataset.routeNode))); } applyRoute(); }
function clearRoute(){ routePath=[]; document.querySelector('#route-result').textContent='路径高亮已清除。选择起点和终点后，路径会在图中高亮。'; applyRoute(); }
document.querySelectorAll('.view-tab').forEach((tab)=>tab.addEventListener('click',()=>selectView(tab.dataset.view)));
document.querySelector('#reset-view').addEventListener('click',()=>{selectedId='exact_paper_release';selectView('construction');selectNode(selectedId);});
document.querySelector('#clear-selection').addEventListener('click',()=>{selectedId='';document.querySelector('#inspector-title').textContent='Select a node';document.querySelector('#inspector-cn').textContent='点击图中的模块，查看它在 paper-first 生命周期中的职责和边界。';document.querySelector('#inspector-sub').textContent='Node details appear here';document.querySelector('#inspector-type').textContent='SEMANTIC NODE';document.querySelector('#inspector-id').textContent='—';document.querySelector('#inspector-tag').textContent='—';document.querySelector('#inspector-incoming').textContent='—';document.querySelector('#inspector-outgoing').textContent='—';document.querySelector('#inspector-boundary').textContent='颜色表示角色，不表示通过或失败。';document.querySelector('#inspector-note').textContent='Query 是下游只读消费者；Provisional Path 不写入正式知识。';applyFocus();});
populateRouteSelects();
document.querySelector('#trace-route').addEventListener('click',traceRoute);
document.querySelector('#clear-route').addEventListener('click',clearRoute);
document.querySelector('#theme-toggle').addEventListener('click',()=>{const html=document.documentElement;html.dataset.theme=html.dataset.theme==='light'?'dark':'light';});
draw(); selectNode(selectedId);
