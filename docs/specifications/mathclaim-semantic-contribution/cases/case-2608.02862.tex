\subsection{Case 2608.02862: mixed attribution and baseline-sensitive rankings}\label{case:contrib-02862}

This numerical-analysis slice contains an imported entropy-maximality result and current numerical observations. Candidate \field{local-maximality-mismatch} mixes those sources under ambiguous attribution. No mathematical relation can repair the identity problem; reduction is blocked until the candidate is split or clause-level attribution becomes independently addressable.

The statement that energy criteria identify no single winning method and the ranking obtained from a combined energy--entropy functional are not contradictory or duplicate. They use different comparison functions and therefore have different baselines. Similarly, convergence of a Ces\`aro-averaged density does not entail a method ranking under entropy functionals.

Definitions of selection observables and the functionals built from them are connected construction steps: the chosen functionals depend on the observables, but the observable definitions do not logically entail that particular functional choice. Both remain visible in a contribution view because the paper contributes the operational selection procedure, not merely the terminal rankings. This case makes the baseline parameter of \ContributionProfile{} mandatory and shows why missing comparison dimensions must be unknown rather than zero.