import AgtXIvVarela.VRepConditional
import AgtXIvVarela.ExternalPerfectGraphFoundation

/-!
# AgtXIv formalization of the Varela projected stabilizer polytope

This package depends on the separately verified conceptual-root mathematics.
Its current public theorem is intentionally conditional: the exact coordinate
projection and the normalized maximal-context candidate family are concrete,
while the two load-bearing repaired-proof obligations remain visible theorem
premises until their Pauli/stabilizer proofs are closed.

The perfect-graph theorem is represented separately by exact finite-graph
definitions and an explicit foundation parameter.  This package does not
declare it as an axiom and does not prove it.
-/
